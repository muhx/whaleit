---
phase: 04-transaction-core
plan: 07
type: execute
wave: 5
depends_on: [04-05]
files_modified:
  - apps/frontend/src/pages/transactions/transaction-form.tsx
  - apps/frontend/src/pages/transactions/transfer-edit-alert-dialog.tsx
  - apps/frontend/src/pages/transactions/split-editor.tsx
  - apps/frontend/src/pages/transactions/category-autocomplete.tsx
  - apps/frontend/src/pages/transactions/transfer-form-fields.tsx
  - apps/frontend/src/pages/transactions/__tests__/transaction-form.test.tsx
  - apps/frontend/src/pages/transactions/__tests__/split-editor.test.tsx
autonomous: true
requirements: [TXN-01, TXN-02, TXN-07, TXN-08]
must_haves:
  truths:
    - "Direction ToggleGroup (Income/Expense/Transfer) is the form's first
      field; selected option shows `data-state=on` accent border + accent text
      per UI-SPEC §3 (single primary CTA + ToggleGroup active state are the two
      accent uses)"
    - "Income/Expense form has Amount, Account, Date, Payee, Category
      Autocomplete, optional Splits, optional Notes, optional FX-rate override
      (when currency != account.currency)"
    - "Transfer form has Source account + Destination account (must differ — Zod
      validation), Amount in source currency, Date, Notes; submits via
      `useCreateTransfer()` (D-01: paired rows; D-02: per-leg FX snapshot)"
    - "Editing a paired transfer leg's amount opens AlertDialog: 'Apply to both
      legs (preserves transfer pairing) or only this leg (breaks the link)?'
      (D-04). Confirming 'this leg only' calls `useUpdateTransferLeg(...,
      editMode='THIS_LEG_ONLY')` which triggers the service to clear
      `transfer_group_id` on the edited row"
    - 'Category Autocomplete uses `Autocomplete` with `+ Create category
      "{input}"` footer affordance hitting the existing taxonomy
      `createCategory` mutation (D-15 — pre-fills with merchant memory result on
      payee blur)'
    - "Split editor: minimum 2 rows (trash disabled when 2 remain); live
      remainder indicator (sum match/short/over with
      `text-success`/`text-warning`/`text-destructive`); submit gated until sum
      === amount exactly (per UI-SPEC §7)"
    - "Form validation messages (inline, `text-xs text-destructive`) match
      UI-SPEC §3 §Submission verbatim copy (e.g., 'Amount must be greater than
      0.', 'Source and destination accounts must be different.')"
  artifacts:
    - path: "apps/frontend/src/pages/transactions/transaction-form.tsx"
      provides:
        "New/Edit transaction form (Sheet desktop, Dialog mobile full-screen)"
      contains: "function TransactionForm"
    - path: "apps/frontend/src/pages/transactions/split-editor.tsx"
      provides: "Inline split-row editor with live remainder indicator"
      contains: "function SplitEditor"
    - path: "apps/frontend/src/pages/transactions/transfer-edit-alert-dialog.tsx"
      provides: "AlertDialog for D-04 amount-edit prompt"
      contains: "Apply to both legs"
  key_links:
    - from: "apps/frontend/src/pages/transactions/transaction-form.tsx"
      to: "apps/frontend/src/lib/schemas/transaction.ts"
      via:
        "useForm + zodResolver(NewIncomeOrExpenseTransactionSchema |
        NewTransferTransactionSchema | TransactionUpdateSchema)"
      pattern: "zodResolver"
    - from: "apps/frontend/src/pages/transactions/transaction-form.tsx"
      to: "apps/frontend/src/hooks/use-transactions.ts"
      via:
        "useCreateTransaction / useUpdateTransaction / useCreateTransfer /
        useUpdateTransferLeg"
      pattern: "useCreate"
    - from: "apps/frontend/src/pages/transactions/category-autocomplete.tsx"
      to: "apps/frontend/src/hooks/use-merchant-categories.ts"
      via: "useLookupPayeeCategory(accountId, payee)"
      pattern: "useLookupPayeeCategory"
---

<objective>
Build the manual entry / edit form for transactions, the AlertDialog for transfer-pair amount edits, the split editor, and the category Autocomplete with inline-create + payee-memory pre-fill.

Purpose: TXN-01 (manual add), TXN-08 (split entry), D-04 (paired-leg edit), D-15
(memory auto-fill on payee blur). All wired through plan 04-05 hooks. Output: A
form sheet that swaps fields by direction, validates inline, and submits via the
right mutation per type. Component tests cover the validation, the split-sum
gate, and the transfer AlertDialog branch. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-transaction-core/04-CONTEXT.md
@.planning/phases/04-transaction-core/04-RESEARCH.md
@.planning/phases/04-transaction-core/04-PATTERNS.md
@.planning/phases/04-transaction-core/04-UI-SPEC.md
@.planning/phases/04-transaction-core/04-05-PLAN.md
@apps/frontend/src/pages/settings/accounts/components/update-balance-modal.tsx
@apps/frontend/src/lib/schemas/transaction.ts
@apps/frontend/src/lib/types/transaction.ts
@apps/frontend/src/hooks/use-transactions.ts
@apps/frontend/src/hooks/use-merchant-categories.ts
@apps/frontend/src/hooks/use-accounts.ts
@apps/frontend/src/hooks/use-taxonomies.ts

<interfaces>
<!-- Schemas + hooks from 04-05 -->
From apps/frontend/src/lib/schemas/transaction.ts:
- NewIncomeOrExpenseTransactionSchema
- NewTransferTransactionSchema
- TransactionUpdateSchema
- NewSplitSchema
- CsvFieldMappingSchema (used by 04-08)

From apps/frontend/src/hooks/use-transactions.ts:

- useCreateTransaction(), useUpdateTransaction(), useDeleteTransaction()
- useCreateTransfer(), useUpdateTransferLeg(), useBreakTransferPair()

From apps/frontend/src/hooks/use-merchant-categories.ts:

- useLookupPayeeCategory(accountId, payee, enabled)

From apps/frontend/src/hooks/use-taxonomies.ts:

- useTaxonomyCategories(taxonomyId) → useQuery<TaxonomyCategory[]>
- useCreateTaxonomyCategory() → mutation { taxonomyId, name, color, parentId? }
  → TaxonomyCategory (verify this mutation exists; if missing, add it in this
  plan since UI-SPEC requires inline-create) </interfaces> </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Category Autocomplete + Split editor (with split-editor tests)</name>
  <files>
    apps/frontend/src/pages/transactions/category-autocomplete.tsx
    apps/frontend/src/pages/transactions/split-editor.tsx
    apps/frontend/src/pages/transactions/__tests__/split-editor.test.tsx
  </files>
  <read_first>
    - .planning/phases/04-transaction-core/04-UI-SPEC.md §Category Model lines 248-258 (Autocomplete + inline `+ Create category` footer)
    - .planning/phases/04-transaction-core/04-UI-SPEC.md §7 lines 704-770 (split editor full spec — initial 2 rows, trash disabled at 2, live remainder, submit gating)
    - apps/frontend/src/hooks/use-taxonomies.ts (existing `useTaxonomyCategories` + verify `useCreateTaxonomyCategory` — if missing, examine adapter for createCategory shape)
    - apps/frontend/src/lib/schemas/transaction.ts (NewSplitSchema — for per-row validation; superRefine on the parent schema enforces sum)
    - .planning/phases/04-transaction-core/04-CONTEXT.md §decisions D-15 (pre-fill timing — happens on payee blur)
  </read_first>
  <behavior>
    - **CategoryAutocomplete**:
      - Props: `value: string | null` (categoryId), `onChange: (id) => void`, `directionFilter?: "INCOME" | "EXPENSE"` (filter list per UI-SPEC §3 step 6 — "filtered to categories matching the current Direction by default; Show all categories collapse-toggle to reveal off-direction categories").
      - Reads `useTaxonomyCategories("sys_taxonomy_transaction_categories")`. Filter list by direction default mapping (income → cat_income only matches income default; expense → all 9 expense defaults). Provide `Show all categories` toggle.
      - Inline `+ Create category "{input}"` footer when typed text matches no category name (case-insensitive). Clicking it opens an inline Popover: name input (pre-filled), color picker (defaults to hashed-from-name pick from taxonomy palette), `[Create category]` button. On create, calls `useCreateTaxonomyCategory()` with `taxonomyId = "sys_taxonomy_transaction_categories"`, then auto-selects the new id.
      - If `useCreateTaxonomyCategory` mutation does not exist in `use-taxonomies.ts`, add it in this task. Also add `createTaxonomyCategory` to `apps/frontend/src/adapters/shared/taxonomies.ts` if missing — wire to existing server endpoint. (Per UI-SPEC: "Planner verifies that the existing taxonomy adapter exposes a `createCategory` mutation reachable from the transaction form — if not, planner adds it.")
    - **SplitEditor**:
      - Props: `splits: NewSplit[]`, `onChange: (splits: NewSplit[]) => void`, `transactionAmount: number`, `currency: string`, `directionFilter: "INCOME" | "EXPENSE"`, `onExitSplitMode: () => void`.
      - Initial state: when entering split mode upstream, parent passes `splits=[ {category, amount: txnAmount, sortOrder: 0}, {category: "", amount: 0, sortOrder: 1} ]` (two-row default).
      - Each row: bordered card, CategoryAutocomplete + MoneyInput + Trash icon button. Trash disabled when `splits.length === 2`.
      - `[+ Add another split]` ghost button beneath last row appends `{ category: "", amount: 0, sortOrder: splits.length }`.
      - Live remainder indicator below the rows:
        - sum = transactionAmount → `Total split: ${sum} ✓ matches transaction amount` in `text-success` with Check icon.
        - sum < transactionAmount → `Total split: ${sum}  Remaining ${diff} ⚠` in `text-warning`.
        - sum > transactionAmount → `Total split: ${sum}  Over by ${diff} ⚠` in `text-destructive`.
        - Tolerance: ±0.005 (any difference >$0.005 is "not matching" — penny errors block submit per UI-SPEC).
      - `[← Back to single category]` ghost button. If any split row has been edited from its default, show inline confirm `Discard splits and use a single category?` with `[Yes, discard]` / `[Keep splits]`. On accept: emit `onExitSplitMode()` with row[0] category as the active value.
    - **Tests for SplitEditor** (≥7):
      - `initial render shows 2 rows`
      - `trash icon disabled when 2 rows remain`
      - `[+ Add another split] adds a row`
      - `total === amount → green text-success indicator`
      - `total < amount → text-warning with Remaining`
      - `total > amount → text-destructive with Over by`
      - `[← Back to single category] confirms discard when any row was edited`
  </behavior>
  <action>(see `<behavior>` for full component specs)</action>
  <verify>
    <automated>test -f apps/frontend/src/pages/transactions/category-autocomplete.tsx && test -f apps/frontend/src/pages/transactions/split-editor.tsx && pnpm test -- --run transactions/__tests__/split-editor 2>&1 | tail -10 | grep -qE "PASS|passed" && pnpm type-check 2>&1 | tail -5 | grep -qE "Done|0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `category-autocomplete.tsx` exists; calls `useTaxonomyCategories("sys_taxonomy_transaction_categories")`.
    - `category-autocomplete.tsx` has inline `+ Create category` flow (grep for `Create category`).
    - `split-editor.tsx` defines minimum-2-row rule, live remainder indicator with all 3 states.
    - Split editor test runs ≥7 cases, all pass.
    - `useCreateTaxonomyCategory` mutation exists in `use-taxonomies.ts` (added if missing).
  </acceptance_criteria>
  <done>Category autocomplete + split editor + tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TransactionForm + transfer-edit AlertDialog + transfer-form-fields (with form tests)</name>
  <files>
    apps/frontend/src/pages/transactions/transaction-form.tsx
    apps/frontend/src/pages/transactions/transfer-form-fields.tsx
    apps/frontend/src/pages/transactions/transfer-edit-alert-dialog.tsx
    apps/frontend/src/pages/transactions/__tests__/transaction-form.test.tsx
  </files>
  <read_first>
    - apps/frontend/src/pages/settings/accounts/components/update-balance-modal.tsx (MoneyInput + Zod + form pattern role-match)
    - .planning/phases/04-transaction-core/04-UI-SPEC.md §3 lines 430-516 (form spec — sections 1-9 in render order, validation copy)
    - .planning/phases/04-transaction-core/04-UI-SPEC.md §Copywriting Contract lines 773-895 (verbatim labels)
    - .planning/phases/04-transaction-core/04-CONTEXT.md §decisions D-04 (transfer-pair amount-edit AlertDialog) and §specifics line 432-435 (friendly-companion copy)
    - apps/frontend/src/lib/schemas/transaction.ts (schemas)
    - apps/frontend/src/hooks/use-transactions.ts (mutations)
    - apps/frontend/src/hooks/use-merchant-categories.ts (lookup hook)
  </read_first>
  <behavior>
    - **TransactionForm props**: `mode: "create" | "edit"`, `initial?: Transaction | { accountId?: string }`, `open: boolean`, `onClose: () => void`, `onSubmitSuccess?: (txn: Transaction) => void`.
    - **Direction toggle** is the first field. Default = `EXPENSE`. Switching direction resets the form to the appropriate schema variant (Income/Expense vs Transfer). When switching FROM Transfer TO Income/Expense or vice versa, clear category/payee fields (different sets).
    - **Income/Expense layout** (sections 1-9 from UI-SPEC):
      1. Direction ToggleGroup
      2. Amount (`MoneyInput`, autofocus on open, helper "Enter as a positive number — direction handles the sign.")
      3. Account (`Select` over non-archived accounts; pre-filled from `initial.accountId`)
      4. Date (`DatePickerInput`, default today, max = today + 1 day)
      5. Payee (`Input`, required, placeholder per direction; on blur calls `useLookupPayeeCategory(accountId, payee)` — if a memory hit returns AND no category is currently picked, pre-fill the Category Autocomplete with the memory's `categoryId` and set `categorySource = "MEMORY"` for the submit payload)
      6. Category (`CategoryAutocomplete` from Task 1, directionFilter passed through)
      7. Optional Split mode: `[+ Split transaction]` ghost button. Clicking sets `hasSplits=true` and renders `<SplitEditor />` in place of the Category field. `categoryId` becomes null when in split mode.
      8. Notes (`Textarea`, optional, 200-char soft limit)
      9. Collapsible "More details" — FX rate override (visible only when `currency !== account.currency`), Reference / external ID input.
      Footer: `[Discard]` (closes form) on left, `[Save transaction]` (create) / `[Save changes]` (edit) on right. Right CTA disabled when form is invalid OR (in edit mode) when no field has changed.
    - **Transfer layout** — separate `<TransferFormFields />` component:
      1. Direction ToggleGroup (Transfer)
      2. Source account (`Select`, required)
      3. Destination account (`Select`, required, must differ — Zod superRefine error shows inline)
      4. Amount (`MoneyInput`, in source-account currency)
      5. Date
      6. Notes (optional)
      Footer same as I/E.
    - **Edit-mode behavior for transfer legs**: when `initial.transferGroupId` is non-null AND user changes the `amount` field:
      - Before submit, pop the AlertDialog: title "How would you like to apply this change?", body "This transaction is part of a transfer pair. Editing the amount can apply to both legs (preserves the link) or just this one (breaks the pair).", buttons `[Apply to both legs]` (default — preserves pairing, submits with editMode=APPLY_BOTH) and `[Only this leg]` (breaks link, submits with editMode=THIS_LEG_ONLY).
      - The AlertDialog is its own component `<TransferEditAmountAlertDialog open onConfirm(editMode) onCancel />`.
      - For non-amount edits (notes/date) on a transfer leg, automatically use editMode=APPLY_BOTH (date+notes always sync per D-04).
    - **Submission** —
      - Income/Expense create → `useCreateTransaction()`.
      - Income/Expense update → `useUpdateTransaction()` with editMode=undefined.
      - Transfer create → `useCreateTransfer({ src, dst })`.
      - Transfer leg update → `useUpdateTransferLeg(transaction, editMode)`.
    - **Form tests** (≥10):
      1. `direction toggle defaults to EXPENSE`
      2. `amount<=0 shows "Amount must be greater than 0."`
      3. `expense without payee shows "Payee is required."`
      4. `expense without category shows "Category is required."`
      5. `transfer with same source and destination shows "Source and destination accounts must be different."`
      6. `date >1d in future shows "Date can't be more than a day in the future."`
      7. `payee blur triggers useLookupPayeeCategory and pre-fills category when memory hit`
      8. `editing amount on a paired-transfer leg opens the AlertDialog`
      9. `confirming "Only this leg" calls useUpdateTransferLeg with editMode=THIS_LEG_ONLY`
      10. `confirming "Apply to both legs" calls useUpdateTransferLeg with editMode=APPLY_BOTH`
      11. `submit disabled in edit mode until a field changes`
      12. `successful submit calls onSubmitSuccess and closes`
  </behavior>
  <action>
    1. **`apps/frontend/src/pages/transactions/transfer-edit-alert-dialog.tsx`** — UI-SPEC-compliant AlertDialog using shadcn/ui AlertDialog primitives (`AlertDialogContent`, `AlertDialogTitle`, `AlertDialogDescription`, `AlertDialogFooter`, `AlertDialogAction`, `AlertDialogCancel`). Title: "How would you like to apply this change?". Description: "This transaction is part of a transfer pair. Editing the amount can apply to both legs (preserves the link) or just this one (breaks the pair)." Footer buttons (left to right): `[Cancel]`, `[Only this leg]` (variant outline), `[Apply to both legs]` (variant default = accent). Each button calls the `onConfirm(editMode)` callback with the matching `TransferEditMode` value.

    2. **`apps/frontend/src/pages/transactions/transfer-form-fields.tsx`** — RHF-driven sub-component for the Transfer branch (source select, destination select, amount, date, notes — note transfers do NOT show category per UI-SPEC §3 step 6 "hidden for Transfer"). Uses `NewTransferTransactionSchema` with `useForm` + `zodResolver`. Receives the form instance from parent via `useFormContext` so the parent can `handleSubmit` the values.

    3. **`apps/frontend/src/pages/transactions/transaction-form.tsx`** — main form. Uses `Sheet` desktop / `Dialog` mobile (mirror Phase-3 pattern in `update-balance-modal.tsx`). Switches schema based on direction state.

       Local state required:
       - `direction: TransactionDirection` — default "EXPENSE"; controlled by ToggleGroup.
       - `showAmountEditDialog: boolean`, `pendingSubmit: TransactionUpdate | null` — for the D-04 AlertDialog flow.

       Two RHF instances (one per branch) using `zodResolver(NewIncomeOrExpenseTransactionSchema)` and `zodResolver(NewTransferTransactionSchema)`. The parent picks which to render based on `direction`.

       Mutation hooks: `useCreateTransaction()`, `useUpdateTransaction()`, `useCreateTransfer()`, `useUpdateTransferLeg()`.

       `onSubmitIE(values)`:
       - mode==="create" → `create.mutateAsync({...values, source: "MANUAL", direction})` then `onSubmitSuccess?.(txn); onClose()`.
       - mode==="edit" → `update.mutateAsync({transaction: {id: initial!.id, ...values}, editMode: undefined})` then close.

       `onSubmitTransfer(values)`:
       - mode==="create" → `createTransfer.mutateAsync({src: {accountId: values.sourceAccountId, amount, currency, transactionDate, notes}, dst: {accountId: values.destinationAccountId, amount, currency, transactionDate, notes}})`.
       - mode==="edit" with `initial?.transferGroupId` AND `values.amount !== initial.amount` → set `pendingSubmit` and open `<TransferEditAmountAlertDialog />`.
       - mode==="edit" non-amount change → `updateTransferLeg.mutateAsync({transaction: {id: initial.id, ...values}, editMode: "APPLY_BOTH"})`.

       `handleAmountEditConfirm(editMode)` → `updateTransferLeg.mutateAsync({transaction: pendingSubmit, editMode})` → close.

       Wire payee blur for D-15 auto-fill — DO NOT use `useEffect`. Instead derive from query result inside an event handler:
       ```tsx
       const payee = useWatch({ name: "payee" });
       const accountId = useWatch({ name: "accountId" });
       const lookup = useLookupPayeeCategory(accountId, payee, payee.length > 0);
       const onPayeeBlur = () => {
           if (lookup.data?.categoryId && !ieForm.getValues("categoryId")) {
               ieForm.setValue("categoryId", lookup.data.categoryId);
           }
       };
       ```

       Render shape:
       - `<Sheet open={open} onOpenChange={(v) => !v && onClose()}>` containing `<SheetContent>` with `<SheetHeader>` (title `"New transaction"` / `"Edit transaction"` + subtitle `_payee_ · _date_` in edit mode).
       - DirectionToggle component (ToggleGroup with three options).
       - Conditional render: `direction === "TRANSFER"` → `<TransferFormFields />`; else `<IncomeExpenseFormFields direction={direction} />`.
       - `<TransferEditAmountAlertDialog open={showAmountEditDialog} onConfirm={handleAmountEditConfirm} onCancel={...} />` at the root.

       Implement `IncomeExpenseFormFields` inline in the same file or as a sibling component. Render order: Amount → Account → Date → Payee → Category Autocomplete (or SplitEditor when `hasSplits=true`) → Notes → "More details" Collapsible. Footer: Discard + Save with disabled states per UI-SPEC §3 footer rules.

    4. **`apps/frontend/src/pages/transactions/__tests__/transaction-form.test.tsx`** — vitest + RTL + RHF testing utilities. Mock `useCreateTransaction`, `useUpdateTransaction`, `useCreateTransfer`, `useUpdateTransferLeg`, `useLookupPayeeCategory`, `useAccounts`, `useTaxonomyCategories`. Cover the 12 tests listed in `<behavior>`.

       For the AlertDialog tests, render the form in edit mode with a transferGroupId-bearing initial value, then change the amount field and submit; assert AlertDialog opens. Click `[Only this leg]` and assert `useUpdateTransferLeg` was called with `editMode: "THIS_LEG_ONLY"`.

  </action>
  <verify>
    <automated>test -f apps/frontend/src/pages/transactions/transaction-form.tsx && test -f apps/frontend/src/pages/transactions/transfer-edit-alert-dialog.tsx && grep -q "THIS_LEG_ONLY\\|APPLY_BOTH" apps/frontend/src/pages/transactions/transfer-edit-alert-dialog.tsx && grep -q "useLookupPayeeCategory" apps/frontend/src/pages/transactions/transaction-form.tsx && pnpm test -- --run transactions/__tests__/transaction-form 2>&1 | tail -10 | grep -qE "PASS|passed" && pnpm type-check 2>&1 | tail -5 | grep -qE "Done|0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `transaction-form.tsx` exists, declares `TransactionForm` component, calls `useCreateTransaction`/`useUpdateTransaction`/`useCreateTransfer`/`useUpdateTransferLeg`.
    - References `useLookupPayeeCategory` (D-15 wiring).
    - `transfer-edit-alert-dialog.tsx` references `THIS_LEG_ONLY` and `APPLY_BOTH`.
    - `transfer-form-fields.tsx` exists.
    - Form test runs ≥10 cases, all pass.
    - No `useEffect` introduced for derivable state (lint passes — react-hooks rule + project convention).
  </acceptance_criteria>
  <done>Form + AlertDialog + tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire form into TransactionsPage and TransactionDetailSheet</name>
  <files>
    apps/frontend/src/pages/transactions/transactions-page.tsx
    apps/frontend/src/pages/transactions/transaction-detail-sheet.tsx
  </files>
  <read_first>
    - apps/frontend/src/pages/transactions/transactions-page.tsx (created in plan 04-06; locate the `[+ New transaction]` button and the `formOpen` state placeholder)
    - apps/frontend/src/pages/transactions/transaction-detail-sheet.tsx (created in plan 04-06; locate the `[Edit transaction]` button)
    - apps/frontend/src/pages/transactions/transaction-form.tsx (Task 2)
  </read_first>
  <action>
    1. **`apps/frontend/src/pages/transactions/transactions-page.tsx`** — replace the placeholder form section with the real component:
       ```tsx
       <TransactionForm
           mode="create"
           open={formOpen}
           onClose={() => setFormOpen(false)}
           initial={{ accountId: filters.accountIds?.[0] }}
       />
       <TransactionForm
           mode="edit"
           open={!!editingTxn}
           initial={editingTxn ?? undefined}
           onClose={() => setEditingTxn(null)}
       />
       ```
       Add the `editingTxn: Transaction | null` state. Wire the `onEdit` callback from TransactionDetailSheet to set `editingTxn` (and close the detail sheet first).

    2. **`apps/frontend/src/pages/transactions/transaction-detail-sheet.tsx`** — emit `onEdit(transaction)` on the `[Edit transaction]` button click. Already declared in the props interface from plan 04-06; this task just confirms the click wires through.

    3. Add an additional pass: in `transactions-page.tsx`, also handle `useDetectTransactionDuplicates()` mutation result to feed `pendingDupes` count into the DuplicateBanner. The simplest source is to look at the latest import_run_id stored in localStorage or query cache; defer the live count to plan 04-09 — but ensure THIS plan exposes a callback `onPendingDupesChange` that 04-09 can hook into without re-editing this file.

       Concrete implementation: add a `usePendingDuplicateCount()` placeholder hook (returns 0 in this plan) imported from `@/hooks/use-pending-duplicates`. Plan 04-09 implements that hook and rebuilds the count from query cache. This keeps the boundary clean.

       Create a stub file `apps/frontend/src/hooks/use-pending-duplicates.ts` with two exported hooks: `usePendingDuplicateCount()` returning the literal `0`, and `useReviewDuplicates()` returning `{ open: false, setOpen: () => {} }`. Plan 04-09 expands these.

  </action>
  <verify>
    <automated>grep -q "<TransactionForm" apps/frontend/src/pages/transactions/transactions-page.tsx && grep -q "onEdit" apps/frontend/src/pages/transactions/transaction-detail-sheet.tsx && test -f apps/frontend/src/hooks/use-pending-duplicates.ts && pnpm type-check 2>&1 | tail -5 | grep -qE "Done|0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `transactions-page.tsx` mounts `<TransactionForm />` for both create and edit modes.
    - `transaction-detail-sheet.tsx` calls `onEdit` from the [Edit transaction] button.
    - Stub hook `use-pending-duplicates.ts` exists for plan 04-09 to expand.
    - `pnpm type-check` green.
  </acceptance_criteria>
  <done>Form + detail sheet wired; type-check green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                           |
| --------------------- | --------------------------------------------------------------------- |
| Form input → mutation | Untrusted at the wire; Zod validates client-side; server re-validates |

## STRIDE Threat Register

| Threat ID | Category  | Component                              | Disposition | Mitigation Plan                                              |
| --------- | --------- | -------------------------------------- | ----------- | ------------------------------------------------------------ |
| T-04-27   | Tampering | XSS via payee field                    | mitigate    | React default escaping (text content); never inject raw HTML |
| T-04-28   | Tampering | Schema bypass via direct mutation call | inherited   | Server `NewTransaction::validate()` enforces (T-04-05)       |

</threat_model>

<verification>
- `pnpm test -- --run transactions/__tests__` reports OK for split-editor and transaction-form tests.
- `pnpm type-check` green.
- `pnpm lint` clean for new files (no useEffect for derivable state).
</verification>

<success_criteria>

1. `TransactionForm` swaps schema by direction, validates inline per UI-SPEC,
   submits to the right mutation.
2. Editing a transfer-leg amount triggers AlertDialog with both action paths
   working.
3. SplitEditor enforces sum gating and minimum-2 rule.
4. Category Autocomplete supports inline-create + payee-memory pre-fill on blur.
5. All component tests pass. </success_criteria>

<output>
After completion, create `.planning/phases/04-transaction-core/04-07-SUMMARY.md` with the form's prop signature so 04-09's review surfaces can re-open the form pre-filled, and the stub hook signatures left for plan 04-09 to expand.
</output>
