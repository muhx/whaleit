---
phase: 04-transaction-core
plan: 10
type: execute
wave: 7
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05, 04-06, 04-07, 04-08, 04-09]
files_modified:
  - e2e/12-transactions-manual.spec.ts
  - e2e/13-transactions-import-csv.spec.ts
  - e2e/14-transactions-import-ofx.spec.ts
  - e2e/15-transactions-duplicate-review.spec.ts
  - e2e/16-transactions-transfer-pair.spec.ts
  - e2e/17-transactions-splits.spec.ts
  - e2e/18-transactions-reconciliation.spec.ts
  - e2e/fixtures/transactions/chase_100.csv
  - e2e/fixtures/transactions/chase.ofx
  - e2e/fixtures/transactions/chase_with_duplicate.csv
  - e2e/helpers.ts
  - .planning/phases/04-transaction-core/04-VALIDATION.md
autonomous: false
requirements:
  [TXN-01, TXN-02, TXN-03, TXN-04, TXN-05, TXN-06, TXN-07, TXN-08, TXN-09]
must_haves:
  truths:
    - "All 7 Phase-4 E2E specs follow the `run-e2e-tests` skill — env setup is
      mandatory; tests run via `pnpm playwright test --project=chromium
      e2e/12-..7-...spec.ts` after `pnpm e2e:setup` (or whatever the skill's
      exact incantation is — read e2e/README.md first)"
    - "Manual-add spec (TXN-01) covers: open form via [+ New transaction], fill
      amount/account/date/payee/category, submit, see new row in ledger with
      correct icon + color"
    - "CSV-import spec (TXN-04) covers: upload chase_100.csv → map columns →
      review (no duplicates) → confirm → 100 rows visible at /transactions;
      re-importing same file inserts 0 new rows (idempotency)"
    - "OFX-import spec (TXN-05) covers: upload chase.ofx → skip Mapping (D-19) →
      review → confirm → rows visible with FITID populated"
    - "Duplicate-review spec (TXN-06) covers: import chase_with_duplicate.csv
      (one row matches an existing entry); Review step shows the duplicate row
      tinted; click [Discard new] on review surface; the row is excluded from
      import"
    - "Transfer-pair spec (D-01, D-04) covers: create transfer between two
      accounts (paired rows visible on both account ledgers); edit amount on one
      leg → AlertDialog opens → click [Only this leg] → sibling no longer linked"
    - "Splits spec (TXN-08) covers: create transaction with 2 splits totaling
      exactly the amount; submit succeeds; sum mismatch blocks submit"
    - "Reconciliation spec (Phase-3 D-14) covers: create new account with
      opening_balance=$100; add first transaction $30 expense; verify Opening
      Balance system row + user row both visible; running balance reflects $70"
    - "Running-balance spec (TXN-09) within manual + csv-import specs: insert
      rows out of date order; running balance values match expected sequence"
    - "All specs follow existing helpers in e2e/helpers.ts (e.g.,
      loginAsTestUser, createAccount); add new helpers as needed and export from
      helpers.ts"
    - "After all 7 specs are green, 04-VALIDATION.md `nyquist_compliant: true`
      is set in frontmatter"
  artifacts:
    - path: "e2e/12-transactions-manual.spec.ts"
      provides:
        "Manual add/edit/delete + running balance smoke (TXN-01, TXN-09)"
      contains: "test('user can add a manual transaction'"
    - path: "e2e/13-transactions-import-csv.spec.ts"
      provides: "CSV import + idempotency (TXN-04)"
      contains: "chase_100.csv"
    - path: "e2e/14-transactions-import-ofx.spec.ts"
      provides: "OFX import (TXN-05)"
      contains: "chase.ofx"
    - path: "e2e/15-transactions-duplicate-review.spec.ts"
      provides: "Duplicate review wizard step + post-import banner (TXN-06)"
      contains: "Discard new"
    - path: "e2e/16-transactions-transfer-pair.spec.ts"
      provides: "Transfer create + amount-edit AlertDialog (D-01, D-04)"
      contains: "Apply to both legs"
    - path: "e2e/17-transactions-splits.spec.ts"
      provides: "Split editor sum gating (TXN-08)"
      contains: "matches transaction amount"
    - path: "e2e/18-transactions-reconciliation.spec.ts"
      provides: "First-insert reconciliation hook (Phase-3 D-14)"
      contains: "Opening Balance"
  key_links:
    - from: "e2e/*-transactions-*.spec.ts"
      to: ".claude/skills/run-e2e-tests/SKILL.md + e2e/README.md"
      via: "skill-mandated env setup + pnpm playwright test"
      pattern: "pnpm playwright"
    - from: "e2e/13-transactions-import-csv.spec.ts"
      to: "e2e/fixtures/transactions/chase_100.csv"
      via: "page.setInputFiles(... chase_100.csv)"
      pattern: "chase_100.csv"
---

<objective>
Land the Phase-4 Playwright E2E suite. Seven specs cover every TXN-NN behavior end-to-end. Execute the suite, get it green, and sign off `04-VALIDATION.md`.

Purpose: This is the phase gate. `/gsd-verify-work` requires all unit +
integration + E2E green. Without this plan, Phase 4 cannot ship. Output: 7 spec
files + 3 fixture files + helper additions + `04-VALIDATION.md` flipped to
`nyquist_compliant: true`. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

@.claude/skills/run-e2e-tests/SKILL.md @e2e/README.md </execution_context>

<context>
@.planning/phases/04-transaction-core/04-CONTEXT.md
@.planning/phases/04-transaction-core/04-RESEARCH.md
@.planning/phases/04-transaction-core/04-PATTERNS.md
@.planning/phases/04-transaction-core/04-UI-SPEC.md
@.planning/phases/04-transaction-core/04-VALIDATION.md
@e2e/helpers.ts
@e2e/04-csv-import.spec.ts
@e2e/11-accounts.spec.ts
@e2e/00-auth-flow.spec.ts
@e2e/01-happy-path.spec.ts

<interfaces>
<!-- Existing helpers in e2e/helpers.ts that THIS plan will reuse + extend -->
From e2e/helpers.ts:
- loginAsTestUser(page) — auth setup before each test
- createTestAccount(page, { name, type, currency, openingBalance }) — Phase 3 helper
- (other Phase 1-3 helpers — read the file)

<!-- Add new helpers in this plan -->

- createTestTransaction(page, { accountName, direction, amount, payee, category,
  date }) — drives the form
- importTransactionCsvFixture(page, { fixtureName, accountName, mapping }) —
  drives the wizard
- importTransactionOfxFixture(page, { fixtureName, accountName }) — drives the
  OFX wizard
- expectRunningBalance(page, accountName, expected) — reads ledger row balance
  text and asserts </interfaces> </context>

<tasks>

<task type="auto">
  <name>Task 1: Fixtures + helpers</name>
  <files>
    e2e/fixtures/transactions/chase_100.csv
    e2e/fixtures/transactions/chase.ofx
    e2e/fixtures/transactions/chase_with_duplicate.csv
    e2e/helpers.ts
  </files>
  <read_first>
    - e2e/helpers.ts (current helper module — see existing function shapes; mirror style)
    - e2e/04-csv-import.spec.ts (12K — existing CSV-import spec for activities; gives the page-driver pattern for the wizard)
    - e2e/11-accounts.spec.ts (account creation pattern — Phase 3 reference)
    - .planning/phases/04-transaction-core/04-VALIDATION.md §Wave 0 Requirements (fixture file list)
    - crates/core/tests/fixtures/chase_100.csv (created in plan 04-02; copy or reference identical anonymized synthetic data)
    - crates/core/tests/fixtures/chase.ofx (created in plan 04-02)
  </read_first>
  <action>
    1. **`e2e/fixtures/transactions/chase_100.csv`** — 100 anonymized synthetic rows. Either copy directly from `crates/core/tests/fixtures/chase_100.csv` (preferred — identical fixture) OR generate a fresh one with the same shape. Columns: `Posted Date,Description,Amount,Type,Category` (Chase-style). Mix of positive (income) and negative (expense) amounts. Dates span ~30 days. Include 1 EU-formatted date as a stretch row (`14.04.2026` if MM/DD/YYYY is the default). Keep file <30KB.

    2. **`e2e/fixtures/transactions/chase.ofx`** — minimal OFX 1.x SGML with 5-10 STMTTRN blocks. All FITIDs populated. Header `OFXHEADER:100 ENCODING:USASCII...`. Either copy from `crates/core/tests/fixtures/chase.ofx` or hand-craft a minimal sample.

    3. **`e2e/fixtures/transactions/chase_with_duplicate.csv`** — 6 rows: 5 unique + 1 row that matches a transaction the test will pre-create (same account_id + amount + date). The test seeds the matching transaction first, then imports this CSV; the duplicate-review surface should flag the 1 matching row.

       Concrete fixture content: rows 1-5 are arbitrary unique. Row 6 matches whatever the spec seeds (use a deterministic seed: e.g., `Whole Foods Market,42.10,2026-04-24`).

    4. **`e2e/helpers.ts`** — append (do not break existing helpers):
       ```typescript
       // Phase 4 helpers
       export interface CreateTransactionOptions {
           accountName: string;
           direction: "Income" | "Expense" | "Transfer";
           amount: string;        // pre-formatted, e.g. "42.10"
           payee?: string;
           category?: string;
           date?: string;         // YYYY-MM-DD; default today
           notes?: string;
           sourceAccount?: string;       // for Transfer
           destinationAccount?: string;  // for Transfer
       }

       export async function createTestTransaction(page: Page, opts: CreateTransactionOptions): Promise<void> {
           // Click [+ New transaction] FAB or header CTA
           // Set direction toggle
           // Fill the form per opts
           // Submit and wait for the toast
       }

       export interface ImportCsvFixtureOptions {
           fixturePath: string;   // resolved e2e/fixtures/transactions/...csv
           accountName: string;
           mapping: { dateColumn: string; amountColumn?: string; debitColumn?: string; creditColumn?: string; payeeColumn: string; categoryColumn?: string; notesColumn?: string };
           saveAsTemplate?: string;
       }

       export async function importTransactionCsvFixture(page: Page, opts: ImportCsvFixtureOptions): Promise<{ inserted: number; pendingDuplicates: number }> {
           // Navigate to /transactions/import?accountId=...
           // Drop file via setInputFiles
           // Set mapping
           // Click Continue → Review → Confirm
           // Read result toast/text and return counts
       }

       export interface ImportOfxFixtureOptions {
           fixturePath: string;
           accountName: string;
       }

       export async function importTransactionOfxFixture(page: Page, opts: ImportOfxFixtureOptions): Promise<{ inserted: number }> {
           // Navigate to /transactions/import
           // Pick account
           // Drop OFX file (no mapping step per D-19)
           // Continue to Review → Confirm
       }

       export async function getTransactionRow(page: Page, payee: string): Promise<Locator> {
           return page.getByTestId("transaction-row").filter({ hasText: payee });
       }

       export async function expectRunningBalance(page: Page, payee: string, expected: string): Promise<void> {
           const row = await getTransactionRow(page, payee);
           await expect(row).toContainText(`Bal ${expected}`);
       }
       ```
       Use existing `loginAsTestUser` and `createTestAccount` from the file unchanged.

  </action>
  <verify>
    <automated>test -f e2e/fixtures/transactions/chase_100.csv && test -f e2e/fixtures/transactions/chase.ofx && test -f e2e/fixtures/transactions/chase_with_duplicate.csv && grep -q "createTestTransaction\\|importTransactionCsvFixture\\|importTransactionOfxFixture" e2e/helpers.ts && wc -l e2e/fixtures/transactions/chase_100.csv | awk '$1 >= 100'</automated>
  </verify>
  <acceptance_criteria>
    - All 3 fixture files exist.
    - `chase_100.csv` contains ≥100 lines (header + 100 data rows).
    - `e2e/helpers.ts` exports `createTestTransaction`, `importTransactionCsvFixture`, `importTransactionOfxFixture`, `expectRunningBalance` (grep returns hits for each).
  </acceptance_criteria>
  <done>Fixtures + helpers committed.</done>
</task>

<task type="auto">
  <name>Task 2: Specs 12-14 — manual + CSV import + OFX import</name>
  <files>
    e2e/12-transactions-manual.spec.ts
    e2e/13-transactions-import-csv.spec.ts
    e2e/14-transactions-import-ofx.spec.ts
  </files>
  <read_first>
    - e2e/04-csv-import.spec.ts (existing activity-import spec — driver pattern reference)
    - e2e/01-happy-path.spec.ts (24.9K — full flow pattern)
    - e2e/helpers.ts (Task 1 helpers)
    - .planning/phases/04-transaction-core/04-UI-SPEC.md §Copywriting Contract lines 773-895 (assert exact button labels in tests)
    - .claude/skills/run-e2e-tests/SKILL.md + e2e/README.md (env setup — read FIRST per skill)
  </read_first>
  <action>
    1. **`e2e/12-transactions-manual.spec.ts`** — TXN-01, TXN-02, TXN-03, TXN-09 happy path. Tests (≥5):
       - `user can add a manual income transaction` — login, create account "Chase Checking" with opening_balance 1000, click [+ New transaction], pick Income, fill $500 + payee "Acme Corp Payroll" + category Income + today, submit, expect ledger row with `ArrowDownLeft` (use `getByTestId("transaction-row")` filter) + `+$500.00` + running-balance `Bal $1,500.00`.
       - `user can add a manual expense transaction` — same setup, Expense $42.10 + Whole Foods + Groceries, expect `ArrowUpRight` + `−$42.10` + running-balance reflects.
       - `payee memory pre-fills category on second entry` (TXN-02) — first add expense at "Coffee Shop" with category Dining; second add expense at "Coffee Shop"; assert Category Autocomplete pre-fills with Dining BEFORE user picks it manually.
       - `user can edit a transaction` — create txn, click row → detail sheet → Edit → change amount → Save changes → expect updated amount in row.
       - `user can delete a transaction` — create txn, click row → detail sheet → Delete → AlertDialog confirm → toast "Transaction deleted" + row removed.
       - `search by payee filters list` (TXN-03) — create 3 txns with different payees; type "Whole" in search; expect only matching row visible.
       - `running balance is correct after out-of-order insert` (TXN-09) — create txn dated yesterday $50 then dated 2 days ago $100; expect both rows show running balances reflecting the chronological order regardless of insert order.

    2. **`e2e/13-transactions-import-csv.spec.ts`** — TXN-04. Tests (≥3):
       - `user can import 100-row CSV and see all rows in ledger` — login, create account, navigate to /transactions/import → upload chase_100.csv → map columns → continue to review → confirm → toast `100 transactions imported` → /transactions shows ≥100 rows for that account.
       - `re-importing the same CSV produces 0 new rows (idempotency)` — repeat the import; toast says `0 transactions imported` (or `100 skipped as duplicates` — verify against actual server response copy).
       - `saved template appears in next import; selecting it pre-fills mapping` (D-16, D-18) — first import saves a template "My Chase format"; second import with the same file shows the template in the picker; selecting it auto-fills the mapping table.
       - `template header mismatch surfaces inline message` (D-17) — first import saves "Template A" with chase_100 headers; second import with a different CSV that has different column positions; selecting Template A shows the verbatim message: `Your saved 'Template A' template doesn't match this file's columns. Re-map?`.

    3. **`e2e/14-transactions-import-ofx.spec.ts`** — TXN-05. Tests (≥2):
       - `user can import OFX file (no mapping step)` (D-19) — login, create account, /transactions/import → upload chase.ofx → wizard skips Mapping → goes straight to Review → assert ≥3 rows preview → Confirm → toast / ledger.
       - `OFX FITID populates external_ref on imported rows` — after import, click a row to open detail sheet; the "Reference / external ID" field shows the FITID from the fixture.

    Each spec MUST start with the env setup from e2e/README.md (per skill). Use `test.describe.configure({ mode: "serial" })` if test order matters within a file.

  </action>
  <verify>
    <automated>test -f e2e/12-transactions-manual.spec.ts && test -f e2e/13-transactions-import-csv.spec.ts && test -f e2e/14-transactions-import-ofx.spec.ts && grep -q "user can add a manual" e2e/12-transactions-manual.spec.ts && grep -q "chase_100.csv" e2e/13-transactions-import-csv.spec.ts && grep -q "chase.ofx" e2e/14-transactions-import-ofx.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 3 spec files exist with the required test names.
    - Each spec imports from `./helpers` (uses Task 1 helpers).
    - Tests reference verbatim UI-SPEC copy (e.g., `New transaction`, `100 transactions imported`, `doesn't match this file's columns`).
  </acceptance_criteria>
  <done>Specs 12-14 written; ready for execution in Task 4.</done>
</task>

<task type="auto">
  <name>Task 3: Specs 15-18 — duplicate review + transfer pair + splits + reconciliation</name>
  <files>
    e2e/15-transactions-duplicate-review.spec.ts
    e2e/16-transactions-transfer-pair.spec.ts
    e2e/17-transactions-splits.spec.ts
    e2e/18-transactions-reconciliation.spec.ts
  </files>
  <read_first>
    - .planning/phases/04-transaction-core/04-UI-SPEC.md §6 lines 626-700 (duplicate review)
    - .planning/phases/04-transaction-core/04-CONTEXT.md §decisions D-01, D-04 (transfer rules)
    - .planning/phases/04-transaction-core/04-CONTEXT.md §decisions D-13, D-14 carry-forward (reconciliation)
    - .planning/phases/04-transaction-core/04-UI-SPEC.md §7 lines 704-770 (splits)
    - e2e/helpers.ts (Task 1)
  </read_first>
  <action>
    1. **`e2e/15-transactions-duplicate-review.spec.ts`** — TXN-06. Tests (≥4):
       - `wizard Review step flags inline duplicate row` — create account, manually add a transaction matching the duplicate row in `chase_with_duplicate.csv` (Whole Foods $42.10 on 2026-04-24); import the CSV → at the Review step, assert the matching row has the bucket-tinted background (`bg-destructive/10` for ALMOST_CERTAIN bucket) + visible `[Discard new]` and `[Keep both]` buttons.
       - `clicking [Discard new] in the wizard excludes that row from import` — continue from prior; click `[Discard new]` on the duplicate row; complete the import; assert ledger has 5 rows from the import (not 6) plus the originally-seeded duplicate.
       - `post-import banner appears on /transactions when duplicates remain` — import without resolving in the wizard (use `[Skip for now]`); after navigate to /transactions, assert the DuplicateBanner is visible with text containing "1 possible duplicate" + a `[Review duplicates →]` button.
       - `post-import sheet renders 'pair 1 of 1' and resolves on action` — click the banner; assert sheet title `Review duplicates · 1 of 1`; click `[Discard new]`; assert sheet shows `All caught up`; click `[Done]`; banner disappears.
       - `sheet does not contain 'Reject' or 'Accept' button labels` (regression guard) — open sheet on any duplicate; assert `page.getByRole('button', { name: /Reject|Accept/ })` returns 0 matches.

    2. **`e2e/16-transactions-transfer-pair.spec.ts`** — D-01, D-04. Tests (≥3):
       - `creating a transfer produces paired rows on both account ledgers` — login, create two accounts (A: Chase Checking, B: Chase Savings); click [+ New transaction] → Transfer → source A → destination B → amount $200 → submit; navigate to A's "Recent transactions" — see one outflow row with `ArrowLeftRight` icon; navigate to B's — see one inflow row with same icon; both rows share a transfer-pair indicator.
       - `editing a transfer leg amount opens AlertDialog with both action paths` — open the source-leg row → Edit → change amount $200 → $250 → click Save changes → AlertDialog appears with title `How would you like to apply this change?` + buttons `[Cancel]` / `[Only this leg]` / `[Apply to both legs]`.
       - `[Apply to both legs] keeps pair linked; [Only this leg] breaks the link` — split into two test cases:
         - Apply both: confirm Apply both; navigate to B's ledger; sibling amount also $250.
         - Only this leg: from a fresh transfer, click Only this leg; navigate to B's ledger; sibling still $200; the pair indicator on the edited row is gone.

    3. **`e2e/17-transactions-splits.spec.ts`** — TXN-08. Tests (≥3):
       - `user can create transaction with 2 splits totaling exactly the amount` — open form; set Expense $50; click `[+ Split transaction]`; fill row 1 Groceries $30, row 2 Household $20; assert remainder indicator shows `Total split: $50.00 ✓ matches transaction amount` in `text-success`; submit; assert ledger row shows `Split · 2 categories`.
       - `submit blocked when split sum != amount` — same setup; row 1 $30, row 2 $10 (total $40 vs $50); assert remainder shows `Remaining $10.00 ⚠`; assert `[Save transaction]` button is disabled.
       - `over-by indicator on excess sum` — row 1 $30, row 2 $25 (total $55); assert `Over by $5.00 ⚠` in `text-destructive`; submit disabled.
       - `[← Back to single category] confirms discard` — enter split mode, edit row 2's category; click `[← Back to single category]`; expect inline confirm `Discard splits and use a single category?`; click Yes, discard; form returns to single-Category mode.

    4. **`e2e/18-transactions-reconciliation.spec.ts`** — Phase-3 D-14 carry-forward. Tests (≥3):
       - `first manual transaction synthesizes Opening Balance row` — login, create account "Chase Checking" with `opening_balance=$100, current_balance=null`; add first transaction $30 expense "Coffee"; navigate to per-account ledger; expect 2 rows: Opening Balance $100 (system-generated, today, no payee — appears as date=account.created_at) AND the user $30 row; running balance after both = $70.
       - `account with current_balance != opening_balance synthesizes adjustment row` — create account opening=$100, current=$120, balance_updated_at=2 days ago; first user transaction; expect 3 rows: Opening Balance $100, Balance adjustment +$20, user txn.
       - `legacy account with no opening_balance skips reconciliation` — create account with NO opening_balance; first user transaction; ledger has only 1 row.

  </action>
  <verify>
    <automated>test -f e2e/15-transactions-duplicate-review.spec.ts && test -f e2e/16-transactions-transfer-pair.spec.ts && test -f e2e/17-transactions-splits.spec.ts && test -f e2e/18-transactions-reconciliation.spec.ts && grep -q "Discard new" e2e/15-transactions-duplicate-review.spec.ts && grep -q "Apply to both legs" e2e/16-transactions-transfer-pair.spec.ts && grep -q "matches transaction amount\\|Remaining\\|Over by" e2e/17-transactions-splits.spec.ts && grep -q "Opening Balance" e2e/18-transactions-reconciliation.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 spec files exist with the required test names.
    - Each references the verbatim UI-SPEC copy from CONTEXT.md / UI-SPEC.md.
    - Specs 15 includes the regression guard against "Reject" / "Accept".
  </acceptance_criteria>
  <done>Specs 15-18 written.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: [BLOCKING] Run full E2E suite + sign off VALIDATION.md</name>
  <files>.planning/phases/04-transaction-core/04-VALIDATION.md</files>
  <what-built>All 7 spec files committed. Time to run them per the `run-e2e-tests` skill (env setup is mandatory).</what-built>
  <how-to-verify>
    User runs locally (E2E skill mandates manual env setup):

    1. **Read e2e/README.md and follow it exactly and in order** — per the skill rules. This typically includes:
       - `pnpm e2e:setup` (or whatever the README says — bring up Postgres + apply migrations + seed test user).
       - Build the frontend if needed.
       - Set required env vars (DATABASE_URL, JWT_SECRET, etc.).

    2. Run each Phase-4 spec individually first (faster feedback loop):
       ```
       pnpm playwright test --project=chromium e2e/12-transactions-manual.spec.ts
       pnpm playwright test --project=chromium e2e/13-transactions-import-csv.spec.ts
       pnpm playwright test --project=chromium e2e/14-transactions-import-ofx.spec.ts
       pnpm playwright test --project=chromium e2e/15-transactions-duplicate-review.spec.ts
       pnpm playwright test --project=chromium e2e/16-transactions-transfer-pair.spec.ts
       pnpm playwright test --project=chromium e2e/17-transactions-splits.spec.ts
       pnpm playwright test --project=chromium e2e/18-transactions-reconciliation.spec.ts
       ```

    3. Run the full suite to catch cross-spec regressions:
       ```
       pnpm playwright test --project=chromium
       ```
       Expected: ALL specs green (Phase 1-3 specs from prior phases continue to pass).

    4. Update `.planning/phases/04-transaction-core/04-VALIDATION.md`:
       a. In the frontmatter, set `status: approved` and `nyquist_compliant: true` and `wave_0_complete: true`.
       b. Fill the Per-Task Verification Map with ROW per TXN-NN test command (one row per test from RESEARCH §Phase Requirements → Test Map). Status column: ✅ green for the ones that just passed.
       c. At the bottom, set `**Approval:** approved` with the date.

    5. If ANY spec fails:
       - Capture the failure output + screenshot from `playwright-report/` or `test-results/`.
       - Diagnose: env issue? test bug? regression in implementation?
       - If env: fix env per README and retry.
       - If test bug: fix the spec.
       - If implementation regression: open a gap-closure pass. Do NOT mark green.

    6. Once green, commit the updated VALIDATION.md.

  </how-to-verify>
  <resume-signal>Type "approved" once all 7 Phase-4 specs are green AND VALIDATION.md is signed off (`nyquist_compliant: true` + status=approved + per-task map filled). If any fail, paste the failure output and we'll diagnose.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary      | Description                                                                                     |
| ------------- | ----------------------------------------------------------------------------------------------- |
| Test fixtures | Anonymized synthetic data only — no real PII or real bank account numbers in any fixture        |
| Test user     | Tests run against the seeded test user from e2e/README.md; does NOT authenticate as a real user |

## STRIDE Threat Register

| Threat ID | Category               | Component                  | Disposition | Mitigation Plan                                                                                                    |
| --------- | ---------------------- | -------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------ |
| T-04-35   | Information Disclosure | Real bank data in fixtures | mitigate    | Fixtures are synthetic; review chase_100.csv before commit to confirm no real data leaked from actual bank exports |
| T-04-36   | DoS                    | Flaky tests waste CI time  | mitigate    | Use `expect(locator).toBeVisible({ timeout: 5000 })` with explicit timeouts; avoid `page.waitForTimeout()`         |

</threat_model>

<verification>
- All 7 Phase-4 specs green via `pnpm playwright test --project=chromium e2e/12-..7-...spec.ts`.
- Full E2E suite still green (Phase 1-3 specs not regressed).
- `04-VALIDATION.md` `nyquist_compliant: true` + `status: approved` + per-task map filled.
- Phase 4 ready for `/gsd-verify-work`.
</verification>

<success_criteria>

1. 7 spec files cover every TXN-NN + every Phase-4 D-NN behavior end-to-end.
2. 3 fixture files exist; helpers.ts extended with 4 new helpers.
3. All Phase-4 specs green.
4. 04-VALIDATION.md signed off. </success_criteria>

<output>
After completion, create `.planning/phases/04-transaction-core/04-10-SUMMARY.md` with the spec count, the green/red status, the runtime per spec (so RETROSPECTIVE.md can reference it), and any flaky-test warnings.
</output>
