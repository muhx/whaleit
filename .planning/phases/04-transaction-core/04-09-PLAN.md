---
phase: 04-transaction-core
plan: 09
type: execute
wave: 6
depends_on: [04-06, 04-07, 04-08]
files_modified:
  - apps/frontend/src/hooks/use-pending-duplicates.ts
  - apps/frontend/src/pages/transactions/duplicate-review-sheet.tsx
  - apps/frontend/src/pages/transactions/duplicate-review-list.tsx
  - apps/frontend/src/pages/transactions/import/components/duplicate-review-list.tsx
  - apps/frontend/src/pages/transactions/transactions-page.tsx
  - apps/frontend/src/pages/transactions/import/steps/transaction-review-step.tsx
  - apps/frontend/src/pages/transactions/import/steps/transaction-confirm-step.tsx
  - apps/frontend/src/pages/transactions/__tests__/duplicate-review-sheet.test.tsx
autonomous: true
requirements: [TXN-06]
must_haves:
  truths:
    - "Duplicate-review surfaces match UI-SPEC §6 buckets exactly: ≥95 →
      bg-destructive/10 + label 'almost certainly duplicate'; 70-94 →
      bg-warning/10 + 'Likely duplicate'; 50-69 → bg-muted/50 + 'Possible
      duplicate'; <50 NEVER surfaced (D-09)"
    - "Wizard Review-step inline duplicate display lives in
      `import/components/duplicate-review-list.tsx` — per-row tinted background
      + confidence chip + per-row [Discard new] / [Keep both] actions"
    - "Post-import banner at top of `/transactions` (DuplicateBanner from plan
      04-06) wires its `pendingCount` from `usePendingDuplicateCount()` which
      reads pending pairs from localStorage"
    - "Banner [Review duplicates →] opens DuplicateReviewSheet — Sheet (desktop)
      / full-screen Dialog (mobile) — with two-card layout (EXISTING vs NEW)"
    - "Action button copy is verbatim from UI-SPEC §6 + CONTEXT specifics:
      `[Discard new]` (accent — most-likely action) and `[Keep both]`
      (secondary). NO 'Reject' / 'Accept' wording (CONTEXT specifics line
      432-435)"
    - "[Discard new] deletes the newly-imported transaction via
      `useDeleteTransaction()`; [Keep both] marks the pair as resolved
      (decrements `pendingCount`); [Skip for now] advances pagination without
      resolving"
    - "Empty-resolution state: when last unresolved pair is acted on, sheet
      shows 'All caught up' heading + 'Nice work — every duplicate has been
      reviewed.' + [Done] button"
    - "Per-pair dismiss-history is DEFERRED (D-10): Phase 4 ships one-shot
      review only — closing the sheet without resolving leaves pairs in the
      queue for later"
  artifacts:
    - path: "apps/frontend/src/pages/transactions/duplicate-review-sheet.tsx"
      provides:
        "Two-card review sheet with pagination + bucket-colored confidence chips"
      contains: "Discard new"
    - path: "apps/frontend/src/pages/transactions/import/components/duplicate-review-list.tsx"
      provides: "Inline per-row duplicate display in the wizard Review step"
      contains: "ALMOST_CERTAIN"
    - path: "apps/frontend/src/hooks/use-pending-duplicates.ts"
      provides:
        "Real implementation of pending-duplicate count + review-open state
        (replaces 04-07's stub)"
      contains: "usePendingDuplicateCount"
  key_links:
    - from: "apps/frontend/src/pages/transactions/transactions-page.tsx"
      to: "apps/frontend/src/pages/transactions/duplicate-review-sheet.tsx"
      via: "<DuplicateReviewSheet open={...} ... />"
      pattern: "<DuplicateReviewSheet"
    - from: "apps/frontend/src/pages/transactions/duplicate-banner.tsx"
      to: "apps/frontend/src/hooks/use-pending-duplicates.ts"
      via: "const pendingCount = usePendingDuplicateCount()"
      pattern: "usePendingDuplicateCount"
    - from: "apps/frontend/src/pages/transactions/import/steps/transaction-review-step.tsx"
      to: "apps/frontend/src/pages/transactions/import/components/duplicate-review-list.tsx"
      via: "<DuplicateReviewList drafts={...} />"
      pattern: "DuplicateReviewList"
---

<objective>
Land the two duplicate-review surfaces UI-SPEC §6 designs: the inline per-row duplicate display inside the import wizard's Review step, and the post-import full review sheet that the `/transactions` banner opens.

**Wave coordination (checker round 1 issue #1 re-pack):** Wave 5 = 04-06 + 04-08 (parallel, no file overlap). Wave 6 = 04-07 + 04-09 (both edit `transactions-page.tsx`, but 04-07 adds the form mount and 04-09 adds the DuplicateReviewSheet mount — they touch the same file. To avoid a wave-6 race, this plan depends on 04-07 explicitly, so 04-09 runs serial-after-04-07 inside wave 6 IF the scheduler honors intra-wave deps; otherwise this plan is bumped to wave 7). Wave 7 = 04-10 (E2E).

Purpose: TXN-06's user-facing flow. The detector logic lives in plan 04-02; the
import preview wires the candidates in 04-08; THIS plan presents them and lets
the user act on them. Output: Working duplicate-review UX with the friendly
copy + confidence buckets matching UI-SPEC + CONTEXT exactly. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-transaction-core/04-CONTEXT.md
@.planning/phases/04-transaction-core/04-RESEARCH.md
@.planning/phases/04-transaction-core/04-PATTERNS.md
@.planning/phases/04-transaction-core/04-UI-SPEC.md
@.planning/phases/04-transaction-core/04-06-PLAN.md
@.planning/phases/04-transaction-core/04-07-PLAN.md
@.planning/phases/04-transaction-core/04-08-PLAN.md
@apps/frontend/src/hooks/use-pending-duplicates.ts
@apps/frontend/src/pages/transactions/duplicate-banner.tsx
@apps/frontend/src/pages/transactions/transactions-page.tsx
@apps/frontend/src/pages/transactions/import/steps/transaction-review-step.tsx
@apps/frontend/src/pages/transactions/import/context/transaction-import-context.tsx
@apps/frontend/src/lib/types/transaction.ts
@apps/frontend/src/hooks/use-transactions.ts

<interfaces>
<!-- Stub from plan 04-07 to be replaced -->
From apps/frontend/src/hooks/use-pending-duplicates.ts (stub):
- usePendingDuplicateCount(): number — currently returns 0; this plan replaces with localStorage-backed reactive source.
- useReviewDuplicates(): { open, setOpen } — extended this plan into useDuplicateActions().

<!-- Hooks from 04-05 -->

From apps/frontend/src/hooks/use-transactions.ts:

- useDeleteTransaction() → mutation (used by Discard new)
- useTransaction(id) → useQuery<Transaction> (load EXISTING + NEW for the
  side-by-side cards)

<!-- Wizard context from 04-08 -->

From
apps/frontend/src/pages/transactions/import/context/transaction-import-context.tsx:

- DraftTransaction (with duplicateBucket / duplicateConfidence /
  existingTransactionId / userResolution fields)
- state.pendingDuplicates: { rowIndex, existingId, confidence, bucket }[]
  </interfaces> </context>

<tasks>

<task type="auto">
  <name>Task 1: Real `use-pending-duplicates.ts` (replaces stub) + DuplicateReviewSheet + page-side review-list</name>
  <files>
    apps/frontend/src/hooks/use-pending-duplicates.ts
    apps/frontend/src/pages/transactions/duplicate-review-sheet.tsx
    apps/frontend/src/pages/transactions/duplicate-review-list.tsx
  </files>
  <read_first>
    - apps/frontend/src/hooks/use-pending-duplicates.ts (stub from plan 04-07 task 3)
    - .planning/phases/04-transaction-core/04-UI-SPEC.md §6 lines 626-700 (full review-sheet spec — two-card layout + pagination + empty resolution state)
    - .planning/phases/04-transaction-core/04-CONTEXT.md §specifics lines 425-438 ("Discard new" / "Keep both" copy; never "Reject" / "Accept")
    - .planning/phases/04-transaction-core/04-RESEARCH.md §Categorization Memory + Color section (bucket → bg class mapping)
    - apps/frontend/src/pages/transactions/duplicate-banner.tsx (already wired in plan 04-06; this plan provides the live count source)
  </read_first>
  <action>
    1. **`apps/frontend/src/hooks/use-pending-duplicates.ts`** — replace the stub with a localStorage-backed reactive source.

       Storage strategy:
       - On import success (handled in Task 3 — Confirm step), the wizard writes the pending pair list to `localStorage` under key `whaleit:transactions:pendingDuplicates`.
       - `usePendingDuplicates()` reads localStorage and subscribes to storage events.
       - `useDuplicateActions()` provides `dismissPair`, `setPairs`, `clearAll` mutators that write back and dispatch a synthetic StorageEvent so same-tab listeners refresh.

       Concrete shape to implement:

       Define interface `PendingDuplicatePair { rowIndex: number; existingId: string; newId: string; confidence: number; bucket: "ALMOST_CERTAIN" | "LIKELY" | "POSSIBLE" }`.

       Define module-private constants `STORAGE_KEY = "whaleit:transactions:pendingDuplicates"`, plus pure helpers `readPairs()` (try-catch JSON.parse, returns empty array on any failure), `writePairs(pairs)` (writes JSON + dispatches a `StorageEvent` with `key=STORAGE_KEY`), and a `subscribe(cb)` function compatible with `useSyncExternalStore`.

       Export three hooks:
       - `usePendingDuplicates(): PendingDuplicatePair[]` — uses `useSyncExternalStore(subscribe, readPairs, () => [])`.
       - `usePendingDuplicateCount(): number` — derives from `usePendingDuplicates().length`.
       - `useDuplicateActions(): { dismissPair(rowIndex), setPairs(pairs), clearAll() }` — `useCallback`-wrapped mutators that call `writePairs`.

       The previous `useReviewDuplicates` from the stub is removed — its callers in plan 04-07 are local useState in the page (Task 3 below switches to `useState` for sheet open state).

    2. **`apps/frontend/src/pages/transactions/duplicate-review-list.tsx`** — page-side renderer used inside `DuplicateReviewSheet`. Renders the active pair as the two-card layout with the confidence chip between them (UI-SPEC §6).

       Props: `pair: PendingDuplicatePair`, `onDiscardNew: () => void`, `onKeepBoth: () => void`.

       Loads each pair's EXISTING and NEW transactions via `useTransaction(pair.existingId)` and `useTransaction(pair.newId)`. While loading: render Skeletons.

       Bucket → chip styles (these constants are also reused in Task 2's wizard-side list):
       - `ALMOST_CERTAIN` → `bg-destructive/10`, label `"≥95% match · almost certainly duplicate"`.
       - `LIKELY` → `bg-warning/10`, label `"Likely duplicate ({confidence}% match)"`.
       - `POSSIBLE` → `bg-muted/50`, label `"Possible duplicate ({confidence}% match)"`.

       Layout (per UI-SPEC §6 lines 648-670):
       - Two cards side-by-side desktop (`grid grid-cols-2 gap-3`); stack on mobile (`md:grid-cols-2 grid-cols-1`).
       - Each card: `bg-card border border-border rounded-md p-4` with `EXISTING` / `NEW` micro-label, then date / payee / category chip / signed amount / external_ref short hash.
       - Centered confidence chip between cards.
       - Footer slot rendered by parent (DuplicateReviewSheet provides `[Discard new]` and `[Keep both]` buttons).

    3. **`apps/frontend/src/pages/transactions/duplicate-review-sheet.tsx`** — Sheet (desktop) / Dialog (mobile) wrapper with pagination.

       Props: `open: boolean`, `onClose: () => void`.

       Internal state: `idx` (current pair index, starts at 0).

       Reads `pairs = usePendingDuplicates()` and `{ dismissPair } = useDuplicateActions()`. Mutation: `useDeleteTransaction()` (for `[Discard new]`).

       Render branches:
       - `pairs.length === 0` → empty resolution state. SheetTitle `"All caught up"`. Body paragraph (text-sm text-muted-foreground): `"Nice work — every duplicate has been reviewed."`. Footer: single `<Button onClick={onClose}>Done</Button>`.
       - Otherwise: SheetTitle `"Review duplicates · {idx + 1} of {pairs.length}"`. Body: `<DuplicateReviewList pair={current} onDiscardNew={...} onKeepBoth={...} />`. Footer pagination row: `[‹ Previous]` (ghost, disabled when idx===0) — `[Skip for now]` (outline, advances idx without resolving) — `[Next ›]` (ghost, disabled when idx >= pairs.length-1). The two action buttons `[Keep both]` (outline) and `[Discard new]` (default = accent) sit above the pagination row.

       Action handlers:
       - `handleDiscardNew()` → `await deleteTxn.mutateAsync(current.newId)` → `dismissPair(current.rowIndex)` → toast.success("Discarded duplicate") → if last pair, `onClose()`.
       - `handleKeepBoth()` → `dismissPair(current.rowIndex)` → toast("Kept both transactions") → if last pair, `onClose()`.
       - `handleSkip()` → `setIdx(idx + 1)` if not at end.

       After `dismissPair` is called, the pairs array shrinks via storage event re-render — keep `idx` clamped to `Math.min(idx, pairs.length - 1)` so pagination stays valid.

       Verbatim copy guard: this file MUST NOT use the words "Reject" or "Accept" (UI-SPEC violation). Use `[Discard new]` / `[Keep both]` exactly.

  </action>
  <verify>
    <automated>test -f apps/frontend/src/pages/transactions/duplicate-review-sheet.tsx && test -f apps/frontend/src/pages/transactions/duplicate-review-list.tsx && grep -q "Discard new" apps/frontend/src/pages/transactions/duplicate-review-sheet.tsx && grep -q "Keep both" apps/frontend/src/pages/transactions/duplicate-review-sheet.tsx && grep -q "All caught up" apps/frontend/src/pages/transactions/duplicate-review-sheet.tsx && ! grep -qE "Reject|Accept" apps/frontend/src/pages/transactions/duplicate-review-sheet.tsx && grep -q "ALMOST_CERTAIN" apps/frontend/src/pages/transactions/duplicate-review-list.tsx && grep -q "useDeleteTransaction\\|useDuplicateActions" apps/frontend/src/pages/transactions/duplicate-review-sheet.tsx && pnpm type-check 2>&1 | tail -5 | grep -qE "Done|0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `use-pending-duplicates.ts` exports `usePendingDuplicates`, `usePendingDuplicateCount`, `useDuplicateActions`.
    - `duplicate-review-sheet.tsx` references "Discard new", "Keep both", "All caught up" exactly.
    - File does NOT contain "Reject" or "Accept" as button labels (regression guard against the prohibited copy from CONTEXT specifics line 432-435).
    - `duplicate-review-list.tsx` references all three bucket strings (ALMOST_CERTAIN, LIKELY, POSSIBLE).
    - `pnpm type-check` green.
  </acceptance_criteria>
  <done>Sheet + list + hook compile; copy matches UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Wizard-side inline duplicate display (`import/components/duplicate-review-list.tsx`)</name>
  <files>
    apps/frontend/src/pages/transactions/import/components/duplicate-review-list.tsx
    apps/frontend/src/pages/transactions/import/steps/transaction-review-step.tsx
  </files>
  <read_first>
    - apps/frontend/src/pages/transactions/import/steps/transaction-review-step.tsx (created in plan 04-08 task 3)
    - apps/frontend/src/pages/transactions/import/context/transaction-import-context.tsx (DraftTransaction shape — duplicateBucket / userResolution fields)
    - .planning/phases/04-transaction-core/04-UI-SPEC.md §6 lines 626-700
  </read_first>
  <action>
    1. **`apps/frontend/src/pages/transactions/import/components/duplicate-review-list.tsx`** — wizard-side inline list. Renders rows with bucket-colored backgrounds + per-row [Discard new] / [Keep both] inline actions.

       Constants:
       - `BUCKET_BG = { ALMOST_CERTAIN: "bg-destructive/10", LIKELY: "bg-warning/10", POSSIBLE: "bg-muted/50" }`
       - `BUCKET_LABEL = { ALMOST_CERTAIN: "almost certainly duplicate", LIKELY: "Likely duplicate", POSSIBLE: "Possible duplicate" }`

       `DuplicateRow` props: `draft: DraftTransaction`, `existing: Transaction | undefined`, `onDiscardNew: () => void`, `onKeepBoth: () => void`. Returns null when `draft.duplicateBucket === null`.

       Card layout: outer `<div className={cn("rounded-md p-3 border", BUCKET_BG[draft.duplicateBucket])}>` containing:
       - Top row: bucket label + confidence percent (`{BUCKET_LABEL[bucket]} · {confidence}% match`).
       - Two-column grid showing EXISTING vs NEW with date / payee / amount / category chip.
       - Action row (right-aligned, gap-2): `<Button size="sm" variant="outline" onClick={onKeepBoth}>Keep both</Button>` then `<Button size="sm" onClick={onDiscardNew}>Discard new</Button>` (accent default variant).

       Container component `DuplicateReviewList` props: `drafts: DraftTransaction[]`, `onDiscardNew: (rowIndex: number) => void`, `onKeepBoth: (rowIndex: number) => void`. Filters drafts where `duplicateBucket !== null && userResolution === null`. Returns null when filtered list is empty. Otherwise renders a heading `{count} possible duplicate{s}` + maps to `<DuplicateRow>` per pair (resolves `existing` via `useTransaction(draft.existingTransactionId)`).

       Verbatim copy guard: this file MUST NOT use "Reject" or "Accept".

    2. **`apps/frontend/src/pages/transactions/import/steps/transaction-review-step.tsx`** — surgical edit:
       a. Import `DuplicateReviewList` from `./components/duplicate-review-list` (the new wizard component, NOT the page-side `/transactions/duplicate-review-list.tsx`).
       b. Render `<DuplicateReviewList drafts={state.drafts} onDiscardNew={handleDiscardNew} onKeepBoth={handleKeepBoth} />` ABOVE the editable preview table.
       c. The dispatch handlers update `draft.userResolution` to `'DISCARD_NEW'` or `'KEEP_BOTH'` in context. `'DISCARD_NEW'` ALSO sets `draft.skip = true` so it's excluded from the import payload sent in the Confirm step.
       d. Pass through to the per-row table so each draft row also shows its bucket-color tint per UI-SPEC §6 — set `className={cn(BUCKET_BG[draft.duplicateBucket])}` on the `<tr>` when `duplicateBucket !== null && userResolution === null`. (Once user resolves the pair, the tint clears so they can see they handled it.)

  </action>
  <verify>
    <automated>test -f apps/frontend/src/pages/transactions/import/components/duplicate-review-list.tsx && grep -q "DuplicateReviewList" apps/frontend/src/pages/transactions/import/steps/transaction-review-step.tsx && grep -q "ALMOST_CERTAIN\\|LIKELY\\|POSSIBLE" apps/frontend/src/pages/transactions/import/components/duplicate-review-list.tsx && grep -q "Discard new" apps/frontend/src/pages/transactions/import/components/duplicate-review-list.tsx && ! grep -qE "Reject|Accept" apps/frontend/src/pages/transactions/import/components/duplicate-review-list.tsx && pnpm type-check 2>&1 | tail -5 | grep -qE "Done|0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - File `import/components/duplicate-review-list.tsx` exists.
    - Review step imports + renders `DuplicateReviewList` above the preview table.
    - Component references all three bucket constants and the verbatim copy strings.
    - File does NOT contain "Reject" or "Accept".
    - `pnpm type-check` green.
  </acceptance_criteria>
  <done>Inline review wired; type-check green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire DuplicateReviewSheet into TransactionsPage + Confirm-step persistence + sheet test</name>
  <files>
    apps/frontend/src/pages/transactions/transactions-page.tsx
    apps/frontend/src/pages/transactions/import/steps/transaction-confirm-step.tsx
    apps/frontend/src/pages/transactions/__tests__/duplicate-review-sheet.test.tsx
  </files>
  <read_first>
    - apps/frontend/src/pages/transactions/transactions-page.tsx (created in 04-06; updated in 04-07; locate the placeholder `pendingDupes={0}` literal)
    - apps/frontend/src/pages/transactions/import/steps/transaction-confirm-step.tsx (created in 04-08; locate the `useImportTransactionsCsv().mutateAsync` success handler)
    - apps/frontend/src/hooks/use-pending-duplicates.ts (Task 1)
    - apps/frontend/src/pages/transactions/duplicate-review-sheet.tsx (Task 1)
  </read_first>
  <behavior>
    - Banner pendingCount source: live from `usePendingDuplicateCount()` (replaces literal 0 from plan 04-06/07).
    - Sheet opens from banner click OR auto-opens when URL contains `?reviewDuplicates=1`.
    - Confirm-step persists the unresolved pending pairs to localStorage IMMEDIATELY after the import mutation succeeds, before navigating to `/transactions`.
    - Tests cover empty + populated states + button copy regression.
  </behavior>
  <action>
    1. **`apps/frontend/src/pages/transactions/transactions-page.tsx`** — surgical edits:
       a. Replace the placeholder `const pendingDupes = 0;` with `const pendingDupes = usePendingDuplicateCount();`.
       b. Add `const [reviewOpen, setReviewOpen] = useState(false);`.
       c. Wire the banner: `<DuplicateBanner pendingCount={pendingDupes} onReview={() => setReviewOpen(true)} />`.
       d. Mount `<DuplicateReviewSheet open={reviewOpen} onClose={() => setReviewOpen(false)} />` at the page root.
       e. Auto-open the sheet when `?reviewDuplicates=1` is in the URL. DERIVE in render — do not use `useEffect`. Pattern:
          ```tsx
          const [reviewOpen, setReviewOpen] = useState(() => searchParams.get("reviewDuplicates") === "1");
          ```
          (lazy initializer honors the URL query on mount without an effect.)

    2. **`apps/frontend/src/pages/transactions/import/steps/transaction-confirm-step.tsx`** — surgical edit. After successful import mutation, persist pending pairs:

       After `const result = await importTransactionsCsv.mutateAsync(req);` (or `.import_ofx`), build the pair list from `state.drafts`:
       - Filter drafts where `d.duplicateBucket && !d.userResolution`.
       - Map each to `{ rowIndex: d.rowIndex, existingId: d.existingTransactionId!, newId: result.insertedRowIds[d.rowIndex], confidence: d.duplicateConfidence!, bucket: d.duplicateBucket! }`.
       - Call `setPairs(pairs)` from `useDuplicateActions()` BEFORE `navigate("/transactions")`.

       Toast `"{result.inserted} transactions imported"` then navigate.

       Note: `TransactionImportResult` includes `insertedRowIds: string[]` in input-row order — this is locked into plans 04-02 (core contract), 04-03 (repository order-preservation test), 04-04 (DTO), and 04-05 (frontend type) per checker round 1 issue #3. The executor here just consumes it. If the contract is somehow missing at runtime, that is a regression in an upstream plan and must be fixed by re-running that plan, NOT patched here.

    3. **`apps/frontend/src/pages/transactions/__tests__/duplicate-review-sheet.test.tsx`** — vitest + RTL. Required tests (≥6):
       - `renders 'All caught up' when pairs is empty`
       - `renders 'pair 1 of 3' header when 3 pairs queued`
       - `[Discard new] calls useDeleteTransaction with new id then dismisses pair`
       - `[Keep both] dismisses pair without calling delete`
       - `[Skip for now] advances to next pair without resolving`
       - `bucket=ALMOST_CERTAIN renders bg-destructive/10 + 'almost certainly duplicate' label`
       - `Sheet does NOT contain the words 'Reject' or 'Accept'` (regression guard)
       - `closing the sheet without resolving leaves pairs in queue (D-10 deferred dismiss-history)`

       Mock pattern: stub localStorage to return a known pair array; mock `useDeleteTransaction` to return a vi.fn-backed mutation. Use `useSyncExternalStore` shim if needed (vitest's jsdom supports `localStorage` natively, so `localStorage.setItem(STORAGE_KEY, JSON.stringify(pairs))` before render is sufficient).

  </action>
  <verify>
    <automated>grep -q "usePendingDuplicateCount" apps/frontend/src/pages/transactions/transactions-page.tsx && grep -q "<DuplicateReviewSheet" apps/frontend/src/pages/transactions/transactions-page.tsx && grep -q "useDuplicateActions\\|setPairs" apps/frontend/src/pages/transactions/import/steps/transaction-confirm-step.tsx && pnpm test -- --run transactions/__tests__/duplicate-review-sheet 2>&1 | tail -10 | grep -qE "PASS|passed" && pnpm type-check 2>&1 | tail -5 | grep -qE "Done|0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `transactions-page.tsx` references `usePendingDuplicateCount` and mounts `<DuplicateReviewSheet />`.
    - `transaction-confirm-step.tsx` references `useDuplicateActions` or `setPairs` to persist pending pairs after successful import.
    - Test file runs ≥6 cases; all pass.
    - `pnpm type-check` green.
  </acceptance_criteria>
  <done>Banner→Sheet wired live; tests green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                                                                                                                           |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| localStorage → render | localStorage is per-user-browser; not shared cross-user. Pairs reference transaction IDs the user already owns (server enforces ownership on `useDeleteTransaction`). |

## STRIDE Threat Register

| Threat ID | Category               | Component                                           | Disposition | Mitigation Plan                                                                                                                                                   |
| --------- | ---------------------- | --------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-32   | Tampering              | localStorage tampered to point at another user's id | mitigate    | `[Discard new]` calls `useDeleteTransaction(newId)`; server enforces ownership and returns 404 for non-owned ids — UI surfaces error toast, banner stays          |
| T-04-33   | Information Disclosure | XSS via payee in card display                       | mitigate    | React default text-content escaping; payee rendered as text node, never as HTML                                                                                   |
| T-04-34   | Tampering              | Pairs persist after logout                          | mitigate    | Existing logout flow already calls `localStorage.clear()` for app namespace (verify exists; if not, add `localStorage.removeItem(STORAGE_KEY)` to logout handler) |

</threat_model>

<verification>
- `pnpm type-check` green.
- `pnpm test -- --run transactions/__tests__` includes duplicate-review-sheet tests, all pass.
- `pnpm lint` clean for new files (no `useEffect` for derivable state).
- Manual smoke: import a CSV with one synthetic duplicate → confirm step persists pair → /transactions banner shows count 1 → click [Review duplicates] → sheet shows pair 1 of 1 with correct bucket tint → [Discard new] removes the new transaction → sheet shows "All caught up" → [Done] closes.
</verification>

<success_criteria>

1. `use-pending-duplicates.ts` upgraded from stub to localStorage-backed
   reactive hook.
2. `DuplicateReviewSheet` + `DuplicateReviewList` (page-side) ship verbatim
   UI-SPEC §6 copy and bucket colors.
3. Wizard-side `import/components/duplicate-review-list.tsx` surfaces inline
   duplicates per row inside the Review step.
4. Confirm step persists unresolved pairs to localStorage so the banner picks
   them up post-navigate.
5. Sheet test guards against "Reject"/"Accept" copy regressions.
6. All tests + type-check + lint green. </success_criteria>

<output>
After completion, create `.planning/phases/04-transaction-core/04-09-SUMMARY.md` with the localStorage key + pair shape (so any future plan can read/write the queue) and the test counts.
</output>
