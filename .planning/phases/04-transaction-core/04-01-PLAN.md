---
phase: 04-transaction-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/storage-postgres/migrations/20260501000000_transactions_initial/up.sql
  - crates/storage-postgres/migrations/20260501000000_transactions_initial/down.sql
  - crates/storage-postgres/migrations/20260501010000_transaction_csv_templates/up.sql
  - crates/storage-postgres/migrations/20260501010000_transaction_csv_templates/down.sql
  - crates/storage-postgres/src/schema.rs
  - crates/storage-postgres/src/transactions/mod.rs
  - crates/storage-postgres/src/lib.rs
  - crates/storage-postgres/src/transactions/migration_tests.rs
autonomous: false
requirements: [TXN-01, TXN-04, TXN-05, TXN-06, TXN-07, TXN-08, TXN-09]
must_haves:
  truths:
    - "Migration `20260501000000_transactions_initial` creates `transactions`,
      `transaction_splits`, `payee_category_memory` tables and
      `v_transactions_with_running_balance` VIEW"
    - "System taxonomy 'Transaction Categories' with id
      `sys_taxonomy_transaction_categories` is seeded with 10 default categories
      (cat_income, cat_dining, cat_entertainment, cat_groceries, cat_healthcare,
      cat_housing, cat_shopping, cat_transport, cat_utilities,
      cat_uncategorized)"
    - "All money columns use NUMERIC(20,8) (per Phase 3 D-10 carry-forward)"
    - "transactions.transfer_group_id is NULL except on TRANSFER-direction rows
      (D-01); transfer_leg_role enum SOURCE/DESTINATION drives running-balance
      sign"
    - "transactions.idempotency_key has UNIQUE constraint and is NULLable only
      for system-generated reconciliation rows"
    - "transactions.is_system_generated marks reconciliation rows synthesized by
      Phase-3 D-14 hook"
    - "transactions.fx_rate + fx_rate_source columns capture per-row FX snapshot
      (D-02 cross-currency transfers; TXN-07)"
    - "D-12 wiring: `payee_category_memory(account_id, normalized_merchant,
      category_id, last_seen_at, seen_count)` with composite PRIMARY KEY
      `(account_id, normalized_merchant)` — supports last-write-wins upsert"
    - "D-16/17/18 templates wiring: `transaction_csv_templates(id, name,
      mapping JSONB, header_signature TEXT[], created_at, updated_at)` —
      user-saved (D-16), header-signature for D-17 mismatch detection,
      globally-scoped per user (D-18 — no account_id column)"
    - "schema.rs is regenerated to expose all 3 new tables and matches the live
      migrated DB exactly"
    - "Migration up + down round-trip cleanly; re-running up.sql is idempotent
      (ON CONFLICT (id) DO NOTHING for seeds)"
  artifacts:
    - path: "crates/storage-postgres/migrations/20260501000000_transactions_initial/up.sql"
      provides:
        "transactions + splits + payee memory + view + system taxonomy seed"
      contains: "CREATE TABLE transactions"
    - path: "crates/storage-postgres/migrations/20260501000000_transactions_initial/down.sql"
      provides: "Reverse-DDL in dependency order"
      contains: "DROP TABLE IF EXISTS transactions"
    - path: "crates/storage-postgres/src/schema.rs"
      provides:
        "Diesel table! macros for
        transactions/transaction_splits/payee_category_memory"
      contains: "table! { transactions"
    - path: "crates/storage-postgres/src/transactions/mod.rs"
      provides:
        "Module root for transactions storage; gated `pub mod migration_tests;`
        for #[cfg(test)]"
      contains: "pub mod"
    - path: "crates/storage-postgres/migrations/20260501010000_transaction_csv_templates/up.sql"
      provides: "transaction_csv_templates table for D-16/17/18"
      contains: "CREATE TABLE transaction_csv_templates"
    - path: "crates/storage-postgres/migrations/20260501010000_transaction_csv_templates/down.sql"
      provides: "Reverse-DDL for templates table"
      contains: "DROP TABLE IF EXISTS transaction_csv_templates"
  key_links:
    - from: "crates/storage-postgres/src/lib.rs"
      to: "crates/storage-postgres/src/transactions/mod.rs"
      via: "`pub mod transactions;`"
      pattern: "pub mod transactions"
    - from: "crates/storage-postgres/src/schema.rs"
      to: "crates/storage-postgres/migrations/20260501000000_transactions_initial/up.sql"
      via: "diesel print-schema (or app-launch embed-and-apply)"
      pattern: "table! \\{ transactions"
---

<objective>
Land the Phase 4 PostgreSQL schema: `transactions`, `transaction_splits`, `payee_category_memory`, the running-balance view, and the seeded "Transaction Categories" system taxonomy. Regenerate `schema.rs` so downstream Rust crates compile against the new tables.

Purpose: Every other Phase 4 plan depends on this schema. Without it,
`crates/core/src/transactions/` cannot type-check against Diesel. Output: One
migration directory + regenerated `schema.rs` + an empty
`crates/storage-postgres/src/transactions/` module skeleton wired into `lib.rs`
so subsequent plans can drop in `model.rs` / `repository.rs` without touching
`lib.rs` again. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-transaction-core/04-CONTEXT.md
@.planning/phases/04-transaction-core/04-RESEARCH.md
@.planning/phases/04-transaction-core/04-PATTERNS.md
@.planning/phases/04-transaction-core/04-VALIDATION.md
@crates/storage-postgres/migrations/20260425000000_accounts_extend_types_and_balances/up.sql
@crates/storage-postgres/migrations/20260425000000_accounts_extend_types_and_balances/down.sql
@crates/storage-postgres/migrations/20260101000000_initial_schema/up.sql
@crates/storage-postgres/src/schema.rs
@crates/storage-postgres/src/accounts/mod.rs
@crates/storage-postgres/src/accounts/migration_tests.rs
@crates/storage-postgres/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `up.sql` — tables + view + seed</name>
  <files>crates/storage-postgres/migrations/20260501000000_transactions_initial/up.sql</files>
  <read_first>
    - crates/storage-postgres/migrations/20260425000000_accounts_extend_types_and_balances/up.sql (header-comment style + NUMERIC(20,8) precedent)
    - crates/storage-postgres/migrations/20260101000000_initial_schema/up.sql (taxonomies / taxonomy_categories table shape — required for FK + seed)
    - .planning/phases/04-transaction-core/04-RESEARCH.md §Schema Design (lines 542-731 — full SQL)
    - .planning/phases/04-transaction-core/04-CONTEXT.md §decisions D-01, D-02, D-08, D-13, D-14 carry-forward
  </read_first>
  <action>
    Create new directory `crates/storage-postgres/migrations/20260501000000_transactions_initial/` and write `up.sql`. The file is the verbatim DDL from RESEARCH §Schema Design lines 546-689 PLUS the missing `transfer_leg_role` column flagged in the NOTE block at lines 679-689 PLUS the seed block from lines 691-722.

    Header comment (3 lines, mirrors `20260425000000_accounts_extend_types_and_balances/up.sql:1-7` style):
    ```
    -- Phase 4: Transaction Core schema (TXN-01..09).
    -- Creates `transactions`, `transaction_splits`, `payee_category_memory`,
    -- `v_transactions_with_running_balance`. Money columns NUMERIC(20,8) per Phase 3 D-10.
    ```

    Concrete contents — produce these exact statements in order:

    1. `CREATE EXTENSION IF NOT EXISTS pg_trgm;` (required for `idx_tx_payee_trgm` GIN index).

    2. `CREATE TABLE transactions (...)` with these columns (NUMERIC(20,8) for money, TEXT for ids, TIMESTAMP for stamps):
       - `id TEXT PRIMARY KEY`
       - `account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE RESTRICT`
       - `direction TEXT NOT NULL CHECK (direction IN ('INCOME','EXPENSE','TRANSFER'))`
       - `amount NUMERIC(20,8) NOT NULL CHECK (amount > 0)`
       - `currency TEXT NOT NULL`
       - `transaction_date DATE NOT NULL`
       - `payee TEXT`
       - `notes TEXT`
       - `category_id TEXT REFERENCES taxonomy_categories(id) ON DELETE SET NULL`
       - `has_splits BOOLEAN NOT NULL DEFAULT FALSE`
       - `fx_rate NUMERIC(20,8)` and `fx_rate_source TEXT CHECK (fx_rate_source IN ('SYSTEM','MANUAL_OVERRIDE'))` (NULLable; both NULL together — see Risk #9)
       - `transfer_group_id TEXT`
       - `counterparty_account_id TEXT REFERENCES accounts(id) ON DELETE RESTRICT`
       - `transfer_leg_role TEXT CHECK (transfer_leg_role IN ('SOURCE','DESTINATION'))` (NULLable; non-NULL only when direction='TRANSFER')
       - `idempotency_key TEXT UNIQUE` (NULLable; only system-generated reconciliation rows skip it)
       - `import_run_id TEXT`
       - `source TEXT NOT NULL CHECK (source IN ('MANUAL','CSV','OFX','SYSTEM'))`
       - `external_ref TEXT`
       - `is_system_generated BOOLEAN NOT NULL DEFAULT FALSE`
       - `is_user_modified BOOLEAN NOT NULL DEFAULT FALSE`
       - `category_source TEXT CHECK (category_source IN ('USER','MEMORY','IMPORT'))`
       - `created_at TIMESTAMP NOT NULL DEFAULT (NOW() AT TIME ZONE 'utc')`
       - `updated_at TIMESTAMP NOT NULL DEFAULT (NOW() AT TIME ZONE 'utc')`
       Plus three CHECK constraints:
       - `transfer_must_have_group_id`: `(direction='TRANSFER' AND transfer_group_id IS NOT NULL) OR (direction!='TRANSFER' AND transfer_group_id IS NULL)`
       - `non_transfer_must_have_payee`: `direction='TRANSFER' OR payee IS NOT NULL`
       - `counterparty_only_for_transfer`: `(direction='TRANSFER' AND counterparty_account_id IS NOT NULL) OR (direction!='TRANSFER' AND counterparty_account_id IS NULL)`
       - `transfer_leg_role_pairs_with_transfer`: `(direction='TRANSFER' AND transfer_leg_role IS NOT NULL) OR (direction!='TRANSFER' AND transfer_leg_role IS NULL)`
       - `fx_rate_pair`: `(fx_rate IS NULL AND fx_rate_source IS NULL) OR (fx_rate IS NOT NULL AND fx_rate_source IS NOT NULL)`

    3. The 7 indexes from RESEARCH §Schema Design lines 602-612 verbatim:
       `idx_tx_account_date`, `idx_tx_account_idempotency`, `idx_tx_transfer_group` (partial WHERE NOT NULL), `idx_tx_category` (partial), `idx_tx_import_run` (partial), `idx_tx_payee_trgm` (GIN trgm), `idx_tx_date`, `idx_tx_has_splits` (partial WHERE has_splits=TRUE).

    4. `CREATE TABLE transaction_splits (...)` — verbatim from RESEARCH lines 617-626 (id, transaction_id FK CASCADE, category_id FK RESTRICT, amount NUMERIC(20,8), notes, sort_order, timestamps). Plus indexes `idx_tx_splits_tx_id`, `idx_tx_splits_category` from lines 628-629.

    5. `CREATE TABLE payee_category_memory (...)` — verbatim from RESEARCH lines 638-645 (composite PK `(account_id, normalized_merchant)`, category_id FK CASCADE, last_seen_at, seen_count). Plus index `idx_payee_mem_last_seen` from line 647.

    6. `CREATE VIEW v_transactions_with_running_balance AS SELECT t.*, SUM(...)` — verbatim from RESEARCH lines 652-676. Window: `PARTITION BY t.account_id ORDER BY t.transaction_date ASC, t.created_at ASC ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW`. Sum CASE: INCOME → +amount, EXPENSE → -amount, TRANSFER+DESTINATION → +amount, TRANSFER+SOURCE → -amount, ELSE 0.

    7. Seed block — verbatim from RESEARCH lines 691-722:
       - `INSERT INTO taxonomies` row with `id='sys_taxonomy_transaction_categories'`, `name='Transaction Categories'`, `is_system=TRUE`, `is_single_select=TRUE`, color `#8abceb`, sort_order 100. Append `ON CONFLICT (id) DO NOTHING`.
       - 10 `INSERT INTO taxonomy_categories` rows with stable IDs `cat_income`, `cat_dining`, `cat_entertainment`, `cat_groceries`, `cat_healthcare`, `cat_housing`, `cat_shopping`, `cat_transport`, `cat_utilities`, `cat_uncategorized` and sort_orders 0,1,2,3,4,5,6,7,8,99 with the colors specified in UI-SPEC §Category Model. Append `ON CONFLICT (id) DO NOTHING`.

    Do NOT add separate `created_at` / `updated_at` triggers — `accounts` and other tables in the workspace stamp updates application-side; mirror that. Service layer is responsible for `updated_at = NOW()` on UPDATE.

  </action>
  <verify>
    <automated>test -f crates/storage-postgres/migrations/20260501000000_transactions_initial/up.sql && grep -q "CREATE TABLE transactions" crates/storage-postgres/migrations/20260501000000_transactions_initial/up.sql && grep -q "CREATE TABLE transaction_splits" crates/storage-postgres/migrations/20260501000000_transactions_initial/up.sql && grep -q "CREATE TABLE payee_category_memory" crates/storage-postgres/migrations/20260501000000_transactions_initial/up.sql && grep -q "CREATE VIEW v_transactions_with_running_balance" crates/storage-postgres/migrations/20260501000000_transactions_initial/up.sql && grep -q "sys_taxonomy_transaction_categories" crates/storage-postgres/migrations/20260501000000_transactions_initial/up.sql && grep -q "transfer_leg_role" crates/storage-postgres/migrations/20260501000000_transactions_initial/up.sql && grep -cE "ON CONFLICT \(id\) DO NOTHING" crates/storage-postgres/migrations/20260501000000_transactions_initial/up.sql | awk '$1 >= 2'</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/storage-postgres/migrations/20260501000000_transactions_initial/up.sql` exists.
    - Contains literal strings `CREATE TABLE transactions`, `CREATE TABLE transaction_splits`, `CREATE TABLE payee_category_memory`, `CREATE VIEW v_transactions_with_running_balance`.
    - Contains `transfer_leg_role` column declaration and CHECK constraint.
    - Contains all 10 seeded category IDs (`cat_income`, `cat_dining`, `cat_entertainment`, `cat_groceries`, `cat_healthcare`, `cat_housing`, `cat_shopping`, `cat_transport`, `cat_utilities`, `cat_uncategorized`).
    - Contains `CREATE EXTENSION IF NOT EXISTS pg_trgm`.
    - At least 2 `ON CONFLICT (id) DO NOTHING` clauses (taxonomy + categories).
    - Money columns use `NUMERIC(20,8)` (grep `NUMERIC(20,8)` returns ≥3 matches: amount, fx_rate, splits.amount).
  </acceptance_criteria>
  <done>up.sql committed; greps above all pass.</done>
</task>

<task type="auto">
  <name>Task 2: Write `down.sql` — reverse DDL in dependency order</name>
  <files>crates/storage-postgres/migrations/20260501000000_transactions_initial/down.sql</files>
  <read_first>
    - crates/storage-postgres/migrations/20260425000000_accounts_extend_types_and_balances/down.sql (style)
    - crates/storage-postgres/migrations/20260501000000_transactions_initial/up.sql (created in Task 1)
  </read_first>
  <action>
    Reverse Task 1 in dependency order. Statements (one per line, all idempotent via `IF EXISTS`):

    1. `DROP VIEW IF EXISTS v_transactions_with_running_balance;`
    2. `DROP TABLE IF EXISTS transaction_splits;`
    3. `DROP TABLE IF EXISTS payee_category_memory;`
    4. `DROP TABLE IF EXISTS transactions;`
    5. `DELETE FROM taxonomy_categories WHERE taxonomy_id = 'sys_taxonomy_transaction_categories';`
    6. `DELETE FROM taxonomies WHERE id = 'sys_taxonomy_transaction_categories';`

    Do NOT drop the `pg_trgm` extension — other Phase 4+ features may rely on it; harmless to leave installed.

  </action>
  <verify>
    <automated>test -f crates/storage-postgres/migrations/20260501000000_transactions_initial/down.sql && grep -q "DROP VIEW IF EXISTS v_transactions_with_running_balance" crates/storage-postgres/migrations/20260501000000_transactions_initial/down.sql && grep -q "DROP TABLE IF EXISTS transactions" crates/storage-postgres/migrations/20260501000000_transactions_initial/down.sql && grep -q "DELETE FROM taxonomies WHERE id = 'sys_taxonomy_transaction_categories'" crates/storage-postgres/migrations/20260501000000_transactions_initial/down.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists.
    - Contains `DROP VIEW IF EXISTS v_transactions_with_running_balance`, `DROP TABLE IF EXISTS transactions`, `DELETE FROM taxonomies WHERE id = 'sys_taxonomy_transaction_categories'`.
    - Order is: VIEW → splits → payee_memory → transactions → category rows → taxonomy row.
  </acceptance_criteria>
  <done>down.sql written and greps pass.</done>
</task>

<task type="auto">
  <name>Task 3: Write `transaction_csv_templates` migration (D-16/17/18 templates table)</name>
  <files>
    crates/storage-postgres/migrations/20260501010000_transaction_csv_templates/up.sql
    crates/storage-postgres/migrations/20260501010000_transaction_csv_templates/down.sql
  </files>
  <read_first>
    - crates/storage-postgres/migrations/20260101000000_initial_schema/up.sql (JSONB column precedent if any)
    - crates/storage-postgres/migrations/20260501000000_transactions_initial/up.sql (Task 1 — header style + idempotency seed pattern)
    - .planning/phases/04-transaction-core/04-CONTEXT.md §decisions D-16, D-17, D-18 (user-saved only, header_signature for mismatch detection, globally-scoped per user)
    - .planning/phases/04-transaction-core/04-RESEARCH.md §Frontend Wizard Fork Plan lines 1259-1330 (template shape)
  </read_first>
  <action>
    Create new directory `crates/storage-postgres/migrations/20260501010000_transaction_csv_templates/` with `up.sql` and `down.sql`.

    **up.sql** — header comment then DDL:
    ```
    -- Phase 4: CSV import templates (D-16/17/18).
    -- D-16: user-saved only, no starter pack.
    -- D-17: header_signature TEXT[] enables column-position mismatch detection.
    -- D-18: globally-scoped per user (NO account_id column — same template visible across all user accounts).
    ```

    Then:
    ```sql
    CREATE TABLE transaction_csv_templates (
        id TEXT PRIMARY KEY,
        name TEXT NOT NULL,
        mapping JSONB NOT NULL,
        header_signature TEXT[] NOT NULL,
        created_at TIMESTAMP NOT NULL DEFAULT (NOW() AT TIME ZONE 'utc'),
        updated_at TIMESTAMP NOT NULL DEFAULT (NOW() AT TIME ZONE 'utc'),
        CONSTRAINT transaction_csv_templates_name_not_empty CHECK (length(trim(name)) > 0)
    );

    CREATE UNIQUE INDEX idx_transaction_csv_templates_name ON transaction_csv_templates (name);
    ```

    **down.sql** — reverse:
    ```sql
    DROP TABLE IF EXISTS transaction_csv_templates;
    ```

    NOTE: This is a separate migration directory (20260501010000_*) deliberately ordered AFTER the 20260501000000 transactions migration so the schema.rs regeneration in Task 6 covers BOTH migrations in a single Diesel `print-schema` pass.
  </action>
  <verify>
    <automated>test -f crates/storage-postgres/migrations/20260501010000_transaction_csv_templates/up.sql && test -f crates/storage-postgres/migrations/20260501010000_transaction_csv_templates/down.sql && grep -q "CREATE TABLE transaction_csv_templates" crates/storage-postgres/migrations/20260501010000_transaction_csv_templates/up.sql && grep -q "header_signature" crates/storage-postgres/migrations/20260501010000_transaction_csv_templates/up.sql && grep -q "DROP TABLE IF EXISTS transaction_csv_templates" crates/storage-postgres/migrations/20260501010000_transaction_csv_templates/down.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist in `20260501010000_transaction_csv_templates/`.
    - `up.sql` declares `transaction_csv_templates` table with columns: `id`, `name`, `mapping JSONB`, `header_signature TEXT[]`, `created_at`, `updated_at`.
    - Table has NO `account_id` column (D-18 globally-scoped).
    - `name` has UNIQUE index (so saving twice with same name updates instead of duplicates — service layer handles ON CONFLICT).
    - `down.sql` drops the table cleanly.
  </acceptance_criteria>
  <done>Templates migration files committed.</done>
</task>

<task type="auto">
  <name>Task 4: Stub `crates/storage-postgres/src/transactions/` module + wire into lib.rs</name>
  <files>
    crates/storage-postgres/src/transactions/mod.rs
    crates/storage-postgres/src/lib.rs
  </files>
  <read_first>
    - crates/storage-postgres/src/accounts/mod.rs (template — see "exact-pattern" in PATTERNS.md Layer 3 line 77)
    - crates/storage-postgres/src/lib.rs (add `pub mod transactions;` adjacent to `pub mod accounts;`)
  </read_first>
  <action>
    Create empty stub `crates/storage-postgres/src/transactions/mod.rs` with body:

    ```rust
    //! Transactions storage — Phase 4. Implementation lands in plans 04-03+.

    pub mod model;
    pub mod repository;

    pub use model::{TransactionDB, TransactionSplitDB, PayeeCategoryMemoryDB};
    pub use repository::PgTransactionRepository;

    #[cfg(test)]
    mod migration_tests;
    #[cfg(test)]
    mod repository_tests;
    ```

    Then create empty placeholder files so the module compiles:
    - `crates/storage-postgres/src/transactions/model.rs` containing only:
      ```rust
      //! Placeholder — TransactionDB / TransactionSplitDB / PayeeCategoryMemoryDB land in plan 04-03.
      ```
    - `crates/storage-postgres/src/transactions/repository.rs` containing only:
      ```rust
      //! Placeholder — PgTransactionRepository lands in plan 04-03.
      ```

    BUT since `mod.rs` re-exports types/structs that don't exist yet, the simplest option is to leave `mod.rs` minimal for now:

    Final `mod.rs` body for THIS plan only:
    ```rust
    //! Transactions storage — Phase 4. Implementation lands in plans 04-03+.

    #[cfg(test)]
    mod migration_tests;
    ```

    (The full re-exports get added by plan 04-03 alongside the actual `model.rs` / `repository.rs`. Plan 04-03's frontmatter already shows them in `files_modified`.)

    Then EDIT `crates/storage-postgres/src/lib.rs`: add `pub mod transactions;` next to the existing `pub mod accounts;` declaration. Place alphabetically; no other changes.

  </action>
  <verify>
    <automated>test -f crates/storage-postgres/src/transactions/mod.rs && grep -q "pub mod transactions" crates/storage-postgres/src/lib.rs && cargo check -p whaleit-storage-postgres 2>&1 | tail -5 | grep -qE "Finished|Compiling"</automated>
  </verify>
  <acceptance_criteria>
    - `crates/storage-postgres/src/transactions/mod.rs` exists.
    - `crates/storage-postgres/src/lib.rs` contains `pub mod transactions`.
    - `cargo check -p whaleit-storage-postgres` succeeds (no compile errors).
  </acceptance_criteria>
  <done>cargo check green for whaleit-storage-postgres.</done>
</task>

<task type="auto">
  <name>Task 5: Write migration round-trip test</name>
  <files>crates/storage-postgres/src/transactions/migration_tests.rs</files>
  <read_first>
    - crates/storage-postgres/src/accounts/migration_tests.rs (exact analog — 2.0K)
    - crates/storage-postgres/migrations/20260501000000_transactions_initial/up.sql
    - crates/storage-postgres/migrations/20260501000000_transactions_initial/down.sql
    - crates/storage-postgres/migrations/20260501010000_transaction_csv_templates/up.sql (Task 3)
    - crates/storage-postgres/migrations/20260501010000_transaction_csv_templates/down.sql (Task 3)
  </read_first>
  <action>
    Mirror `crates/storage-postgres/src/accounts/migration_tests.rs` 1:1. Tests required:

    1. `test_migration_up_creates_transactions_table` — boots a testcontainer Postgres, runs migrations, asserts row from `information_schema.tables` for each of: `transactions`, `transaction_splits`, `payee_category_memory`.
    2. `test_migration_up_creates_running_balance_view` — asserts row in `information_schema.views` for `v_transactions_with_running_balance`.
    3. `test_migration_up_seeds_transaction_categories` — asserts:
       - `SELECT COUNT(*) FROM taxonomies WHERE id='sys_taxonomy_transaction_categories'` returns 1
       - `SELECT COUNT(*) FROM taxonomy_categories WHERE taxonomy_id='sys_taxonomy_transaction_categories'` returns 10
    4. `test_migration_up_is_idempotent` — re-runs `up.sql` (or the migrations harness twice) and confirms category count remains 10.
    5. `test_migration_down_reverses` — runs all migrations up, then runs the latest down.sql, asserts none of the new tables exist (information_schema empty) AND the `sys_taxonomy_transaction_categories` row is gone.

    6. `test_templates_migration_creates_table` — asserts row in `information_schema.tables` for `transaction_csv_templates` with columns id, name, mapping (JSONB), header_signature (ARRAY), created_at, updated_at.

    7. `test_templates_migration_round_trip` — runs templates up.sql then down.sql; asserts table is gone.

    Use the workspace's existing testcontainer harness (look in `crates/storage-postgres/src/accounts/migration_tests.rs` for the helper used; reuse identically).

    Wave 0 dependency note: this test file IS Wave 0 for migration validation. Other plans depend on a green run of this file before they layer on top.

  </action>
  <verify>
    <automated>cargo test -p whaleit-storage-postgres --features integration transactions::migration_tests 2>&1 | tail -10 | grep -qE "test result.*ok"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with 5 `#[tokio::test]` (or equivalent) functions.
    - All 5 tests pass against a testcontainer Postgres.
    - File mirrors the structure of `accounts/migration_tests.rs` (same harness imports, same async pattern).
  </acceptance_criteria>
  <done>cargo test for transactions::migration_tests reports OK.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: [BLOCKING] Regenerate `schema.rs` against migrated DB (covers BOTH migrations)</name>
  <files>crates/storage-postgres/src/schema.rs</files>
  <what-built>Migration files written and migration_tests pass against testcontainer. Now `schema.rs` must be regenerated so it exposes Diesel `table!` macros for `transactions`, `transaction_splits`, `payee_category_memory`. Without this, plan 04-03 cannot use Diesel DSL against the new tables.</what-built>
  <how-to-verify>
    User runs locally (Diesel `print-schema` requires a live DB connection — non-autonomous):

    1. Spin up Postgres + apply all migrations:
       ```
       docker compose up -d postgres
       export DATABASE_URL=postgres://whaleit:whaleit@localhost:5432/whaleit
       diesel migration run --migration-dir crates/storage-postgres/migrations
       ```
    2. Regenerate schema:
       ```
       diesel print-schema --database-url "$DATABASE_URL" > crates/storage-postgres/src/schema.rs
       ```
    3. Verify the new tables appear (4 total: 3 from transactions migration + 1 from templates migration):
       ```
       grep -q "table! { transactions" crates/storage-postgres/src/schema.rs
       grep -q "table! { transaction_splits" crates/storage-postgres/src/schema.rs
       grep -q "table! { payee_category_memory" crates/storage-postgres/src/schema.rs
       grep -q "table! { transaction_csv_templates" crates/storage-postgres/src/schema.rs
       ```
    4. Run the workspace check:
       ```
       cargo check --workspace
       ```
       Expected: green. (Plans 04-02/04-03 will fill in the Rust types.)
    5. Run full storage-postgres test suite:
       ```
       cargo test -p whaleit-storage-postgres --features integration 2>&1 | tail -5
       ```
       Expected: all green.

    If `cargo check --workspace` fails with "table not found" errors against `transactions`, schema.rs was NOT regenerated correctly — fix and re-run.

    If any other crate breaks because of an unrelated schema regen drift (existing crate columns reordered or missing), STOP and report — do not paper over with hand edits.

  </how-to-verify>
  <resume-signal>Type "approved" once schema.rs has the three new `table!` blocks AND `cargo check --workspace` is green AND `cargo test -p whaleit-storage-postgres` is green. If anything fails, paste the error and we'll diagnose.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary       | Description                                                                      |
| -------------- | -------------------------------------------------------------------------------- |
| migration → DB | DDL is trusted (workspace-only); seeds use stable IDs (no UUIDs) for idempotency |

## STRIDE Threat Register

| Threat ID | Category               | Component                  | Disposition | Mitigation Plan                                                                                                                                         |
| --------- | ---------------------- | -------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-01   | Tampering              | seed UUIDs drift on re-run | mitigate    | Stable string IDs (`sys_taxonomy_transaction_categories`, `cat_income`, ...) + `ON CONFLICT (id) DO NOTHING` (RESEARCH Risk #10)                        |
| T-04-02   | Tampering              | direction/sign mismatch    | mitigate    | DB CHECK constraints (transfer_must_have_group_id, non_transfer_must_have_payee, counterparty_only_for_transfer, transfer_leg_role_pairs_with_transfer) |
| T-04-03   | Information Disclosure | account FK CASCADE leak    | accept      | `ON DELETE RESTRICT` on transactions.account_id prevents accidental cascade leak                                                                        |
| T-04-04   | Tampering              | idempotency_key collision  | mitigate    | UNIQUE constraint on transactions.idempotency_key; service catches dup-key error and returns 409                                                        |

</threat_model>

<verification>
- `cargo check --workspace` green after regen.
- `cargo test -p whaleit-storage-postgres --features integration transactions::migration_tests` green.
- All seeded category IDs queryable: `SELECT id FROM taxonomy_categories WHERE taxonomy_id='sys_taxonomy_transaction_categories'` returns 10 rows.
</verification>

<success_criteria>

1. New migration directory `20260501000000_transactions_initial/` exists with
   `up.sql` + `down.sql`.
2. Three new tables + one VIEW visible in PG `information_schema` after
   migration runs.
3. 10 seeded category rows present with stable IDs.
4. `crates/storage-postgres/src/schema.rs` regenerated and contains
   `table! { transactions`, `table! { transaction_splits`,
   `table! { payee_category_memory`, `table! { transaction_csv_templates`
   blocks (4 new table! macros total).
5. `cargo check --workspace` green;
   `cargo test -p whaleit-storage-postgres --features integration` green.
   </success_criteria>

<output>
After completion, create `.planning/phases/04-transaction-core/04-01-SUMMARY.md` with the migration filenames written, the regenerated schema.rs lines added, and the test counts.
</output>
