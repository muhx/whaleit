---
phase: 01-codebase-health-rebrand
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02-PLAN
files_modified:
  - packages/ui/package.json
  - packages/addon-sdk/package.json
  - packages/addon-dev-tools/package.json
  - pnpm-workspace.yaml
  - apps/frontend/vite.config.ts
  - apps/frontend/package.json
  - apps/frontend/src/routes.tsx
  - apps/frontend/src/App.tsx
  - apps/frontend/src/pages/onboarding/onboarding-page.tsx
  - apps/frontend/src/pages/onboarding/onboarding-step1.tsx
  - apps/frontend/src/pages/onboarding/onboarding-step2.tsx
  - apps/frontend/src/pages/onboarding/onboarding-appearance.tsx
  - apps/frontend/src/pages/onboarding/onboarding-connect.tsx
autonomous: true
requirements:
  - BRAND-01
  - BRAND-04

must_haves:
  truths:
    - "All frontend imports use @whaleit/* scope instead of @whaleit/*"
    - "The whaleit-connect feature directory is renamed to connect"
    - "Onboarding screens show WhaleIt branding and tagline"
    - "No user-facing 'Whaleit' text remains in any frontend file"
  artifacts:
    - path: "packages/ui/package.json"
      provides: "Renamed package scope"
      contains: "@whaleit/ui"
    - path: "apps/frontend/src/features/connect/"
      provides: "Renamed feature directory"
    - path: "apps/frontend/src/pages/onboarding/onboarding-page.tsx"
      provides: "Rebranded onboarding"
      contains: "WhaleIt"
  key_links:
    - from: "apps/frontend/vite.config.ts"
      to: "packages/ui/src"
      via: "@whaleit/ui alias"
      pattern: "@whaleit/ui"
    - from: "apps/frontend/src/routes.tsx"
      to: "features/connect/"
      via: "import path"
      pattern: "features/connect"
---

<objective>
Rename npm package scope from @whaleit/* to @whaleit/*, rename the whaleit-connect feature directory, rebrand all onboarding screens, and sweep remaining frontend "Whaleit" text references.

Purpose: Per D-01 (package scope rename), D-02 (feature directory rename), D-15
(onboarding rebrand-only). Covers BRAND-01 (all user-facing text) and BRAND-04
(onboarding screens).

Output: All frontend imports using @whaleit/\* scope, renamed feature directory,
rebranded onboarding, zero user-facing "Whaleit" text in frontend. </objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-codebase-health-rebrand/01-CONTEXT.md
@.planning/phases/01-codebase-health-rebrand/01-RESEARCH.md

<interfaces>
<!-- Package scope rename targets: -->
<!-- packages/ui/package.json: "name": "@whaleit/ui" → "@whaleit/ui" -->
<!-- packages/addon-sdk/package.json: "name": "@whaleit/addon-sdk" → "@whaleit/addon-sdk" -->
<!-- packages/addon-dev-tools/package.json: "name": "@whaleit/addon-dev-tools" → "@whaleit/addon-dev-tools" -->
<!-- vite.config.ts line 41-42: alias "@whaleit/addon-sdk" and "@whaleit/ui" -->
<!-- -->
<!-- Feature directory rename: -->
<!-- apps/frontend/src/features/whaleit-connect/ → apps/frontend/src/features/connect/ -->
<!-- apps/frontend/src/pages/settings/whaleit-connect/ → apps/frontend/src/pages/settings/connect/ -->
<!-- -->
<!-- Import references (30+ files with @whaleit/ui or @whaleit/addon-sdk imports): -->
<!-- All import { ... } from "@whaleit/ui" → "@whaleit/ui" -->
<!-- All import { ... } from "@whaleit/ui/components/..." → "@whaleit/ui/components/..." -->
<!-- All import { ... } from "@whaleit/ui/lib/..." → "@whaleit/ui/lib/..." -->
<!-- All import type { ... } from "@whaleit/addon-sdk" → "@whaleit/addon-sdk" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Rename package scope, update vite aliases, and sweep all @wealthfolio imports</name>
  <files>packages/ui/package.json, packages/addon-sdk/package.json, packages/addon-dev-tools/package.json, apps/frontend/vite.config.ts, apps/frontend/package.json, apps/frontend/src/App.tsx, apps/frontend/src/context/privacy-context.tsx, apps/frontend/src/addons/type-bridge.ts, apps/frontend/src/addons/addons-runtime-context.ts, apps/frontend/src/addons/addons-core.ts</files>
  <read_first>
    - packages/ui/package.json (name field, bin, keywords, repository URLs)
    - packages/addon-sdk/package.json (name field, keywords, homepage, repository URLs)
    - packages/addon-dev-tools/package.json (name field, bin, keywords, repository URLs)
    - apps/frontend/vite.config.ts (alias configuration at lines 40-42)
    - apps/frontend/package.json (dependencies referencing @whaleit/*)
    - apps/frontend/src/App.tsx (imports from @whaleit/ui)
    - packages/ui/tsup.config.ts (line 18 references @whaleit/ui)
  </read_first>
  <action>Rename npm package scope from @whaleit/* to @whaleit/* per D-01 and update all import references. This is a systematic find-and-replace operation.

**Step 1: Package declarations**

- `packages/ui/package.json`: `"name": "@whaleit/ui"` → `"@whaleit/ui"`, update
  repository URLs from `muhx/whaleit` to `muhx/whaleit`, update keywords and
  homepage
- `packages/addon-sdk/package.json`: `"name": "@whaleit/addon-sdk"` →
  `"@whaleit/addon-sdk"`, same URL updates
- `packages/addon-dev-tools/package.json`: `"name": "@whaleit/addon-dev-tools"`
  → `"@whaleit/addon-dev-tools"`, same URL updates, update `"whaleit"` bin name
  to `"whaleit"`

**Step 2: Build configuration**

- `apps/frontend/vite.config.ts` line 41: `"@whaleit/addon-sdk"` alias →
  `"@whaleit/addon-sdk"`
- `apps/frontend/vite.config.ts` line 42: `"@whaleit/ui"` alias →
  `"@whaleit/ui"`
- `apps/frontend/package.json`: Update dependency references from `@whaleit/*`
  to `@whaleit/*`
- `packages/ui/tsup.config.ts` line 18: `"@whaleit/ui"` → `"@whaleit/ui"`
- `packages/addon-sdk/src/index.ts` line 2: JSDoc `@whaleit/addon-sdk` →
  `@whaleit/addon-sdk`
- `packages/addon-dev-tools`: Update any CLI references from `wealthfolio` to
  `whaleit`

**Step 3: All frontend imports** — Run a systematic find-and-replace across all
files in `apps/frontend/src/`:

- `"@whaleit/ui"` → `"@whaleit/ui"` (all variations: bare import,
  `/components/ui/`, `/lib/utils`, `/hooks/`)
- `"@whaleit/addon-sdk"` → `"@whaleit/addon-sdk"`

Use a tool like:
`find apps/frontend/src -name "*.ts" -o -name "*.tsx" | xargs sed -i '' 's/@wealthfolio\/ui/@whaleit\/ui/g; s/@wealthfolio\/addon-sdk/@whaleit\/addon-sdk/g'`
or equivalent surgical replacement per file.

**Step 4: Other frontend references**

- `apps/frontend/src/types/global.d.ts`: Update any `@wealthfolio` references
- `apps/frontend/src/addons/`: Update all addon-sdk imports
- `apps/frontend/public/manifest.json`: Update any `@wealthfolio` or "Whaleit"
  references

**Step 5: Run `pnpm install`** after all package.json changes to update lockfile
references.</action> <acceptance*criteria> - `grep -r "@whaleit/"
apps/frontend/src/ --include="*.ts" --include="_.tsx"`returns 0 results -`grep
-r "@whaleit/" packages/ --include="_.ts" --include="_.tsx"
--include="_.json"`returns 0 results (except possibly in comments/history) -`grep
'"@whaleit/ui"' packages/ui/package.json`returns match -`grep
'"@whaleit/addon-sdk"' packages/addon-sdk/package.json`returns match -`grep
'@whaleit/ui' apps/frontend/vite.config.ts`returns match -`pnpm
install`completes without errors -`pnpm build` passes </acceptance_criteria>
<verify> <automated>pnpm install && pnpm build</automated> </verify> <done>All
@whaleit/\* imports renamed to @whaleit/\_. pnpm install and build pass.</done>
</task>

<task type="auto">
  <name>task 2: Rename feature directory, rebrand onboarding, sweep remaining frontend text</name>
  <files>apps/frontend/src/features/connect/ (renamed from whaleit-connect/), apps/frontend/src/pages/settings/connect/ (renamed from whaleit-connect/), apps/frontend/src/routes.tsx, apps/frontend/src/pages/onboarding/onboarding-page.tsx, apps/frontend/src/pages/onboarding/onboarding-step1.tsx, apps/frontend/src/pages/onboarding/onboarding-step2.tsx, apps/frontend/src/pages/onboarding/onboarding-appearance.tsx, apps/frontend/src/pages/onboarding/onboarding-connect.tsx</files>
  <read_first>
    - apps/frontend/src/features/whaleit-connect/ (29 files — understand directory structure and content)
    - apps/frontend/src/pages/settings/whaleit-connect/ (settings page)
    - apps/frontend/src/routes.tsx (import paths at lines 9, 10, 39)
    - apps/frontend/src/App.tsx (WhaleitConnectProvider import at line 3)
    - apps/frontend/src/pages/onboarding/onboarding-page.tsx (current branding text)
    - apps/frontend/src/pages/onboarding/onboarding-step1.tsx (current branding text)
    - apps/frontend/src/pages/onboarding/onboarding-step2.tsx (current branding text)
    - apps/frontend/src/pages/onboarding/onboarding-appearance.tsx (current branding text)
    - apps/frontend/src/pages/onboarding/onboarding-connect.tsx (current branding text)
  </read_first>
  <action>Complete the frontend rebrand with directory rename, onboarding text update, and final text sweep per D-02 and D-15.

**Step 1: Rename feature directories** per D-02

- `mv apps/frontend/src/features/whaleit-connect/ apps/frontend/src/features/connect/`
- `mv apps/frontend/src/pages/settings/whaleit-connect/ apps/frontend/src/pages/settings/connect/`

**Step 2: Update import paths** that reference the renamed directories:

- `apps/frontend/src/routes.tsx` line 9:
  `"@/features/whaleit-connect/pages/auth-callback-page"` →
  `"@/features/connect/pages/auth-callback-page"`
- `apps/frontend/src/routes.tsx` line 10:
  `"@/features/whaleit-connect/pages/connect-page"` →
  `"@/features/connect/pages/connect-page"`
- `apps/frontend/src/routes.tsx` line 39:
  `"./pages/settings/whaleit-connect/connect-settings-page"` →
  `"./pages/settings/connect/connect-settings-page"`
- `apps/frontend/src/App.tsx` line 3: `"@/features/whaleit-connect"` →
  `"@/features/connect"`
- Search for any other imports referencing `whaleit-connect` in the codebase and
  update them all.

**Step 3: Rebrand onboarding screens** per D-15 (text/color/logo swaps only, no
flow redesign):

- In all 5 onboarding files, replace any "Whaleit" text with "WhaleIt"
- Add/update tagline "Your friendly finance companion" where appropriate
  (splash, welcome text)
- Update any logo references to point to the new whale logo
- Update any color references to use the new color tokens from Plan 01 (if they
  reference specific old colors like paper/base)
- Keep existing step structure completely unchanged — only swap branding text

**Step 4: Final frontend text sweep**

- Run `grep -rn "Whaleit" apps/frontend/src/ --include="*.ts" --include="*.tsx"`
  and replace ALL remaining occurrences:
  - "Whaleit" → "WhaleIt" in user-facing strings (window titles, labels,
    descriptions, comments visible in UI)
  - "whaleit-connect" in any remaining import paths → "connect"
  - "Whaleit Connect" → "WhaleIt Connect" in UI text
  - "Whaleit" in variable names that are user-visible (like provider names) →
    "WhaleIt"
- DO NOT change internal variable names that aren't user-visible (e.g.,
  `WhaleitConnectProvider` can be renamed to `ConnectProvider` for cleanliness,
  but it's not strictly required per D-03 since it's not user-visible)
- Pay special attention to:
  `features/connect/providers/whaleit-connect-provider.tsx` (24 "whaleit" refs),
  `components/connected-view.tsx` (20 refs), `pages/connect-page.tsx` (18
  refs)</action> <acceptance_criteria> -
  `test -d apps/frontend/src/features/connect/` succeeds (directory renamed) -
  `test -d apps/frontend/src/features/whaleit-connect/` fails (old directory
  gone) - `test -d apps/frontend/src/pages/settings/connect/` succeeds -
  `grep -rn "whaleit-connect" apps/frontend/src/ --include="*.ts" --include="*.tsx"`
  returns 0 -
  `grep -rn "Whaleit" apps/frontend/src/pages/onboarding/ --include="*.tsx"`
  returns 0 - Onboarding files contain "WhaleIt" text - `pnpm build` passes -
  `pnpm type-check` passes </acceptance_criteria> <verify> <automated>test -d
  apps/frontend/src/features/connect && pnpm type-check && pnpm build && grep
  -rn "Whaleit" apps/frontend/src/pages/onboarding/ --include="\*.tsx" | wc
  -l</automated> </verify> <done>Feature directories renamed. Onboarding
  rebranded. Zero user-facing "Whaleit" text in frontend. Build and type-check
  pass.</done> </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                  |
| --------------------------- | ------------------------------------------------------------ |
| Package registry → Build    | Renamed package scope may conflict with existing packages    |
| Import resolution → Runtime | Incorrect alias configuration causes runtime import failures |

## STRIDE Threat Register

| Threat ID | Category | Component     | Disposition | Mitigation Plan                                                          |
| --------- | -------- | ------------- | ----------- | ------------------------------------------------------------------------ |
| T-03-01   | S        | Package scope | accept      | @whaleit/\* scope unlikely to conflict; verified by pnpm install success |
| T-03-02   | D        | Import paths  | mitigate    | Build and type-check verification catches all broken imports             |

</threat_model>

<verification>
1. `pnpm install && pnpm build` — all packages resolve and build
2. `pnpm type-check` — no type errors from import changes
3. `test -d apps/frontend/src/features/connect` — directory renamed
4. `grep -c "Whaleit" apps/frontend/src/pages/onboarding/*.tsx` returns 0
5. `grep -r "@whaleit/" apps/frontend/src/` returns 0
</verification>

<success_criteria>

- All @whaleit/_ imports changed to @whaleit/_
- Feature directory renamed to features/connect/
- Onboarding shows WhaleIt branding and tagline
- Zero user-facing "Whaleit" in frontend TS/TSX files
- Build and type-check pass </success_criteria>

<output>
After completion, create `.planning/phases/01-codebase-health-rebrand/01-03-SUMMARY.md`
</output>
