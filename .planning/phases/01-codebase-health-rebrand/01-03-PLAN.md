---
phase: 01-codebase-health-rebrand
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02-PLAN
files_modified:
  - packages/ui/package.json
  - packages/addon-sdk/package.json
  - packages/addon-dev-tools/package.json
  - pnpm-workspace.yaml
  - apps/frontend/vite.config.ts
  - apps/frontend/package.json
  - apps/frontend/src/routes.tsx
  - apps/frontend/src/App.tsx
  - apps/frontend/src/pages/onboarding/onboarding-page.tsx
  - apps/frontend/src/pages/onboarding/onboarding-step1.tsx
  - apps/frontend/src/pages/onboarding/onboarding-step2.tsx
  - apps/frontend/src/pages/onboarding/onboarding-appearance.tsx
  - apps/frontend/src/pages/onboarding/onboarding-connect.tsx
autonomous: true
requirements:
  - BRAND-01
  - BRAND-04

must_haves:
  truths:
    - "All frontend imports use @whaleit/* scope instead of @wealthfolio/*"
    - "The wealthfolio-connect feature directory is renamed to connect"
    - "Onboarding screens show WhaleIt branding and tagline"
    - "No user-facing 'Wealthfolio' text remains in any frontend file"
  artifacts:
    - path: "packages/ui/package.json"
      provides: "Renamed package scope"
      contains: "@whaleit/ui"
    - path: "apps/frontend/src/features/connect/"
      provides: "Renamed feature directory"
    - path: "apps/frontend/src/pages/onboarding/onboarding-page.tsx"
      provides: "Rebranded onboarding"
      contains: "WhaleIt"
  key_links:
    - from: "apps/frontend/vite.config.ts"
      to: "packages/ui/src"
      via: "@whaleit/ui alias"
      pattern: "@whaleit/ui"
    - from: "apps/frontend/src/routes.tsx"
      to: "features/connect/"
      via: "import path"
      pattern: "features/connect"
---

<objective>
Rename npm package scope from @wealthfolio/* to @whaleit/*, rename the wealthfolio-connect feature directory, rebrand all onboarding screens, and sweep remaining frontend "Wealthfolio" text references.

Purpose: Per D-01 (package scope rename), D-02 (feature directory rename), D-15 (onboarding rebrand-only). Covers BRAND-01 (all user-facing text) and BRAND-04 (onboarding screens).

Output: All frontend imports using @whaleit/* scope, renamed feature directory, rebranded onboarding, zero user-facing "Wealthfolio" text in frontend.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-codebase-health-rebrand/01-CONTEXT.md
@.planning/phases/01-codebase-health-rebrand/01-RESEARCH.md

<interfaces>
<!-- Package scope rename targets: -->
<!-- packages/ui/package.json: "name": "@wealthfolio/ui" → "@whaleit/ui" -->
<!-- packages/addon-sdk/package.json: "name": "@wealthfolio/addon-sdk" → "@whaleit/addon-sdk" -->
<!-- packages/addon-dev-tools/package.json: "name": "@wealthfolio/addon-dev-tools" → "@whaleit/addon-dev-tools" -->
<!-- vite.config.ts line 41-42: alias "@wealthfolio/addon-sdk" and "@wealthfolio/ui" -->
<!-- -->
<!-- Feature directory rename: -->
<!-- apps/frontend/src/features/wealthfolio-connect/ → apps/frontend/src/features/connect/ -->
<!-- apps/frontend/src/pages/settings/wealthfolio-connect/ → apps/frontend/src/pages/settings/connect/ -->
<!-- -->
<!-- Import references (30+ files with @wealthfolio/ui or @wealthfolio/addon-sdk imports): -->
<!-- All import { ... } from "@wealthfolio/ui" → "@whaleit/ui" -->
<!-- All import { ... } from "@wealthfolio/ui/components/..." → "@whaleit/ui/components/..." -->
<!-- All import { ... } from "@wealthfolio/ui/lib/..." → "@whaleit/ui/lib/..." -->
<!-- All import type { ... } from "@wealthfolio/addon-sdk" → "@whaleit/addon-sdk" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Rename package scope, update vite aliases, and sweep all @wealthfolio imports</name>
  <files>packages/ui/package.json, packages/addon-sdk/package.json, packages/addon-dev-tools/package.json, apps/frontend/vite.config.ts, apps/frontend/package.json, apps/frontend/src/App.tsx, apps/frontend/src/context/privacy-context.tsx, apps/frontend/src/addons/type-bridge.ts, apps/frontend/src/addons/addons-runtime-context.ts, apps/frontend/src/addons/addons-core.ts</files>
  <read_first>
    - packages/ui/package.json (name field, bin, keywords, repository URLs)
    - packages/addon-sdk/package.json (name field, keywords, homepage, repository URLs)
    - packages/addon-dev-tools/package.json (name field, bin, keywords, repository URLs)
    - apps/frontend/vite.config.ts (alias configuration at lines 40-42)
    - apps/frontend/package.json (dependencies referencing @wealthfolio/*)
    - apps/frontend/src/App.tsx (imports from @wealthfolio/ui)
    - packages/ui/tsup.config.ts (line 18 references @wealthfolio/ui)
  </read_first>
  <action>Rename npm package scope from @wealthfolio/* to @whaleit/* per D-01 and update all import references. This is a systematic find-and-replace operation.

  **Step 1: Package declarations**
  - `packages/ui/package.json`: `"name": "@wealthfolio/ui"` → `"@whaleit/ui"`, update repository URLs from `afadil/wealthfolio` to `muhx/whaleit`, update keywords and homepage
  - `packages/addon-sdk/package.json`: `"name": "@wealthfolio/addon-sdk"` → `"@whaleit/addon-sdk"`, same URL updates
  - `packages/addon-dev-tools/package.json`: `"name": "@wealthfolio/addon-dev-tools"` → `"@whaleit/addon-dev-tools"`, same URL updates, update `"wealthfolio"` bin name to `"whaleit"`

  **Step 2: Build configuration**
  - `apps/frontend/vite.config.ts` line 41: `"@wealthfolio/addon-sdk"` alias → `"@whaleit/addon-sdk"`
  - `apps/frontend/vite.config.ts` line 42: `"@wealthfolio/ui"` alias → `"@whaleit/ui"`
  - `apps/frontend/package.json`: Update dependency references from `@wealthfolio/*` to `@whaleit/*`
  - `packages/ui/tsup.config.ts` line 18: `"@wealthfolio/ui"` → `"@whaleit/ui"`
  - `packages/addon-sdk/src/index.ts` line 2: JSDoc `@wealthfolio/addon-sdk` → `@whaleit/addon-sdk`
  - `packages/addon-dev-tools`: Update any CLI references from `wealthfolio` to `whaleit`

  **Step 3: All frontend imports** — Run a systematic find-and-replace across all files in `apps/frontend/src/`:
  - `"@wealthfolio/ui"` → `"@whaleit/ui"` (all variations: bare import, `/components/ui/`, `/lib/utils`, `/hooks/`)
  - `"@wealthfolio/addon-sdk"` → `"@whaleit/addon-sdk"`

  Use a tool like: `find apps/frontend/src -name "*.ts" -o -name "*.tsx" | xargs sed -i '' 's/@wealthfolio\/ui/@whaleit\/ui/g; s/@wealthfolio\/addon-sdk/@whaleit\/addon-sdk/g'` or equivalent surgical replacement per file.

  **Step 4: Other frontend references**
  - `apps/frontend/src/types/global.d.ts`: Update any `@wealthfolio` references
  - `apps/frontend/src/addons/`: Update all addon-sdk imports
  - `apps/frontend/public/manifest.json`: Update any `@wealthfolio` or "Wealthfolio" references

  **Step 5: Run `pnpm install`** after all package.json changes to update lockfile references.</action>
  <acceptance_criteria>
    - `grep -r "@wealthfolio/" apps/frontend/src/ --include="*.ts" --include="*.tsx"` returns 0 results
    - `grep -r "@wealthfolio/" packages/ --include="*.ts" --include="*.tsx" --include="*.json"` returns 0 results (except possibly in comments/history)
    - `grep '"@whaleit/ui"' packages/ui/package.json` returns match
    - `grep '"@whaleit/addon-sdk"' packages/addon-sdk/package.json` returns match
    - `grep '@whaleit/ui' apps/frontend/vite.config.ts` returns match
    - `pnpm install` completes without errors
    - `pnpm build` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm install && pnpm build</automated>
  </verify>
  <done>All @wealthfolio/* imports renamed to @whaleit/*. pnpm install and build pass.</done>
</task>

<task type="auto">
  <name>task 2: Rename feature directory, rebrand onboarding, sweep remaining frontend text</name>
  <files>apps/frontend/src/features/connect/ (renamed from wealthfolio-connect/), apps/frontend/src/pages/settings/connect/ (renamed from wealthfolio-connect/), apps/frontend/src/routes.tsx, apps/frontend/src/pages/onboarding/onboarding-page.tsx, apps/frontend/src/pages/onboarding/onboarding-step1.tsx, apps/frontend/src/pages/onboarding/onboarding-step2.tsx, apps/frontend/src/pages/onboarding/onboarding-appearance.tsx, apps/frontend/src/pages/onboarding/onboarding-connect.tsx</files>
  <read_first>
    - apps/frontend/src/features/wealthfolio-connect/ (29 files — understand directory structure and content)
    - apps/frontend/src/pages/settings/wealthfolio-connect/ (settings page)
    - apps/frontend/src/routes.tsx (import paths at lines 9, 10, 39)
    - apps/frontend/src/App.tsx (WealthfolioConnectProvider import at line 3)
    - apps/frontend/src/pages/onboarding/onboarding-page.tsx (current branding text)
    - apps/frontend/src/pages/onboarding/onboarding-step1.tsx (current branding text)
    - apps/frontend/src/pages/onboarding/onboarding-step2.tsx (current branding text)
    - apps/frontend/src/pages/onboarding/onboarding-appearance.tsx (current branding text)
    - apps/frontend/src/pages/onboarding/onboarding-connect.tsx (current branding text)
  </read_first>
  <action>Complete the frontend rebrand with directory rename, onboarding text update, and final text sweep per D-02 and D-15.

  **Step 1: Rename feature directories** per D-02
  - `mv apps/frontend/src/features/wealthfolio-connect/ apps/frontend/src/features/connect/`
  - `mv apps/frontend/src/pages/settings/wealthfolio-connect/ apps/frontend/src/pages/settings/connect/`

  **Step 2: Update import paths** that reference the renamed directories:
  - `apps/frontend/src/routes.tsx` line 9: `"@/features/wealthfolio-connect/pages/auth-callback-page"` → `"@/features/connect/pages/auth-callback-page"`
  - `apps/frontend/src/routes.tsx` line 10: `"@/features/wealthfolio-connect/pages/connect-page"` → `"@/features/connect/pages/connect-page"`
  - `apps/frontend/src/routes.tsx` line 39: `"./pages/settings/wealthfolio-connect/connect-settings-page"` → `"./pages/settings/connect/connect-settings-page"`
  - `apps/frontend/src/App.tsx` line 3: `"@/features/wealthfolio-connect"` → `"@/features/connect"`
  - Search for any other imports referencing `wealthfolio-connect` in the codebase and update them all.

  **Step 3: Rebrand onboarding screens** per D-15 (text/color/logo swaps only, no flow redesign):
  - In all 5 onboarding files, replace any "Wealthfolio" text with "WhaleIt"
  - Add/update tagline "Your friendly finance companion" where appropriate (splash, welcome text)
  - Update any logo references to point to the new whale logo
  - Update any color references to use the new color tokens from Plan 01 (if they reference specific old colors like paper/base)
  - Keep existing step structure completely unchanged — only swap branding text

  **Step 4: Final frontend text sweep**
  - Run `grep -rn "Wealthfolio" apps/frontend/src/ --include="*.ts" --include="*.tsx"` and replace ALL remaining occurrences:
    - "Wealthfolio" → "WhaleIt" in user-facing strings (window titles, labels, descriptions, comments visible in UI)
    - "wealthfolio-connect" in any remaining import paths → "connect"
    - "Wealthfolio Connect" → "WhaleIt Connect" in UI text
    - "Wealthfolio" in variable names that are user-visible (like provider names) → "WhaleIt"
  - DO NOT change internal variable names that aren't user-visible (e.g., `WealthfolioConnectProvider` can be renamed to `ConnectProvider` for cleanliness, but it's not strictly required per D-03 since it's not user-visible)
  - Pay special attention to: `features/connect/providers/wealthfolio-connect-provider.tsx` (24 "wealthfolio" refs), `components/connected-view.tsx` (20 refs), `pages/connect-page.tsx` (18 refs)</action>
  <acceptance_criteria>
    - `test -d apps/frontend/src/features/connect/` succeeds (directory renamed)
    - `test -d apps/frontend/src/features/wealthfolio-connect/` fails (old directory gone)
    - `test -d apps/frontend/src/pages/settings/connect/` succeeds
    - `grep -rn "wealthfolio-connect" apps/frontend/src/ --include="*.ts" --include="*.tsx"` returns 0
    - `grep -rn "Wealthfolio" apps/frontend/src/pages/onboarding/ --include="*.tsx"` returns 0
    - Onboarding files contain "WhaleIt" text
    - `pnpm build` passes
    - `pnpm type-check` passes
  </acceptance_criteria>
  <verify>
    <automated>test -d apps/frontend/src/features/connect && pnpm type-check && pnpm build && grep -rn "Wealthfolio" apps/frontend/src/pages/onboarding/ --include="*.tsx" | wc -l</automated>
  </verify>
  <done>Feature directories renamed. Onboarding rebranded. Zero user-facing "Wealthfolio" text in frontend. Build and type-check pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package registry → Build | Renamed package scope may conflict with existing packages |
| Import resolution → Runtime | Incorrect alias configuration causes runtime import failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | S | Package scope | accept | @whaleit/* scope unlikely to conflict; verified by pnpm install success |
| T-03-02 | D | Import paths | mitigate | Build and type-check verification catches all broken imports |
</threat_model>

<verification>
1. `pnpm install && pnpm build` — all packages resolve and build
2. `pnpm type-check` — no type errors from import changes
3. `test -d apps/frontend/src/features/connect` — directory renamed
4. `grep -c "Wealthfolio" apps/frontend/src/pages/onboarding/*.tsx` returns 0
5. `grep -r "@wealthfolio/" apps/frontend/src/` returns 0
</verification>

<success_criteria>
- All @wealthfolio/* imports changed to @whaleit/*
- Feature directory renamed to features/connect/
- Onboarding shows WhaleIt branding and tagline
- Zero user-facing "Wealthfolio" in frontend TS/TSX files
- Build and type-check pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-codebase-health-rebrand/01-03-SUMMARY.md`
</output>
