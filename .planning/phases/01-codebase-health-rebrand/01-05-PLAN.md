---
phase: 01-codebase-health-rebrand
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/index.html
  - apps/frontend/public/manifest.json
  - apps/frontend/src/pages/onboarding/onboarding-page.tsx
autonomous: true
gap_closure: true
requirements:
  - BRAND-01
  - BRAND-04

must_haves:
  truths:
    - "Browser tab title displays 'WhaleIt' not 'Whaleit'"
    - "PWA manifest shows 'WhaleIt' as name and short_name"
    - "Onboarding welcome screen displays 'Your friendly finance companion'
      tagline"
  artifacts:
    - path: "apps/frontend/index.html"
      provides: "Browser tab title"
      contains: "<title>WhaleIt</title>"
    - path: "apps/frontend/public/manifest.json"
      provides: "PWA manifest identity"
      contains: '"name": "WhaleIt"'
    - path: "apps/frontend/src/pages/onboarding/onboarding-page.tsx"
      provides: "Onboarding welcome with tagline"
      contains: "Your friendly finance companion"
  key_links:
    - from: "apps/frontend/index.html"
      to: "Browser tab"
      via: "<title> element"
      pattern: "<title>WhaleIt</title>"
    - from: "apps/frontend/public/manifest.json"
      to: "PWA install prompt"
      via: "name/short_name fields"
      pattern: '"name": "WhaleIt"'
    - from: "apps/frontend/src/pages/onboarding/onboarding-page.tsx"
      to: "User onboarding experience"
      via: "Welcome text rendering"
      pattern: "Your friendly finance companion"
---

<objective>
Close 2 blocking verification gaps from Phase 01: missed "Whaleit" → "WhaleIt" renames in browser-facing files, and missing tagline in onboarding screens.

Purpose: Complete the WhaleIt rebrand so every user-facing surface shows the new
identity. Output: Updated index.html, manifest.json, and onboarding-page.tsx
with all gaps closed. </objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-codebase-health-rebrand/01-CONTEXT.md
@.planning/phases/01-codebase-health-rebrand/01-VERIFICATION.md
@.planning/phases/01-codebase-health-rebrand/01-01-SUMMARY.md
@.planning/phases/01-codebase-health-rebrand/01-03-SUMMARY.md

<interfaces>
<!-- Current state of files being modified (read by planner from codebase) -->

From apps/frontend/index.html (line 15):

```html
<title>Whaleit</title>
```

From apps/frontend/public/manifest.json (lines 2-3):

```json
"name": "Whaleit",
"short_name": "Whaleit",
```

From apps/frontend/src/pages/onboarding/onboarding-page.tsx (lines 56-63):

```tsx
<header className="flex-none px-4 pt-8 sm:px-6 sm:pt-12">
  <div className="flex flex-col items-center">
    {/* Logo */}
    <img
      alt="WhaleIt"
      className="mb-3 h-16 w-16 sm:h-20 sm:w-20"
      src="/logo-vantage.png"
    />
```

No tagline text exists anywhere in this file or onboarding-step1.tsx.
</interfaces> </context>

<tasks>

<task type="auto">
  <name>task 1: Fix remaining Whaleit renames in browser-facing files</name>
  <files>apps/frontend/index.html, apps/frontend/public/manifest.json</files>
  <read_first>
    - apps/frontend/index.html (see current title on line 15)
    - apps/frontend/public/manifest.json (see current name/short_name on lines 2-3)
  </read_first>
  <action>
    Per VERIFICATION.md gap #1 — two user-facing files still show "Whaleit":

    1. In `apps/frontend/index.html` line 15:
       Change `<title>Whaleit</title>` to `<title>WhaleIt</title>`

    2. In `apps/frontend/public/manifest.json` lines 2-3:
       Change `"name": "Whaleit"` to `"name": "WhaleIt"`
       Change `"short_name": "Whaleit"` to `"short_name": "WhaleIt"`

    These are simple text replacements — no structural changes needed. Do NOT change anything else in these files.

  </action>
  <verify>
    <automated>grep -c "Whaleit" apps/frontend/index.html apps/frontend/public/manifest.json; echo "Expected: 0 matches in both files"</automated>
  </verify>
  <done>
    - `apps/frontend/index.html` contains `<title>WhaleIt</title>` (exact string)
    - `apps/frontend/public/manifest.json` contains `"name": "WhaleIt"` and `"short_name": "WhaleIt"` (exact strings)
    - Zero occurrences of "Whaleit" remain in either file
  </done>
</task>

<task type="auto">
  <name>task 2: Add WhaleIt tagline to onboarding welcome screen</name>
  <files>apps/frontend/src/pages/onboarding/onboarding-page.tsx</files>
  <read_first>
    - apps/frontend/src/pages/onboarding/onboarding-page.tsx (see current header structure lines 56-80)
  </read_first>
  <action>
    Per VERIFICATION.md gap #2 — onboarding screens show WhaleIt logo but the tagline "Your friendly finance companion" is absent.

    In `apps/frontend/src/pages/onboarding/onboarding-page.tsx`, add the tagline text directly below the logo `<img>` element (line 63) and above the progress indicators `<div>` (line 66).

    Insert between the closing `/>` of the `<img>` tag and the `{/* Progress indicators */}` comment:

    ```tsx
    {/* Tagline */}
    <p className="text-muted-foreground mt-2 text-sm sm:text-base">
      Your friendly finance companion
    </p>
    ```

    This places the tagline in the header area where the logo is shown, visible across all onboarding steps (the header is always rendered). The `text-muted-foreground` class matches the existing subtitle style used in `onboarding-step1.tsx`. The responsive sizing (`text-sm sm:text-base`) follows the same pattern as other text in the file.

    Do NOT change any other part of the file.

  </action>
  <verify>
    <automated>grep -c "Your friendly finance companion" apps/frontend/src/pages/onboarding/onboarding-page.tsx; echo "Expected: 1 or more"</automated>
  </verify>
  <done>
    - `onboarding-page.tsx` contains the text "Your friendly finance companion" in the header section
    - The tagline appears between the logo image and the progress indicators
    - The tagline uses `text-muted-foreground` class consistent with existing styling
  </done>
</task>

</tasks>

<verification>
1. `grep -c "Whaleit" apps/frontend/index.html` returns 0
2. `grep "Whaleit" apps/frontend/public/manifest.json` returns no output
3. `grep "Your friendly finance companion" apps/frontend/src/pages/onboarding/onboarding-page.tsx` returns a match
4. `pnpm type-check` passes (no type errors introduced)
5. `pnpm build` passes (frontend builds successfully)
</verification>

<success_criteria>

- Zero "Whaleit" references in `index.html` and `manifest.json`
- Tagline "Your friendly finance companion" visible in onboarding header across
  all steps
- TypeScript type-check passes
- Frontend build succeeds
- All 14 original must-have truths from Phase 01 now achievable
  </success_criteria>

<output>
After completion, create `.planning/phases/01-codebase-health-rebrand/01-05-SUMMARY.md`
</output>
