---
phase: 01-codebase-health-rebrand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/globals.css
  - apps/tauri/icons/icon.icns
  - apps/tauri/icons/icon.ico
  - apps/tauri/icons/icon.png
  - apps/tauri/icons/32x32.png
  - apps/tauri/icons/64x64.png
  - apps/tauri/icons/128x128.png
  - apps/tauri/icons/128x128@2x.png
  - apps/frontend/public/logo.svg
  - apps/frontend/public/logo.png
  - apps/frontend/public/splashscreen.png
  - apps/frontend/public/app-icon-192.png
  - apps/frontend/public/app-icon-512.png
  - apps/frontend/public/apple-touch-icon.png
autonomous: false
requirements:
  - BRAND-02
  - BRAND-03

must_haves:
  truths:
    - "The app displays a new color palette reflecting WhaleIt's friendly companion branding"
    - "The app icon shows a whale in soft illustration style across all platforms (macOS, Windows, Linux, iOS, web)"
    - "The splash/loading screen shows the WhaleIt logo and tagline"
  artifacts:
    - path: "apps/frontend/src/globals.css"
      provides: "New WhaleIt color palette via @theme block"
      contains: "@theme"
    - path: "apps/tauri/icons/icon.icns"
      provides: "macOS icon bundle"
    - path: "apps/tauri/icons/icon.ico"
      provides: "Windows icon"
    - path: "apps/frontend/public/logo.svg"
      provides: "SVG whale logo for web"
    - path: "apps/frontend/public/splashscreen.png"
      provides: "Splash screen with WhaleIt branding"
  key_links:
    - from: "apps/frontend/src/globals.css"
      to: "all components using Tailwind color tokens"
      via: "CSS custom properties in @theme"
      pattern: "var\\(--color-"
---

<objective>
Create the WhaleIt visual identity: fresh color palette and whale icon assets.

Purpose: Establish the new brand's visual foundation so all subsequent rebrand tasks (onboarding, UI text) can reference the new palette and icons. Per D-08 (soft illustration style), D-09 (fresh palette), D-10 (brand presence in icon/splash/header), D-11 (AI-generated placeholder icons).

Output: Updated globals.css with new color tokens, placeholder whale icon in all required formats, updated splash screen and logo files.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-codebase-health-rebrand/01-CONTEXT.md
@.planning/phases/01-codebase-health-rebrand/01-RESEARCH.md

<interfaces>
<!-- Current globals.css @theme block has these token families the executor must replace: -->
<!-- --font-sans, --font-serif, --font-mono -->
<!-- --color-base-50 through --color-base-950 (warm paper tones) -->
<!-- --color-paper, --color-black -->
<!-- --color-red-*, --color-green-*, --color-blue-*, --color-amber-* (semantic colors) -->
<!-- Many scales disabled: lime, emerald, teal, sky, indigo, violet, fuchsia, pink, rose, slate, zinc, neutral, stone -->
<!-- Total @theme block: ~304 lines -->
<!-- Dark mode overrides: .dark class at line 477 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Design and implement new WhaleIt color palette</name>
  <files>apps/frontend/src/globals.css</files>
  <read_first>
    - apps/frontend/src/globals.css (current @theme block, dark mode overrides, all color tokens)
  </read_first>
  <action>Replace the `@theme {}` block (starting at line 59) with a new WhaleIt color palette per D-09. Design a friendly, approachable color system that conveys "your friendly finance companion" — think ocean/warm tones, not corporate blue or clinical gray. Specific requirements:

  1. Replace `--color-base-*` scale (currently warm paper tones) with a new WhaleIt-specific scale. Use ocean-inspired warm tones: a soft teal-to-slate progression that feels approachable and modern.
  2. Keep `--color-paper` but update to a light, warm white (e.g., `#f8fafb` or similar).
  3. Keep `--color-black` updated to match the new palette (e.g., `#1a2332` or similar deep ocean tone).
  4. Keep all semantic color scales (red, green, blue, amber) but adjust slightly if needed to harmonize with the new base palette.
  5. Keep `--font-sans`, `--font-serif`, `--font-mono` unchanged.
  6. Keep disabled scale declarations (lime, emerald, etc. set to `initial`).
  7. Update the `.dark` theme section (line 477+) to provide dark mode variants of the new palette.
  8. Do NOT change any utility definitions, custom variants, or base layer styles — only the @theme block color tokens and dark mode color overrides.

  The new palette should feel warm, trustworthy, and friendly — fitting for a personal finance companion app called WhaleIt.</action>
  <acceptance_criteria>
    - `globals.css` contains `@theme {}` block with new `--color-base-*` values (not the old warm paper tones `#f2f0e5`, `#e6e4d9`, etc.)
    - `--color-paper` value is different from old `#fffcf0`
    - `--color-black` value is different from old `#100f0f`
    - `--font-sans` still contains "Inter"
    - `.dark` section exists with dark mode color overrides
    - No utility definitions were modified
  </acceptance_criteria>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>New color palette in globals.css @theme block reflecting WhaleIt identity. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 1.5: Verify new color palette visual appearance</name>
  <files>apps/frontend/src/globals.css</files>
  <action>New WhaleIt color palette in globals.css. The build compiles but visual appearance needs human confirmation. Run `pnpm run dev:web`, open in browser, verify: (1) new color palette looks friendly and approachable, (2) dark mode works correctly, (3) no broken colors or missing tokens.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Human confirms color palette looks correct in both light and dark modes.</done>
</task>

<task type="auto">
  <name>task 2: Generate placeholder whale icon and splash assets</name>
  <files>apps/tauri/icons/icon.png, apps/tauri/icons/icon.icns, apps/tauri/icons/icon.ico, apps/tauri/icons/32x32.png, apps/tauri/icons/64x64.png, apps/tauri/icons/128x128.png, apps/tauri/icons/128x128@2x.png, apps/frontend/public/logo.svg, apps/frontend/public/logo.png, apps/frontend/public/splashscreen.png, apps/frontend/public/app-icon-192.png, apps/frontend/public/app-icon-512.png, apps/frontend/public/apple-touch-icon.png</files>
  <read_first>
    - apps/tauri/icons/ (existing icon files to understand format requirements)
    - apps/tauri/tauri.conf.json lines for icon configuration
    - apps/frontend/public/manifest.json (web manifest icon references)
  </read_first>
  <action>Generate placeholder whale icon assets per D-08 (soft illustration style) and D-11 (AI-generated placeholder). The icon should be a friendly whale in soft illustration style — warm, approachable, Notion-meets-Duolingo feel per D-08. Use the new color palette from task 1.

  1. Create a base whale icon SVG in `apps/frontend/public/logo.svg` — simple, friendly whale silhouette using the new WhaleIt color palette. The whale should feel approachable and warm.
  2. Export PNG versions at all required sizes from the SVG:
     - `apps/tauri/icons/icon.png` (1024x1024 source)
     - `apps/tauri/icons/32x32.png`
     - `apps/tauri/icons/64x64.png`
     - `apps/tauri/icons/128x128.png`
     - `apps/tauri/icons/128x128@2x.png` (256x256)
     - `apps/frontend/public/logo.png`
     - `apps/frontend/public/app-icon-192.png`
     - `apps/frontend/public/app-icon-512.png`
     - `apps/frontend/public/apple-touch-icon.png` (180x180)
  3. Create `apps/tauri/icons/icon.icns` using `iconutil` (macOS) from the PNG set.
  4. Create `apps/tauri/icons/icon.ico` using ImageMagick or similar from the PNG set.
  5. Create `apps/frontend/public/splashscreen.png` — a simple splash screen with the whale logo centered and "WhaleIt" text below with tagline "Your friendly finance companion". Use the new color palette background.

  Use `npx sharp-cli` or similar for PNG generation. Use `iconutil -c icns` for macOS. Use ImageMagick `convert` for .ico. If tools aren't available, create a simple SVG-based placeholder and document the tool requirements for professional replacement.</action>
  <acceptance_criteria>
    - `apps/tauri/icons/icon.png` exists and is a valid PNG image
    - `apps/tauri/icons/icon.icns` exists and is a valid macOS icon bundle
    - `apps/tauri/icons/icon.ico` exists and is a valid Windows icon
    - `apps/tauri/icons/32x32.png`, `64x64.png`, `128x128.png`, `128x128@2x.png` all exist
    - `apps/frontend/public/logo.svg` exists and contains whale illustration SVG
    - `apps/frontend/public/splashscreen.png` exists with whale logo + "WhaleIt" text
    - `apps/frontend/public/app-icon-192.png` and `app-icon-512.png` exist
    - All icon files are valid image files (not 0-byte)
  </acceptance_criteria>
  <verify>
    <automated>file apps/tauri/icons/icon.icns apps/tauri/icons/icon.ico apps/tauri/icons/icon.png apps/frontend/public/logo.svg apps/frontend/public/splashscreen.png | grep -c "cannot open"</automated>
  </verify>
  <done>All required icon and splash assets exist in correct formats for macOS, Windows, Linux, iOS, and web.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Icon assets → Build | Generated icon files included in app bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S | Icon assets | accept | Placeholder icons for dev use only — professional design swaps in later |
| T-01-02 | T | globals.css | accept | Color tokens are cosmetic only, no security impact |
</threat_model>

<verification>
1. `pnpm build` — build passes with new globals.css
2. All icon files exist and are valid image formats
3. Splash screen includes "WhaleIt" text and whale visual
</verification>

<success_criteria>
- globals.css has new color palette (not warm paper tones)
- All 11+ icon/asset files exist in required formats
- Build passes with no errors
- Visual appearance approved by user (checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/01-codebase-health-rebrand/01-01-SUMMARY.md`
</output>
