---
phase: 01-codebase-health-rebrand
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/adapters/web/core.ts
  - apps/frontend/src/lib/types.ts
autonomous: true
requirements:
  - BRAND-01

must_haves:
  truths:
    - "Web adapter switch delegates to domain-specific module files instead of
      inline handlers"
    - "Types are organized into domain-specific files with barrel re-export"
    - "ActivityLegacy deprecated type is removed"
    - "All existing imports from @/lib/types and @/adapters/web/core continue to
      work unchanged"
  artifacts:
    - path: "apps/frontend/src/adapters/web/core.ts"
      provides: "Slim dispatcher that delegates to domain modules"
      min_lines: 50
    - path: "apps/frontend/src/lib/types.ts"
      provides: "Barrel re-export file for all domain type files"
    - path: "apps/frontend/src/lib/types/account.ts"
      provides: "Account-related type definitions"
      contains: "Account"
    - path: "apps/frontend/src/lib/types/activity.ts"
      provides: "Activity-related type definitions"
      contains: "Activity"
  key_links:
    - from: "apps/frontend/src/lib/types.ts"
      to: "apps/frontend/src/lib/types/*.ts"
      via: "barrel re-export"
      pattern: "export \\* from"
    - from: "apps/frontend/src/adapters/web/core.ts"
      to: "apps/frontend/src/adapters/web/modules/*.ts"
      via: "module imports and delegation"
      pattern: "import.*from.*modules"
---

<objective>
Split the monolithic web adapter (1,394 lines, 184-case switch) into domain modules and split the God types file (1,929 lines) into domain-based files with barrel re-exports.

Purpose: Per D-12 (web adapter modular split), D-13 (types.ts domain-based
split), D-14 (remove ActivityLegacy). These are the two critical codebase health
bottlenecks identified in STATE.md that block feature expansion in Phases 2-12.

Output: Modular web adapter with domain-specific handler modules, types
organized by domain with backward-compatible barrel re-exports. </objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-codebase-health-rebrand/01-CONTEXT.md
@.planning/phases/01-codebase-health-rebrand/01-RESEARCH.md

<interfaces>
<!-- Web adapter: apps/frontend/src/adapters/web/core.ts -->
<!-- Exports: invoke<T>(command, payload), COMMANDS map, API_PREFIX, EVENTS_ENDPOINT, AI_CHAT_STREAM_ENDPOINT, isDesktop, isWeb, logger -->
<!-- 184 switch cases mapping command names to HTTP API calls -->
<!-- Shared adapter modules in apps/frontend/src/adapters/shared/ provide canonical domain grouping: -->
<!--   accounts, activities, ai-providers, ai-threads, alternative-assets, connect, -->
<!--   contribution-limits, custom-provider, exchange-rates, goals, health, market-data, -->
<!--   platform, portfolio, secrets, taxonomies -->
<!-- -->
<!-- Types: apps/frontend/src/lib/types.ts -->
<!-- 154 exported interfaces/types across ~15 domains -->
<!-- 1 deprecated type: ActivityLegacy (line 70) — zero external usages -->
<!-- Barrel re-export preserves backward compatibility -->
<!-- All other files import as: import { X } from "@/lib/types" or "@/lib/types" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Split web adapter into domain modules</name>
  <files>apps/frontend/src/adapters/web/core.ts, apps/frontend/src/adapters/web/modules/accounts.ts, apps/frontend/src/adapters/web/modules/activities.ts, apps/frontend/src/adapters/web/modules/holdings.ts, apps/frontend/src/adapters/web/modules/portfolio.ts, apps/frontend/src/adapters/web/modules/goals.ts, apps/frontend/src/adapters/web/modules/exchange-rates.ts, apps/frontend/src/adapters/web/modules/ai.ts, apps/frontend/src/adapters/web/modules/connect.ts, apps/frontend/src/adapters/web/modules/market-data.ts, apps/frontend/src/adapters/web/modules/taxonomies.ts, apps/frontend/src/adapters/web/modules/health.ts, apps/frontend/src/adapters/web/modules/device-sync.ts, apps/frontend/src/adapters/web/modules/settings.ts, apps/frontend/src/adapters/web/modules/secrets.ts, apps/frontend/src/adapters/web/modules/assets.ts, apps/frontend/src/adapters/web/modules/addons.ts, apps/frontend/src/adapters/web/modules/utilities.ts</files>
  <read_first>
    - apps/frontend/src/adapters/web/core.ts (FULL file — understand all 184 cases and helper functions)
    - apps/frontend/src/adapters/shared/ (16 module files — canonical grouping pattern to follow)
    - apps/frontend/src/adapters/types.ts (adapter type definitions)
  </read_first>
  <action>Split the 1,394-line web adapter into domain modules per D-12, following the same grouping as `apps/frontend/src/adapters/shared/`.

**Architecture:**

- Keep `core.ts` as the slim dispatcher: exports `invoke<T>()`, `COMMANDS`,
  `API_PREFIX`, `EVENTS_ENDPOINT`, `AI_CHAT_STREAM_ENDPOINT`, `isDesktop`,
  `isWeb`, `logger`
- Create `apps/frontend/src/adapters/web/modules/` directory
- Each module file exports handler functions for its domain's switch cases
- The switch statement in `invoke()` stays but delegates to module functions
  instead of inline logic

**Module files to create** (following shared adapter grouping):

1. `modules/accounts.ts` — get/create/update/delete_account
2. `modules/activities.ts` — search/create/update/delete/save activities + all
   import/check/mapping/template commands
3. `modules/holdings.ts` — get_holding(s), snapshots, manual_holdings,
   import_holdings_csv, check_holdings_import, alternative_holdings
4. `modules/portfolio.ts` — allocations, recalculate, update, net_worth,
   net_worth_history, valuations
5. `modules/performance.ts` — simple_performance, history, summary,
   income_summary (merged into portfolio or separate)
6. `modules/goals.ts` — CRUD + allocations
7. `modules/exchange-rates.ts` — latest, add, update, delete
8. `modules/ai.ts` — providers, threads, messages, models, tags, chat stream +
   update_tool_result
9. `modules/connect.ts` — sync, ingest, broker accounts/connections,
   subscription plans, sync session
10. `modules/market-data.ts` — quotes import/latest/history/check/sync,
    update_quote/mode
11. `modules/taxonomies.ts` — CRUD, categories, assignments, import/export,
    migration
12. `modules/health.ts` — status, config, checks, dismiss, restore, fix
13. `modules/device-sync.ts` — all 18 pairing/device/E2EE/team commands
14. `modules/settings.ts` — get/update settings, auto-update check
15. `modules/secrets.ts` — get/set/delete secrets
16. `modules/assets.ts` — create/alternative assets, symbol search/resolve,
    asset profile
17. `modules/contribution-limits.ts` — create/update, calculate deposits
18. `modules/custom-providers.ts` — create/update/test, set default
19. `modules/addons.ts` — install/uninstall/toggle/ratings/staging/load/check
    updates
20. `modules/liabilities.ts` — link/unlink
21. `modules/utilities.ts` — backup, restore, app info, check update,
    get_platforms

**Implementation pattern per module:**

```typescript
// modules/accounts.ts
import { API_PREFIX } from "../core";

export function handleGetAccounts(url: string): string {
  return url; // GET /accounts — no body, no URL modification needed
}
// ... other account handlers
```

**Refactored core.ts switch:**

```typescript
import * as accountHandlers from "./modules/accounts";
// ... etc

switch (command) {
  case "get_accounts": {
    url = accountHandlers.handleGetAccounts(url);
    break;
  }
  // ... delegates to modules
}
```

**Important:** Preserve the exact same HTTP method, URL, and body construction
logic. This is a structural refactor, not a behavior change. Every one of the
184 cases must map correctly.

**Helper functions** that are currently defined inline in core.ts (like
`fromBase64`) should be moved to a shared `helpers.ts` or kept in core.ts if
used by multiple modules.</action> <acceptance_criteria> -
`apps/frontend/src/adapters/web/modules/` directory exists with 18+ module
files - `apps/frontend/src/adapters/web/core.ts` still exports `invoke`,
`COMMANDS`, `API_PREFIX`, `EVENTS_ENDPOINT`, `AI_CHAT_STREAM_ENDPOINT`,
`isDesktop`, `isWeb`, `logger` - All 184 COMMANDS entries still exist in the
COMMANDS map - `pnpm type-check` passes - `pnpm build` passes - No switch case
was dropped — all 184 commands still handled </acceptance_criteria> <verify>
<automated>pnpm type-check && pnpm build && ls
apps/frontend/src/adapters/web/modules/ | wc -l</automated> </verify> <done>Web
adapter split into 18+ domain modules. Switch delegates to modules. Build and
type-check pass.</done> </task>

<task type="auto">
  <name>task 2: Split types.ts into domain files with barrel re-export, remove ActivityLegacy</name>
  <files>apps/frontend/src/lib/types.ts, apps/frontend/src/lib/types/account.ts, apps/frontend/src/lib/types/activity.ts, apps/frontend/src/lib/types/asset.ts, apps/frontend/src/lib/types/holding.ts, apps/frontend/src/lib/types/portfolio.ts, apps/frontend/src/lib/types/quote.ts, apps/frontend/src/lib/types/goal.ts, apps/frontend/src/lib/types/settings.ts, apps/frontend/src/lib/types/taxonomy.ts, apps/frontend/src/lib/types/health.ts, apps/frontend/src/lib/types/ai.ts, apps/frontend/src/lib/types/sync.ts, apps/frontend/src/lib/types/device.ts, apps/frontend/src/lib/types/alternative-assets.ts, apps/frontend/src/lib/types/contributions.ts, apps/frontend/src/lib/types/liabilities.ts, apps/frontend/src/lib/types/net-worth.ts, apps/frontend/src/lib/types/fx.ts, apps/frontend/src/lib/types/common.ts</files>
  <read_first>
    - apps/frontend/src/lib/types.ts (FULL 1,929-line file — understand every interface and its grouping)
    - apps/frontend/src/lib/constants.ts (types.ts re-exports some types from constants)
  </read_first>
  <action>Split types.ts into domain-specific files with barrel re-export per D-13, and remove ActivityLegacy per D-14.

**Step 1: Create domain type files** in `apps/frontend/src/lib/types/`
directory:

1. `types/account.ts` — Account, AccountSummaryView, AccountGroup,
   AccountValuation, AccountDeposit
2. `types/activity.ts` — Activity, ActivityDetails, ActivitySearchResponse,
   ActivityCreate, ActivityUpdate, ActivityBulkMutation*, ActivityImport,
   ImportMappingData, ParseConfig, ImportTemplateData, CsvRow*, ParseError,
   ParsedCsvResult, ImportValidation*, ImportActivities*, SymbolInput,
   ImportRun\*, ImportAssetCandidate, ImportAssetPreviewItem,
   ImportAssetPreviewStatus, ReviewMode, BreakdownItem, Tag, TemplateKind,
   TemplateContextKind, BrokerProfileScope. **DO NOT include ActivityLegacy**
   (being removed per D-14)
3. `types/asset.ts` — Asset, NewAsset, AssetClassifications,
   AssetTaxonomyAssignment, NewAssetTaxonomyAssignment, UpdateAssetProfile,
   StaleAssetInfo, Instrument, SymbolSearchResult
4. `types/holding.ts` — Holding, HoldingSummary, CashHolding,
   AllocationHoldings, Position, Lot, MonetaryValue, SnapshotInfo,
   HoldingsPositionInput, HoldingsSnapshotInput, ImportHoldingsCsvResult,
   SymbolCheckResult, CheckHoldingsImportResult, AlternativeAssetHolding,
   PerformanceMetrics, SimplePerformanceMetrics, ReturnData, AssetsSection
5. `types/portfolio.ts` — PortfolioAllocations, DateRange, TimePeriod,
   IncomeSummary, IncomeByAsset, IncomeByAccount
6. `types/quote.ts` — Quote, QuoteUpdate, LatestQuoteSnapshot, ResolvedQuote,
   ExchangeInfo, MarketData, MarketDataProviderInfo
7. `types/goal.ts` — Goal, GoalAllocation, GoalProgress
8. `types/settings.ts` — Settings, SettingsContextType, NetWorthConfig
9. `types/taxonomy.ts` — Taxonomy*, TaxonomyAllocation, CategoryRef,
   CategoryWithWeight, CategoryAllocation, MigrationStatus, MigrationResult,
   TaxonomyJson*, TaxonomyCategoryJson, TaxonomyInstrumentMappingJson,
   NewTaxonomy, NewTaxonomyCategory
10. `types/health.ts` — HealthIssue, HealthStatus, HealthConfig, HealthSeverity,
    HealthCategory, AffectedItem, FixAction, NavigateAction
11. `types/ai.ts` — AiProvidersResponse, MergedModel, MergedProvider,
    ModelCapabilities, ModelCapabilityOverrides, ModelCapabilityOverrideUpdate,
    ConnectionField, CapabilityInfo, ProviderTuning, ProviderTuningOverrides,
    ListModelsResponse, FetchedModel, UpdateProviderSettingsRequest,
    SetDefaultProviderRequest
12. `types/sync.ts` — SyncStatus, BrokerSyncState, BrokerSyncProfileData,
    SaveBrokerSyncProfileRulesRequest, LinkLiabilityRequest, LiabilitiesSection
13. `types/device.ts` — TrackedItem, Platform
14. `types/alternative-assets.ts` — AlternativeAssetKindApi,
    CollectibleMetadata, PreciousMetalMetadata, PropertyMetadata,
    VehicleMetadata, CreateAlternativeAssetRequest,
    CreateAlternativeAssetResponse, UpdateValuationRequest,
    UpdateValuationResponse
15. `types/contributions.ts` — ContributionLimit, NewContributionLimit,
    DepositsCalculation
16. `types/liabilities.ts` — LiabilityMetadata, LinkLiabilityRequest,
    UnlinkLiabilityRequest
17. `types/net-worth.ts` — NetWorthResponse, NetWorthHistoryPoint
18. `types/fx.ts` — ExchangeRate
19. `types/common.ts` — UpdateInfo, ValidationResult, TrackingMode

**Step 2: Create barrel re-export file.** Replace
`apps/frontend/src/lib/types.ts` with:

```typescript
// Barrel re-export — backward compatible with all existing imports
// import { X } from "@/lib/types" continues to work

export * from "./types/account";
export * from "./types/activity";
export * from "./types/asset";
export * from "./types/holding";
export * from "./types/portfolio";
export * from "./types/quote";
export * from "./types/goal";
export * from "./types/settings";
export * from "./types/taxonomy";
export * from "./types/health";
export * from "./types/ai";
export * from "./types/sync";
export * from "./types/device";
export * from "./types/alternative-assets";
export * from "./types/contributions";
export * from "./types/liabilities";
export * from "./types/net-worth";
export * from "./types/fx";
export * from "./types/common";

// Re-export types from constants (preserving existing re-exports)
export type { HoldingCategoryFilterId } from "./constants";
export type { ActivitySubtype, ImportRequiredField } from "./constants";
```

**Step 3: Remove ActivityLegacy** per D-14. The `ActivityLegacy` interface at
line 70 is deprecated (since line 68) and has zero external usages (verified in
research). Simply omit it from the new `types/activity.ts` file.

**Important notes:**

- Some types reference other types across domains (e.g., `Activity` references
  `ActivityDetails`). These cross-references use `import` between domain files.
- Types that are currently re-exported from constants
  (`HoldingCategoryFilterId`, `ActivitySubtype`, `ImportRequiredField`) must
  continue to be re-exported from the barrel.
- Maintain the exact same export names — every interface/type that was exported
  from types.ts must still be accessible via `import { X } from "@/lib/types"`.
- The `importActivitySchema`, `importMappingSchema`, `parseConfigSchema` zod
  inference types must be in the same file that imports the schemas.</action>
  <acceptance_criteria> - `apps/frontend/src/lib/types/` directory exists with
  18+ domain type files - `apps/frontend/src/lib/types.ts` is a barrel re-export
  file (under 50 lines) -
  `grep -c "ActivityLegacy" apps/frontend/src/lib/types/ apps/frontend/src/lib/types.ts`
  returns 0 (deprecated type removed) -
  `grep "export \* from" apps/frontend/src/lib/types.ts | wc -l` returns 19+
  (all domain files re-exported) - `pnpm type-check` passes (all existing
  imports resolve correctly) - `pnpm build` passes - `pnpm test` passes
  </acceptance_criteria> <verify> <automated>pnpm type-check && pnpm build &&
  pnpm test && grep -c "ActivityLegacy" apps/frontend/src/lib/types.ts
  apps/frontend/src/lib/types/activity.ts 2>/dev/null</automated> </verify>
  <done>Types.ts split into 18+ domain files with barrel re-export.
  ActivityLegacy removed. All builds and tests pass.</done> </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                       |
| ----------------------------- | ------------------------------------------------- |
| Module imports → Runtime      | Broken import paths cause runtime errors          |
| Type re-exports → Type system | Missing barrel re-exports cause TypeScript errors |

## STRIDE Threat Register

| Threat ID | Category | Component            | Disposition | Mitigation Plan                                                |
| --------- | -------- | -------------------- | ----------- | -------------------------------------------------------------- |
| T-04-01   | D        | Type imports         | mitigate    | type-check verification ensures all re-exports resolve         |
| T-04-02   | D        | Web adapter dispatch | mitigate    | Build verification + existing test suite catches dropped cases |

</threat_model>

<verification>
1. `pnpm type-check && pnpm build && pnpm test` — all pass
2. `ls apps/frontend/src/adapters/web/modules/ | wc -l` — 18+ module files
3. `ls apps/frontend/src/lib/types/ | wc -l` — 18+ domain type files
4. `grep -c "ActivityLegacy" apps/frontend/src/lib/types.ts` returns 0
5. `wc -l apps/frontend/src/adapters/web/core.ts` — significantly reduced from 1,394
6. `wc -l apps/frontend/src/lib/types.ts` — significantly reduced from 1,929 (barrel only)
</verification>

<success_criteria>

- Web adapter split into 18+ domain module files
- core.ts is a slim dispatcher (significantly fewer lines)
- types.ts is a barrel re-export (under 50 lines)
- 18+ domain type files created
- ActivityLegacy removed with zero breakage
- All builds, type-checks, and tests pass
- All existing imports from @/lib/types continue working </success_criteria>

<output>
After completion, create `.planning/phases/01-codebase-health-rebrand/01-04-SUMMARY.md`
</output>
