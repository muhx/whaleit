---
phase: 01-codebase-health-rebrand
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/tauri/tauri.conf.json
  - apps/tauri/gen/apple/project.yml
  - apps/tauri/src/menu.rs
  - apps/tauri/src/lib.rs
  - apps/server/src/main_lib.rs
  - apps/server/src/api.rs
  - apps/server/src/api/settings.rs
  - crates/ai/src/system_prompt.txt
  - Dockerfile
  - compose.yml
  - compose.dev.yml
  - README.md
autonomous: true
requirements:
  - BRAND-01
  - BRAND-05
  - BRAND-06

must_haves:
  truths:
    - "Desktop app window title and menu show 'WhaleIt' not 'Wealthfolio'"
    - "Tauri bundle identifier is com.whaleit.app not com.teymz.wealthfolio"
    - "AI system prompt introduces itself as WhaleIt AI"
    - "README and Docker configuration reference WhaleIt"
    - "Internal Rust crate names remain wealthfolio-* unchanged"
  artifacts:
    - path: "apps/tauri/tauri.conf.json"
      provides: "Bundle identity, product name, deep-link schemes"
      contains: "com.whaleit"
    - path: "apps/tauri/src/menu.rs"
      provides: "Desktop menu labels"
      contains: "WhaleIt"
    - path: "crates/ai/src/system_prompt.txt"
      provides: "AI assistant identity"
      contains: "WhaleIt AI"
    - path: "README.md"
      provides: "Project documentation"
      contains: "WhaleIt"
  key_links:
    - from: "apps/tauri/tauri.conf.json"
      to: "desktop build"
      via: "productName, identifier fields"
      pattern: "productName.*WhaleIt"
---

<objective>
Rename all user-facing "Wealthfolio" strings in backend (Rust), configuration, and documentation to "WhaleIt".

Purpose: Per D-03 (Rust user-facing strings), D-04 (Tauri bundle identifier), D-05 (documentation rebrand), D-06/D-07 (internal crates unchanged). Covers BRAND-01 (user-facing text), BRAND-05 (docs), BRAND-06 (verify internals unchanged).

Output: All backend/config/docs files showing WhaleIt identity while internal crate references remain wealthfolio-*.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-codebase-health-rebrand/01-CONTEXT.md
@.planning/phases/01-codebase-health-rebrand/01-RESEARCH.md

<interfaces>
<!-- Key values executor needs to change: -->
<!-- tauri.conf.json: productName "Wealthfolio" → "WhaleIt", mainBinaryName "Wealthfolio" → "WhaleIt", identifier "com.teymz.wealthfolio" → "com.whaleit.app" -->
<!-- tauri.conf.json: deep-link schemes "wealthfolio" → "whaleit", update endpoints wealthfolio.app → whaleit.app -->
<!-- menu.rs line 6: "Wealthfolio" → "WhaleIt", line 40: "About Wealthfolio" → "About WhaleIt" -->
<!-- system_prompt.txt: "You are Wealthfolio AI" → "You are WhaleIt AI" -->
<!-- Internal crate refs (use wealthfolio_*) must NOT change — 577+ references -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Update Tauri bundle config, Apple project, and Rust user-facing strings</name>
  <files>apps/tauri/tauri.conf.json, apps/tauri/gen/apple/project.yml, apps/tauri/src/menu.rs, apps/tauri/src/lib.rs, apps/server/src/main_lib.rs, apps/server/src/api.rs, apps/server/src/api/settings.rs</files>
  <read_first>
    - apps/tauri/tauri.conf.json (full file — bundle identity, deep-links, updater, window title)
    - apps/tauri/gen/apple/project.yml (bundle ID, product name, entitlement paths)
    - apps/tauri/src/menu.rs (menu labels)
    - apps/tauri/src/lib.rs (error message at line 612)
    - apps/server/src/main_lib.rs (device display name at line 411)
    - apps/server/src/api.rs (OpenAPI tag at line 65)
    - apps/server/src/api/settings.rs (backup filename at line 331)
  </read_first>
  <action>Update all user-facing "Wealthfolio" strings in Tauri config and Rust source files per D-03 and D-04. Specific changes:

  **apps/tauri/tauri.conf.json:**
  - `productName`: `"Wealthfolio"` → `"WhaleIt"`
  - `mainBinaryName`: `"Wealthfolio"` → `"WhaleIt"`
  - `identifier`: `"com.teymz.wealthfolio"` → `"com.whaleit.app"`
  - `updater.endpoints`: `wealthfolio.app` → `whaleit.app` in URL
  - `deep-link.hosts`: `connect.wealthfolio.app` → `connect.whaleit.app`, `connect-staging.wealthfolio.app` → `connect-staging.whaleit.app`
  - `deep-link.schemes`: `"wealthfolio"` → `"whaleit"` (both iOS and desktop entries)
  - Window `title`: `"Wealthfolio"` → `"WhaleIt"`

  **apps/tauri/gen/apple/project.yml:**
  - `bundleIdPrefix`: `com.teymz.wealthfolio` → `com.whaleit`
  - `PRODUCT_NAME`: `Wealthfolio` → `WhaleIt`
  - `PRODUCT_BUNDLE_IDENTIFIER`: `com.teymz.wealthfolio` → `com.whaleit.app`
  - Entitlement file path references: `wealthfolio-app_iOS` → keep as-is (internal path, no user visibility) — actually rename to match new bundle naming for consistency: no, D-06 says internal crate paths stay. These are build artifact paths. Leave `wealthfolio-app_iOS` references as-is since they're build directory names.

  **apps/tauri/src/menu.rs:**
  - Line 6: `"Wealthfolio"` → `"WhaleIt"` (submenu label)
  - Line 40: `"About Wealthfolio"` → `"About WhaleIt"` (about dialog item)
  - Line 90: `"latest version of Wealthfolio"` → `"latest version of WhaleIt"` (update message)

  **apps/tauri/src/lib.rs:**
  - Line 612: `"Failed to build Wealthfolio application"` → `"Failed to build WhaleIt application"`

  **apps/server/src/main_lib.rs:**
  - Line 411: `"Wealthfolio Server"` → `"WhaleIt Server"` (device display name)

  **apps/server/src/api.rs:**
  - Line 65: `tags((name="wealthfolio"))` → `tags((name="whaleit"))` (OpenAPI tag)

  **apps/server/src/api/settings.rs:**
  - Line 331: `"wealthfolio_backup_"` → `"whaleit_backup_"` (backup filename prefix)

  **DO NOT CHANGE:**
  - `apps/server/src/auth.rs` lines 145, 225, 228 — cryptographic salt strings `"wealthfolio-web"`, `"wealthfolio-jwt"`, `"wealthfolio-secrets"` — changing these invalidates existing tokens/secrets
  - Any `use wealthfolio_*` import statements (577+ references)
  - Any `wealthfolio_*::` crate path references
  - `apps/tauri/src/commands/wealthfolio_connect.rs` filename and internal content (keep as-is per D-06)
  - `apps/tauri/src/services/connect_service.rs` comments referencing "Wealthfolio Connect" — this is a cloud service product name, update to "WhaleIt Connect"</action>
  <acceptance_criteria>
    - `apps/tauri/tauri.conf.json` contains `"productName": "WhaleIt"` (not "Wealthfolio")
    - `apps/tauri/tauri.conf.json` contains `"identifier": "com.whaleit.app"` (not "com.teymz.wealthfolio")
    - `apps/tauri/tauri.conf.json` contains `"schemes": ["whaleit"]` (not "wealthfolio")
    - `apps/tauri/src/menu.rs` contains `"WhaleIt"` and `"About WhaleIt"`
    - `crates/ai/src/system_prompt.txt` not yet modified (separate task 2)
    - `grep -c '"Wealthfolio"' apps/tauri/src/menu.rs` returns 0
    - `grep -c 'name = "wealthfolio' Cargo.toml` returns same count as before (internal crates unchanged)
  </acceptance_criteria>
  <verify>
    <automated>cargo check && grep -c '"Wealthfolio"' apps/tauri/tauri.conf.json apps/tauri/src/menu.rs apps/tauri/src/lib.rs</automated>
  </verify>
  <done>All user-facing Rust strings and Tauri config show WhaleIt. Internal crate references unchanged. cargo check passes.</done>
</task>

<task type="auto">
  <name>task 2: Update documentation, Docker, compose, AI prompt, and remaining server strings</name>
  <files>Dockerfile, compose.yml, compose.dev.yml, README.md, crates/ai/src/system_prompt.txt, apps/tauri/src/services/connect_service.rs</files>
  <read_first>
    - Dockerfile (full file — labels, binary names, paths)
    - compose.yml (full file — service names, image, container name, volumes)
    - compose.dev.yml (full file)
    - README.md (full file — 54 wealthfolio references)
    - crates/ai/src/system_prompt.txt (AI identity line)
    - apps/tauri/src/services/connect_service.rs (comments line 1 and 59)
  </read_first>
  <action>Update all documentation and infrastructure config per D-05. Specific changes:

  **crates/ai/src/system_prompt.txt:**
  - Line 1: `"You are Wealthfolio AI"` → `"You are WhaleIt AI"`

  **apps/tauri/src/services/connect_service.rs:**
  - Line 1: `"Wealthfolio Connect cloud API"` → `"WhaleIt Connect cloud API"`
  - Line 59: `"Wealthfolio Connect cloud API"` → `"WhaleIt Connect cloud API"`

  **Dockerfile:**
  - Line 8: `"Wealthfolio Connect configuration"` → `"WhaleIt Connect configuration"`
  - Line 32: `"Wealthfolio Connect configuration"` → `"WhaleIt Connect configuration"`
  - Line 71: `"wealthfolio-server"` → `"whaleit-server"` (binary name — this IS user-facing in Docker)
  - Line 77: `"wealthfolio-server"` → `"whaleit-server"`
  - Line 79: `"wealthfolio.db"` → keep as-is (database filename — changing breaks existing deployments)
  - Line 85: `"wealthfolio-server"` → `"whaleit-server"`
  - Comment references: `"Wealthfolio"` → `"WhaleIt"`

  **compose.yml:**
  - Line 1: `"Wealthfolio - Production"` → `"WhaleIt - Production"`
  - Service name `wealthfolio:` → `whaleit:`
  - `image: afadil/wealthfolio:latest` → `afadil/whaleit:latest`
  - `container_name: wealthfolio` → `container_name: whaleit`
  - Volume `wealthfolio-data:` → `whaleit-data:`
  - `WF_DB_PATH` value `"/data/wealthfolio.db"` → keep as-is (backward compat)
  - Comment `"wealthfolio.example.com"` → `"whaleit.example.com"`

  **compose.dev.yml:**
  - Line 1: `"Wealthfolio - Development"` → `"WhaleIt - Development"`
  - Service name `wealthfolio:` → `whaleit:`
  - `WF_DB_PATH` value keep as-is

  **README.md:**
  - All 54 "Wealthfolio/wealthfolio" references → "WhaleIt/whaleit"
  - Title, URLs, description, badges, links all updated
  - GitHub URLs: `afadil/wealthfolio` → `muhx/whaleit` (new repo location)
  - Website: `wealthfolio.app` → `whaleit.app`
  - Discord, Twitter links keep their actual URLs (don't change external service URLs unless they're also being rebranded)
  - Tagline: `"A Beautiful and Boring Desktop Investment Tracker"` → `"Your Friendly Finance Companion"` per the new branding</action>
  <acceptance_criteria>
    - `crates/ai/src/system_prompt.txt` contains `"WhaleIt AI"` (not "Wealthfolio AI")
    - `Dockerfile` contains `"whaleit-server"` (not "wealthfolio-server" for binary names)
    - `compose.yml` service name is `whaleit:` (not `wealthfolio:`)
    - `compose.yml` container_name is `whaleit`
    - `README.md` contains `"WhaleIt"` in title and description
    - `README.md` does NOT contain `"Wealthfolio"` in any user-visible text
    - `grep -c "Wealthfolio" Dockerfile compose.yml compose.dev.yml README.md` returns 0 (or only internal/deployment-preservation references)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Wealthfolio" crates/ai/src/system_prompt.txt Dockerfile compose.yml README.md apps/tauri/src/services/connect_service.rs</automated>
  </verify>
  <done>All documentation, Docker/compose config, and AI prompt show WhaleIt branding. No user-facing "Wealthfolio" remains in backend/docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config → Build | Tauri config values are trusted build inputs |
| Docker → Deployment | Container names and image names affect network identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | S | Bundle identifier | mitigate | Verify new identifier `com.whaleit.app` doesn't conflict with existing app registrations |
| T-02-02 | I | Deep-link schemes | accept | Scheme change from wealthfolio:// to whaleit:// breaks existing deep links but this is expected during rebrand |
| T-02-03 | T | Auth salt strings | mitigate | Explicitly preserve `auth.rs` salt strings unchanged — documented in task action as DO NOT CHANGE |
</threat_model>

<verification>
1. `cargo check` — Rust code compiles with new strings
2. `grep -c "Wealthfolio" apps/tauri/tauri.conf.json` returns 0
3. `grep -c "Wealthfolio" README.md` returns 0
4. `grep 'name = "wealthfolio' Cargo.toml` returns all original internal crate names (BRAND-06 verification)
</verification>

<success_criteria>
- All user-facing Rust strings show "WhaleIt"
- Tauri config has new bundle identifier com.whaleit.app
- README fully rebranded
- Docker/compose use whaleit naming
- AI prompt says "WhaleIt AI"
- Internal crate names verified unchanged
- cargo check passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-codebase-health-rebrand/01-02-SUMMARY.md`
</output>
