---
phase: 02-dual-database-engine
plan: 06
type: execute
wave: 5
depends_on: ["02-05"]
files_modified:
  - crates/storage-postgres/src/fx/repository.rs
  - crates/storage-postgres/src/fx/model.rs
  - crates/storage-postgres/src/market_data/repository.rs
  - crates/storage-postgres/src/market_data/model.rs
  - crates/storage-postgres/src/portfolio/snapshot/repository.rs
  - crates/storage-postgres/src/portfolio/snapshot/model.rs
  - crates/storage-postgres/src/portfolio/valuation/repository.rs
  - crates/storage-postgres/src/portfolio/valuation/model.rs
autonomous: true
gap_closure: true
requirements: [DB-02, DB-05]

must_haves:
  truths:
    - "FxRepositoryTrait methods execute real diesel-async queries against
      PostgreSQL"
    - "QuoteStore and ProviderSettingsStore methods execute real queries (not
      'not yet implemented')"
    - "SnapshotRepositoryTrait methods execute real queries for portfolio
      snapshots"
    - "ValuationRepositoryTrait methods execute real queries for daily
      valuations"
    - "No 'not yet implemented' strings remain in fx, market_data, portfolio
      modules"
  artifacts:
    - path: "crates/storage-postgres/src/fx/repository.rs"
      provides: "Full FxRepositoryTrait implementation"
      min_lines: 50
      contains: "impl FxRepositoryTrait"
    - path: "crates/storage-postgres/src/market_data/repository.rs"
      provides: "Full QuoteStore + ProviderSettingsStore implementation"
      min_lines: 80
      contains: "impl QuoteStore"
    - path: "crates/storage-postgres/src/portfolio/snapshot/repository.rs"
      provides: "Full SnapshotRepositoryTrait implementation"
      min_lines: 60
      contains: "impl SnapshotRepositoryTrait"
    - path: "crates/storage-postgres/src/portfolio/valuation/repository.rs"
      provides: "Full ValuationRepositoryTrait implementation"
      min_lines: 40
      contains: "impl ValuationRepositoryTrait"
  key_links:
    - from: "crates/storage-postgres/src/fx/repository.rs"
      to: "crates/core/src/fx/"
      via: "impl FxRepositoryTrait for PgFxRepository"
      pattern: "impl FxRepositoryTrait"
    - from: "crates/storage-postgres/src/portfolio/snapshot/repository.rs"
      to: "crates/core/src/portfolio/snapshot/"
      via: "impl SnapshotRepositoryTrait for PgSnapshotRepository"
      pattern: "impl SnapshotRepositoryTrait"
---

<objective>
Replace all stub implementations in fx, market_data, and portfolio PG repositories with real diesel-async queries.

Purpose: Four PG repository modules (fx, market_data, portfolio/snapshot,
portfolio/valuation) have methods that return empty defaults or "not yet
implemented" errors. This means queries against PostgreSQL for FX rates, market
data quotes, portfolio snapshots, and valuations silently return no data. This
plan implements all stub methods with proper diesel-async database operations.

Output: All repository methods execute real PostgreSQL queries. Zero "not yet
implemented" strings in these modules. </objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dual-database-engine/02-CONTEXT.md
@.planning/phases/02-dual-database-engine/02-VERIFICATION.md
@.planning/phases/02-dual-database-engine/02-02-SUMMARY.md

<interfaces>
<!-- Established PG repo pattern from completed modules. -->

From crates/storage-postgres/src/accounts/repository.rs (reference pattern):

```rust
// Standard PG repo pattern:
// 1. Struct holds Arc<PgPool>
// 2. Read methods: pool.get().await? → diesel query → .load()/first() → convert via From<DB> impl
// 3. Write methods: pool.get().await? → conn.transaction(Box::pin(async { ... })) → convert
// 4. All methods are async (async_trait)
```

Key schema tables (from crates/storage-postgres/src/schema.rs):

- quotes: id, symbol, date, open, high, low, close, volume, source, created_at,
  updated_at
- market_data_providers: id, provider_type, settings_json, enabled, created_at,
  updated_at
- market_data_custom_providers: id, name, api_url, api_key_ref, headers_json,
  enabled, created_at, updated_at
- holdings_snapshots: id, account_id, date, cash_value, holdings_value,
  total_value, currency, source, created_at, updated_at
- daily_account_valuation: id, account_id, date, value, currency, source,
  created_at, updated_at

Core trait return types:

- ExchangeRate (from whaleit_core::fx)
- Quote (from whaleit_core::quotes)
- AccountStateSnapshot (from whaleit_core::portfolio::snapshot)
- DailyAccountValuation, NegativeBalanceInfo (from
  whaleit_core::portfolio::valuation) </interfaces> </context>

<tasks>

<task type="auto">
  <name>task 1: Implement FX and market_data PG repositories</name>
  <files>
    crates/storage-postgres/src/fx/repository.rs,
    crates/storage-postgres/src/fx/model.rs,
    crates/storage-postgres/src/market_data/repository.rs,
    crates/storage-postgres/src/market_data/model.rs
  </files>
  <read_first>
    crates/storage-postgres/src/fx/repository.rs,
    crates/storage-postgres/src/market_data/repository.rs,
    crates/storage-sqlite/src/fx/repository.rs,
    crates/storage-sqlite/src/market_data/repository.rs,
    crates/core/src/fx/fx_traits.rs,
    crates/core/src/quotes/mod.rs,
    crates/core/src/quotes/types.rs,
    crates/storage-postgres/src/schema.rs,
    crates/storage-postgres/src/accounts/repository.rs
  </read_first>
  <action>
    Replace all stub implementations with real diesel-async queries.

    **FX Module:**

    1. Create `fx/model.rs` with PG Diesel model structs:
       - `QuoteDB` mapping to `quotes` table (Queryable, Selectable)
       - `NewQuoteDB` for inserts (Insertable)
       - `From<QuoteDB> for whaleit_core::quotes::Quote` conversion impl
       - `From<QuoteDB> for ExchangeRate` conversion impl (quotes table serves dual purpose for FX rates and market data)

    2. Rewrite `fx/repository.rs` implementing all FxRepositoryTrait methods:
       - `get_latest_exchange_rates()` → SELECT from quotes WHERE source LIKE '%fx%' ORDER BY date DESC. Use `diesel_async::RunQueryDsl`. Pool get, query, load, convert.
       - `get_historical_exchange_rates()` → SELECT from quotes WHERE source LIKE '%fx%' ORDER BY date DESC with date range filter
       - `get_latest_exchange_rate(from, to)` → SELECT from quotes WHERE symbol = format!("{from}{to}") OR symbol = format!("{from}/{to}") ORDER BY date DESC LIMIT 1
       - `get_latest_exchange_rate_by_symbol(symbol)` → SELECT from quotes WHERE symbol = ? ORDER BY date DESC LIMIT 1
       - `get_historical_quotes(symbol, start, end)` → SELECT from quotes WHERE symbol AND date range
       - `add_quote(symbol, date, rate, source)` → INSERT into quotes, return inserted Quote
       - `save_exchange_rate(rate)` → INSERT INTO quotes (upsert via ON CONFLICT), return ExchangeRate
       - `update_exchange_rate(rate)` → UPDATE quotes SET ... WHERE id = ?, return ExchangeRate
       - `delete_exchange_rate(rate_id)` → DELETE FROM quotes WHERE id = ?
       - `create_fx_asset(from_currency, to_currency, source)` → INSERT asset, return asset id string

    Read the SQLite reference (`crates/storage-sqlite/src/fx/repository.rs`) to understand the exact query logic for each method. Replicate the query structure using diesel-async.

    **Market Data Module:**

    3. Create `market_data/model.rs` with PG Diesel model structs:
       - `QuoteDB` (same as fx, or re-export from fx — check if they share the same table)
       - `ProviderSettingDB` mapping to `market_data_providers` table
       - `NewProviderSettingDB` for inserts
       - Conversion impls (`From<ProviderSettingDB> for MarketDataProviderSetting`, etc.)

    4. Rewrite `market_data/repository.rs` implementing:
       - All `QuoteStore` methods (save_quote, delete_quote, upsert_quotes, delete_quotes_for_asset, delete_provider_quotes_for_asset, latest, range, latest_batch, latest_with_previous, get_quote_bounds_for_assets, get_latest_quote, get_latest_quotes, get_latest_quotes_pair, get_historical_quotes, get_all_historical_quotes, get_quotes_in_range, find_duplicate_quotes)
       - All `ProviderSettingsStore` methods (get_all_providers, get_provider, update_provider)
       - Use the SQLite reference (`crates/storage-sqlite/src/market_data/repository.rs`) for exact query logic

    Follow the established PG repo pattern: `pool.get().await?` → diesel query with diesel-async → `.load().await` or `.first().await` → convert via `From<DB>` impls.

  </action>
  <verify>
    <automated>grep -c "not yet implemented" crates/storage-postgres/src/fx/repository.rs crates/storage-postgres/src/market_data/repository.rs 2>/dev/null</automated>
  </verify>
  <done>
    - FxRepositoryTrait fully implemented with real diesel-async queries
    - QuoteStore fully implemented with real diesel-async queries
    - ProviderSettingsStore fully implemented with real diesel-async queries
    - Zero "not yet implemented" strings in fx and market_data modules
    - cargo check -p whaleit-storage-postgres passes
  </done>
</task>

<task type="auto">
  <name>task 2: Implement portfolio/snapshot and portfolio/valuation PG repositories</name>
  <files>
    crates/storage-postgres/src/portfolio/snapshot/repository.rs,
    crates/storage-postgres/src/portfolio/snapshot/model.rs,
    crates/storage-postgres/src/portfolio/valuation/repository.rs,
    crates/storage-postgres/src/portfolio/valuation/model.rs
  </files>
  <read_first>
    crates/storage-postgres/src/portfolio/snapshot/repository.rs,
    crates/storage-postgres/src/portfolio/valuation/repository.rs,
    crates/storage-sqlite/src/portfolio/snapshot/repository.rs,
    crates/storage-sqlite/src/portfolio/valuation/repository.rs,
    crates/core/src/portfolio/snapshot/mod.rs,
    crates/core/src/portfolio/valuation/mod.rs,
    crates/storage-postgres/src/schema.rs,
    crates/storage-postgres/src/accounts/repository.rs
  </read_first>
  <action>
    Replace all stub implementations with real diesel-async queries.

    **Portfolio Snapshot Module:**

    1. Create `portfolio/snapshot/model.rs` with PG Diesel models:
       - `SnapshotDB` mapping to `holdings_snapshots` table (Queryable, Selectable)
       - `NewSnapshotDB` for inserts (Insertable)
       - `From<SnapshotDB> for AccountStateSnapshot` conversion
       - `From<&AccountStateSnapshot> for NewSnapshotDB` conversion

    2. Rewrite `portfolio/snapshot/repository.rs` implementing all SnapshotRepositoryTrait methods:
       - `save_snapshots(snapshots)` → INSERT batch into holdings_snapshots
       - `get_snapshots_by_account(account_id, start, end)` → SELECT with optional date range
       - `get_latest_snapshot_before_date(account_id, date)` → SELECT WHERE date < ? ORDER BY date DESC LIMIT 1
       - `get_latest_snapshots_before_date(account_ids, date)` → batch version of above
       - `get_all_latest_snapshots(account_ids)` → get most recent snapshot per account
       - `delete_snapshots_by_account_ids(account_ids)` → DELETE WHERE account_id IN (...)
       - `delete_snapshots_for_account_and_dates(account_id, dates)` → DELETE WHERE date IN (...)
       - `delete_snapshots_for_account_in_range(account_id, start, end)` → DELETE WHERE date range
       - `overwrite_snapshots_for_account_in_range(...)` → DELETE + INSERT in transaction
       - `overwrite_multiple_account_snapshot_ranges(...)` → batch version
       - `get_total_portfolio_snapshots(start, end)` → SELECT across accounts
       - `get_all_non_archived_account_snapshots(start, end)` → SELECT with join/filter
       - `get_earliest_snapshot_date(account_id)` → SELECT MIN(date)
       - `overwrite_all_snapshots_for_account(...)` → DELETE all + INSERT in transaction
       - `update_snapshots_source(account_id, new_source)` → UPDATE SET source = ?
       - `save_or_update_snapshot(snapshot)` → INSERT ON CONFLICT UPDATE
       - `get_non_calculated_snapshot_count(account_id)` → COUNT WHERE source != 'calculated'
       - `get_earliest_non_calculated_snapshot(account_id)` → SELECT WHERE source != 'calculated' ORDER BY date ASC LIMIT 1

    Read the SQLite reference (`crates/storage-sqlite/src/portfolio/snapshot/repository.rs`) for exact query logic.

    **Portfolio Valuation Module:**

    3. Create `portfolio/valuation/model.rs` with PG Diesel models:
       - `ValuationDB` mapping to `daily_account_valuation` table (Queryable, Selectable)
       - `NewValuationDB` for inserts (Insertable)
       - `From<ValuationDB> for DailyAccountValuation` conversion

    4. Rewrite `portfolio/valuation/repository.rs` implementing all ValuationRepositoryTrait methods:
       - `save_valuations(records)` → INSERT batch into daily_account_valuation
       - `get_historical_valuations(account_id, start, end)` → SELECT with optional date range
       - `load_latest_valuation_date(account_id)` → SELECT MAX(date)
       - `delete_valuations_for_account(account_id, since_date)` → DELETE with optional date filter
       - `get_latest_valuations(account_ids)` → get most recent valuation per account
       - `get_valuations_on_date(account_ids, date)` → SELECT WHERE date = ?
       - `get_accounts_with_negative_balance(account_ids)` → SELECT WHERE value < 0

    Read the SQLite reference (`crates/storage-sqlite/src/portfolio/valuation/repository.rs`) for exact query logic.

    **Transaction pattern for writes (overwrite methods):**
    ```rust
    let mut conn = self.pool.get().await.map_err(StoragePgError::from)?;
    conn.transaction::<_, diesel::result::Error, _>(|conn| {
        Box::pin(async move {
            // DELETE existing
            // INSERT new
            Ok(())
        })
    }).await?;
    ```

    After implementation, verify: `cargo check -p whaleit-storage-postgres` passes with 0 errors.

  </action>
  <verify>
    <automated>grep -c "not yet implemented" crates/storage-postgres/src/portfolio/snapshot/repository.rs crates/storage-postgres/src/portfolio/valuation/repository.rs 2>/dev/null</automated>
  </verify>
  <done>
    - SnapshotRepositoryTrait fully implemented with real diesel-async queries
    - ValuationRepositoryTrait fully implemented with real diesel-async queries
    - Zero "not yet implemented" strings in portfolio modules
    - cargo check -p whaleit-storage-postgres passes
    - cargo check -p whaleit-server --features postgres passes
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                                                                                            |
| --------------- | ---------------------------------------------------------------------------------------------------------------------- |
| PG query inputs | Repository method parameters (account_id, dates) used in queries — parameterized via Diesel DSL, no SQL injection risk |

## STRIDE Threat Register

| Threat ID  | Category | Component          | Disposition | Mitigation Plan                                                       |
| ---------- | -------- | ------------------ | ----------- | --------------------------------------------------------------------- |
| T-02-06-01 | T        | portfolio/snapshot | mitigate    | All queries use Diesel parameterized DSL — no raw SQL with user input |
| T-02-06-02 | I        | fx/market_data     | mitigate    | Symbol strings used via Diesel DSL bindings, not string interpolation |

</threat_model>

<verification>
1. `cargo check -p whaleit-storage-postgres` — 0 errors
2. `cargo check -p whaleit-server --features postgres` — 0 errors
3. `grep -r "not yet implemented" crates/storage-postgres/src/fx/ crates/storage-postgres/src/market_data/ crates/storage-postgres/src/portfolio/` — 0 matches
4. `grep "impl FxRepositoryTrait" crates/storage-postgres/src/fx/repository.rs` — found
5. `grep "impl QuoteStore" crates/storage-postgres/src/market_data/repository.rs` — found
6. `grep "impl SnapshotRepositoryTrait" crates/storage-postgres/src/portfolio/snapshot/repository.rs` — found
7. `grep "impl ValuationRepositoryTrait" crates/storage-postgres/src/portfolio/valuation/repository.rs` — found
</verification>

<success_criteria>

- All 4 repository modules (fx, market_data, portfolio/snapshot,
  portfolio/valuation) have real implementations
- Zero "not yet implemented" errors in these modules
- Full workspace compiles with postgres feature
- Query patterns follow established diesel-async + deadpool pattern from
  accounts repo </success_criteria>

<output>
After completion, create `.planning/phases/02-dual-database-engine/02-06-SUMMARY.md`
</output>
